{
  "$schema": "one unprotected home for the units-at-boot pins. Keys are game names; values are the EXACT `ran` count a Null_ build reports.",

  "$why": "These numbers used to be duplicated across .github/workflows/zm-tests.yml, zagent.project.json and run_unit_gate.ps1's -Baseline default. Two of those three are protectedPaths, so bumping a pin was work the agent loop could never do: adding a single test redded a REQUIRED check with zero failing tests, and the fix required editing files `zagent guard` refuses. That made every test-adding ticket unreachable, which is most feature work in a repo whose conventions ask for a test with all new code.",

  "$rule": "The gate asserts `ran == baseline` EXACTLY, so a GROWN suite fails with zero failures. Bump the number HERE and nowhere else -- the workflows and zagent.project.json now name the GAME, not the count.",

  "$never": "Do NOT pin from a Vulkan_ exe. The same tree reports higher on Vulkan than on Null (a standing +37 gap was measured for Zenithmon), because Vulkan builds also compile the Vulkan-only backend tests. Every consumer of this file runs a Null_ executable.",

  "$coverage": "NOT every row here is checked by CI, and that is now ENFORCED rather than merely noted. Test_UnitBaselineManifest.ps1 asserts every row is either named by some workflow's -Game or listed in `advisory` below with a reason. So a new ungated row cannot be added silently: you must gate it or declare it. A pin nothing reads can drift silently, which is the failure this file exists to prevent.",

  "$advisory": "Rows CI does not gate. Each needs a reason, and the reason should describe what it would take to gate it -- an entry here is a debt, not a category. Re-OBSERVE an advisory row rather than trusting it.",
  "advisory": {
    "RenderTest": "engine-gate.yml compiles RenderTest but only in Vulkan_vs2022_Debug_Win64_True, as a compile proof -- hosted runners have no Vulkan ICD, so the exe is never run, and pinning from a Vulkan exe is forbidden anyway (it reports higher than Null for the same tree). Gating this row needs a SECOND, Null_-config RenderTest build in engine-gate, which is an extra compile of a game that job already compiles once. That is a CI-budget decision: see ZEN-3. Do not delete the row -- RenderTest boots the same engine suite, so every backend-neutral engine unit moves it (ZM-49 +6, ZEN-2 +5), and local cross-game sweeps read it."
  },

  "baselines": {
    "$Zenithmon": "ZM boot units. Also narrated in Games/Zenithmon/Docs/Status.md, which stays the human-facing authority for WHY it moved; this file is what the gate reads.",
    "Zenithmon": 3394,

    "$Combat": "The engine boot pin. A backend-neutral ENGINE unit moves this AND every other game's number in the same commit, because they all boot the same engine suite.",
    "Combat": 1650,

    "$RenderTest": "Every number in this file is OBSERVED from a real Null_ run, never arithmetic on the previous one -- a computed pin is how a suite that also LOST a test still ratchets green. Do NOT narrate individual bumps here: that note drifts at the next bump (it already did once). git log carries the derivation.",
    "RenderTest": 1741
  }
}
