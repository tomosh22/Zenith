# Zenithmon Status

**Last updated:** 2026-07-11 -- **S2 box 3 progressed to SC4/5 DONE. SC4 (15 contact + status-try + stat-drop-veto + accuracy abilities) is COMPLETE and GREEN, landing in this commit with full behavioral coverage. Orchestrated fully via subagents (spec extraction -> parallel implement + test authoring on disjoint files -> adversarial review); the review found the C++ and tests correct with the zero-perturbation invariant intact, its only blocker being the CI baseline bump (applied). Next: S2 box 3 SC5 -- turn-end + faint-branch + quickdraw abilities, plus the all-50 complete-realization gate that finally ticks the box-3 Roadmap line.**

**Read this first each session.** [Roadmap.md](Roadmap.md) is the source of truth for what's next; [Questions.md](Questions.md) holds open decisions; [Shortfalls.md](Shortfalls.md) is the gap audit. The box-3 execution plan + design rulings are **ZM-D-036** (SC2 = ZM-D-039, SC3 = ZM-D-040, SC4 = **ZM-D-041**) in [DecisionLog.md](DecisionLog.md).

## Working model -- MASTER-ONLY, no branches/PRs (2026-07-10, ZM-D-031)

**All work is committed DIRECTLY to `master` and pushed** (`git push origin master`). NEVER create a feature branch, pull request, or git worktree. The **LOCAL gate is the pre-push authority**: `zenith build` + boot unit gate (`Tools/run_unit_gate.ps1 -Exe <exe> -Baseline N`, runs the ZM_* unit tests `zenith test` skips) + `zenith test --headless`, ALL green before you push. `zm-tests` CI runs post-push as a backstop only; on red, FIX FORWARD (never revert shipped history, force-push, or `gh run rerun`). See StartPrompts.md.

## Orchestrated-session model (this session)

Development is running under [OrchestratorPlaybook.md](OrchestratorPlaybook.md): the primary Claude instance is the ORCHESTRATOR -- it dispatches subagents for spec-extraction / code / test / review authoring but **owns ALL builds/tests/runs itself** (subagents never build and their "it compiles" claims are worthless), and it **alone writes the living docs** (Status/Roadmap/DecisionLog/Questions/Shortfalls) and owns the direct-master commit/push. SC3 + SC4 both landed this way. Per-SC pattern that worked well: (1) a spec-extraction subagent reads the design archive + `ZM_AbilityData` masks + the seams and writes a complete self-contained `<sc>_spec.md` to the orchestrator's scratchpad; (2) an Implementer and a Test Author author in PARALLEL on DISJOINT files from that one spec; (3) an adversarial Reviewer traces the diff; (4) the orchestrator builds, runs the unit gate + headless, applies doc updates, commits + pushes.

## Build / Tests (all GREEN this iteration)

- Regen: not needed (SC4 edits existing files only; no new TUs).
- Build GREEN (`Vulkan_vs2022_Debug_Win64_True`).
- Unit (boot gate): **1491 ran, 1490 passed, 0 failed, 1 skipped** (skip = pre-existing quarantined engine `RegistryWideNodeRoundTrip`). SC4 added **15** net-new `ZM_*` unit tests (1476 -> 1491; the two coverage gates were renamed SC3->SC4, net 0). **Baseline 1491** in `zm-tests.yml` (bumped this commit).
- Automated (P1): **1/1** (`ZM_Boot_Test`); `zenith test Zenithmon --headless` exits 0.
- Reviews GREEN: adversarial review verdict was "correct C++ + tests, zero-perturbation PASS, no false-confidence tests, coverage gates correct" -- its only FIX-REQUIRED item was the un-bumped baseline, now applied.

## What landed this session -- S2 box-3 SC4 (contact / status-try / stat-veto / accuracy abilities)

SC4 installs **15** ability rows as live hook function-pointers, reusing SC2/SC3 infra unchanged. **CONTACT** (`pfnOnContact`): STATICVEIL/CINDERSKIN/BARBSKIN proc a major status on the attacker at 30% (one `RandBelow(100)<30` per contact hit), THORNMAIL chips the attacker maxHP/8 with no draw. **STATUS_TRY** (`pfnPreventMajor`/`pfnPreventVolatile`): WAKEFUL/PUREBLOOD/THAWHEART/LIMBERLITHE/COLDBLOOD block their major, OWNPACE blocks CONFUSED -- veto+emit placed INSIDE `ApplyMajor`/`ApplyVolatile` (predicates kept pure; R-S1 refinement of ZM-D-036 §4.5, so the roll-then-veto order + goldens survive). **stat-drop-veto** (`pfnPreventStatDrop`): IRONWILL/GUARDIAN veto any foe DROP, KEENEYE only an accuracy drop (seam already live from SC2). **ACCURACY** (`pfnBypassAccuracy`): DEADAIM always, TRUESHOT in weather -- via an M5 wrapper that leaves the NONE-actor accuracy draw byte-identical. New seams: E4 `g_ApplyContactReactions` in `ZM_MoveExecutor::Execute` (once per contact move, skips a recoil-fainted attacker, defender-only, no SC5 dealt-faint) and a state+events `ApplyMajor` overload in `ZM_StatusLogic.{h,cpp}`. Full detail + the R-S1/R-C1/R-C2 rulings in ZM-D-041.

## Current task

**Next per [Roadmap.md](Roadmap.md) S2 box 3: SC5 -- the FINAL box-3 slice: turn-end + faint-branch + quickdraw abilities, plus the all-50 complete-realization gate.** This is the slice that ticks the Roadmap box-3 line. Scope (from ZM-D-036; re-extract the exact remaining roster before authoring):

- **TURN_END heal abilities** (`pfnOnTurnEnd`): dispatched in the EoT order AFTER the PLAYER-then-ENEMY status ticks and BEFORE the final `TURN_END` event; **skip fainted actives**. (The EoT order slot for this was reserved through SC1-SC4.)
- **FAINT-branch abilities** (deferred from SC4): BLOODRUSH (34, `pfnOnDealtFaint` -- self downs the foe -> its MODIFY_STAT boost, realized engine-side), LASTSPITE (35) + AFTERSHOCK (36) (`pfnOnContact` `bSelfFainted` branch -- self KO'd by contact). SC4 already lays the E4 contact seam; SC5 adds the attacker-side `pfnOnDealtFaint` dispatch and the `bSelfFainted` bodies.
- **QUICKDRAW** and any remaining move-order/priority ability (realizes MODIFY_STAT engine-side at the move-order speed seam, NOT flee).
- **The all-50 complete-realization gate:** the coverage-invariant test that asserts EVERY one of the 50 rows completely realizes its declared `m_uHookMask` (via live pfns + the documented engine-side exceptions) and that no slot is null. The SC4 coverage gates still assert `pfnOnTurnEnd`/`pfnOnDealtFaint` + BLOODRUSH/LASTSPITE/AFTERSHOCK null -- SC5 flips those and adds the all-50 check.

Recommended dispatch: the same 4-step orchestrated pattern used for SC3/SC4 (spec-extraction subagent -> `sc5_spec.md` -> parallel Implementer + Test Author -> adversarial Reviewer -> orchestrator builds/gates/commits). Edits to existing files -> no regen unless a new TU is genuinely added. **When SC5 lands, tick the Roadmap S2 box-3 line** ("Abilities via per-hook fn-pointer structs (~50 shipped) + weather"). After box 3, the remaining S2 boxes per Roadmap are `ZM_ExpAndLevel`, `ZM_BattleAI`, and `ZM_Breeding`/`ZM_Daycare`/`ZM_BattleTower`, then the S2 gate (~370 unit tests incl. seeded scenario battles + 2,000-battle fuzz soak).

## Notes for the next agent

- **Design archive (bonus, may be transient):** the full box-3 design (all 50 ability bodies + per-SC test lists) is at `C:\Users\tomos\AppData\Local\Temp\claude\c--dev-Zenith\6a05a4f2-a8e4-41e0-bb7e-b300ad0dc3ce\scratchpad\zm_s2_box3_design.md` (a session-temp file; may be gone). The AUTHORITATIVE sources are ZM-D-036/039/040/041 + the Roadmap S2 line + this file; re-derive the SC5 roster from `ZM_AbilityData.{h,cpp}` (the rows whose `pfnOnTurnEnd`/`pfnOnDealtFaint` slots or QUICKDRAW MODIFY_STAT are still uninstalled -- the SC4 coverage gates in `ZM_Tests_Abilities.cpp` still assert them null) + `GameDesignDocument.md`. The SC4 spec the orchestrator wrote lives at `<this-session-scratchpad>\sc4_spec.md`.
- **Coverage invariant (final at SC5):** installed rows = SC1 weather-condition + SC2 six switch-ins + SC3 twenty + SC4 fifteen = 41 pfn-installed rows; SC5 installs the remainder and adds the **all-50** complete-realization gate. Every live slot maps to a declared bit; the 12 weather-coupled abilities realize WEATHER as a condition read (not a pfn); QUICKDRAW/DAUNTINGROAR/BLOODRUSH realize MODIFY_STAT engine-side.
- **Test-coverage gate is a hard merge bar (user mandate):** every new ability gets a behavioral test (positive + paired identical-seed control), not just a mask-realization assertion. SC3 needed its behavioral half authored before it could land; SC4's authoring included it up front -- keep that discipline.
- **BUMP the zm-tests baseline** in the SAME commit whenever ZM_* unit tests change (now **1491**, in `.github/workflows/zm-tests.yml`; it's an EXACT-match gate, so it goes red if `ran != baseline`).
- **DLL-heal gotcha:** Zenithmon's FIRST build in a fresh config leaves middleware DLLs absent -> exe dies at load (exit `0xC0000135`, empty boot log, "no 'Unit tests complete' line"). Fix: `Import-Module Build\zenith_buildsystem.psm1; Repair-ZenithRuntimeDlls -ExeDir <outdir>`. `zenith test` heals automatically; the bare unit gate does NOT. (Config was warm this iteration.)
- **Invalid-volatile-state gotcha:** setting a volatile BIT without its counter trips a production `Zenith_Assert` at end-of-turn that ABORTS the whole unit boot (looks like the DLL crash). Set the bit AND its counter together.
- **Box-2/3 LOCKED contracts (ZM-D-032..036, 039, 040, 041):** `ZM_BattleEvent` POD is append-only (new kinds append before COUNT, new fields default 0); RNG draw order fixed (PHASE G/M/E + the SC4 contact/accuracy draws only fire for live abilities); a NONE-ability/weather-NONE/status-free actor draws ZERO and emits ZERO; EoT order = weather chip FIRST -> weather/screen countdown -> PLAYER-then-ENEMY status ticks -> (SC5) ability TURN_END heals (skip fainted) -> TURN_END. Later SCs APPEND so earlier goldens never shift.
- **Working-dir gotcha:** Bash + PowerShell SHARE a cwd -- run regen/build from `C:\dev\Zenith`. Editing existing files needs NO regen; only NEW files do.
- **Orchestrator invariants (OrchestratorPlaybook.md):** only the orchestrator builds/tests/runs; subagents author on DISJOINT file scopes and never claim "builds clean"; the orchestrator alone writes the living docs and owns the direct-master commit/push; parallel authoring is fine on non-overlapping files, but integrate + build SERIALLY.
- **Hard rules (Scope.md):** `ZM_` prefix; original names / zero Nintendo IP; data = compiled C arrays; no audio/networking/Dynamax; singles only; baked assets git-ignored. Scope changes need a user DecisionLog entry FIRST.
