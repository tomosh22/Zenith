# Zenithmon Status

**Last updated:** 2026-07-16
**Stage:** S4 (Asset generators) -- in progress. **`ZM_BuildingGen`/`ZM_PropGen` STARTED: SC1 of 6 LANDED (ZM-D-079).** Static-model foundation: additive `ZM_GenCommon` static kit (`ZM_StaticMesh::AppendBox` + `ZM_ValidateGenMeshStatic`), the 30-row `ZM_BuildingData` roster, the frozen `ZM_BuildingGen` seam, a minimal static box shell + flat facade, and the `game:Buildings/<Name>/` scheme. **`ZM_HumanGen` COMPLETE (SC1-SC5).** **`ZM_CreatureGen` + `ZM_CreatureAnimGen` COMPLETE.** S1/S2/S3 COMPLETE.
**Build:** GREEN -- regeneration + Vulkan_vs2022_Debug_Win64_True + Vulkan_vs2022_Debug_Win64_False (non-tools link proof). The full five-config matrix re-runs at the SC5 disk-bake commit (which adds the `ZM_GenBakeStaticMesh` bridge + tools-only bake bodies) and the S4 gate.
**Tests:** boot unit gate **1891 ran / 1890 passed / 0 failed / 1 skipped** (was 1886; +5 `ZM_Gen` BuildingGen SC1 units); `zenith test Zenithmon --headless` **7 passed / 0 failed**. The canonical boot-unit helper reached its known 300 s watchdog only after the successful unit tally. `.github/workflows/zm-tests.yml` baseline = **1891**.

## Current task

**`ZM_BuildingGen` SC2: real parametric shell.** Replace SC1's minimal single-box shell with the real parametric building: footprint (width x depth) x storeys with a gable/hip/flat roof per the row's `ZM_ROOF_KIND`, and per-face UV islands separating the facade wall region from the roof region (so SC3's window/door decals land only on the wall island). Add `ZM_StaticMesh` roof/panel emitters as needed (bone-free, outward-wound). Add `BuildingGen_ShellStructural` + `BuildingGen_MeshSensitivity` (MESH domain perturbs geometry; non-MESH domains don't; cross-id distinct) units. Pure/all-config; no disk bake yet. Gate: Debug True build + boot unit gate + headless. `uZM_BUILDINGGEN_VERSION` stays 1 (no baked output yet). **SC4 (PropGen) is parallelizable with SC2/SC3** once needed -- disjoint files (`ZM_PropData`/`ZM_PropGen`/`ZM_Tests_PropGen`).

## Last completed

**`ZM_BuildingGen`/`ZM_PropGen` SC1 -- static-model foundation (ZM-D-079).** Added an ADDITIVE static-mesh foundation to the shared `ZM_GenCommon` (`ZM_StaticMesh::AppendBox` bone-free 24-vert/12-tri box with outward winding matching `Zenith_MeshAsset::GenerateUnitCube`; `ZM_ValidateGenMeshStatic` -- winding/bounds/indices/UVs + the zero-bone/empty-skin static contract), leaving the skinned loft/validator UNTOUCHED so `uZM_GENCOMMON_VERSION` stays 1 and creatures/humans are byte-unaffected. New `ZM_BuildingData` (30-row roster, gyms type-themed to the GDD leaders), frozen `ZM_BuildingGen` seam (`uZM_BUILDINGGEN_VERSION`=1) with pure recipe resolution + minimal box shell + flat per-palette facade + FNV content hash/equality + `game:Buildings/<Name>/` paths + tools bake STUBS. +5 `ZM_Gen` units. Plan -> implement -> Debug True + Debug False gate -> adversarial reviewer (clean). Reviewer NIT (by design): SC1 bundles depend only on (dims, palette), so distinct ids can be byte-identical until SC2/SC3 consume the seed/theme -- a future disk hash-gate must NOT assume all 30 are distinct yet.

## Notes for next agent

- **The active S4 item is `ZM_BuildingGen`/`ZM_PropGen` (SC1 done, SC2-SC6 remain).** `ZM_HumanGen`/`ZM_CreatureGen`/`ZM_CreatureAnimGen` are COMPLETE -- do NOT re-touch. After BuildingGen/PropGen, `ZM_BakeManifest` closes S4, then the S4 gate (4-config matrix + windowed gallery + **visual sign-off** -- the S4 gallery is the next VISUAL GATE, still a few SCs away).
- **SC breakdown (ZM-D-079):** SC1 foundation+seam+roster+box (DONE); SC2 real parametric shell; SC3 facade window/door decals (reuse `ZM_SynthStampRectDecal` -- no new texture synth); SC4 PropGen (parallelizable, disjoint files); SC5 tools disk bake for BOTH families -- adds a NEW additive tools-only `ZM_GenBakeStaticMesh` bridge (skeleton-less `.zmesh`+`.zmtrl`+skeleton-less `.zmodel`; `HasSkeleton()`==false engine static path; `uZM_GENCOMMON_VERSION` stays 1) + a bake smoke asserting the reloaded `.zmodel` has NO skeleton + 0 anims; SC6 coverage/totality + `AssetManifest` s3 + `TestPlan` reconcile.
- **Static-model reuse (SC1):** buildings/props are STATIC -- use `ZM_StaticMesh::AppendBox` (bone-free) + `ZM_ValidateGenMeshStatic`, NEVER the skinned `ZM_MeshLoft`/`ZM_PushLoftVertex` (they bind bone buffers) or `ZM_ValidateGenMesh` (fails a no-weight mesh). Winding = `cross(C-A,B-A) . normal > 0`, per-face hard normals (do NOT re-run `ZM_GenGenerateNormals` -- it welds/smooths box corners). `ZM_GEN_DOMAIN` is PINNED -- reuse MESH+ALBEDO, never append.
- **Doc cadence (matches humans):** CI baseline (`zm-tests.yml`) bumps EVERY SC; `TestPlan`/`AssetManifest` prose reconcile at item completion (SC6). So `TestPlan`'s stated baseline currently lags at 1886 -- reconcile it to the final count at SC6 (add the full BuildingGen/PropGen `ZM_Gen` sections then).
- **Gate ORDER + baseline (unchanged):** `zenith test Zenithmon --headless` FIRST (heals DLLs, else `0xC0000135`); boot unit gate hangs after the units line -> `run_unit_gate.ps1 -Baseline N -TimeoutSec 300` times out+kills but captures `Unit tests complete: N ran`. Non-default configs via msbuild ABSOLUTE sln path (`C:\Program Files\Microsoft Visual Studio\18\Insiders\MSBuild\Current\Bin\MSBuild.exe`), serial only. `git -C C:/dev/Zenith ...`.
- **Working model:** MASTER-ONLY (ZM-D-031); local gate is the authority; `zm-tests` is the post-push backstop, fix forward on red. Sweep stray `zenithmon.exe` (`Get-Process zenithmon`) before ending. Orchestrator+subagents this session (Plan -> Implement -> Reviewer per SC); only the orchestrator builds/tests/commits. A stray harness worktree exists at `.claude/worktrees/` -- NEVER regen/build/commit from it (Invariant 2).
