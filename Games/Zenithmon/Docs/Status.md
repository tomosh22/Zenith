# Zenithmon Status

**Last updated:** 2026-07-16
**Stage:** **S5 (Battle integration slice) IN PROGRESS.** S0-S4 COMPLETE. S5 item 1 (Battle scene / `ZM_BattleArena`) DONE (ZM-D-089). **S5 item 2 is being landed as 4 sub-commits: SC2 (encounter selection logic) DONE (ZM-D-090); SC1 (engine E5 grass reset), SC3 (`ZM_TallGrassSystem`), SC4 (windowed integration) REMAIN.** The Roadmap S5 item-2 box stays UNCHECKED until SC1+SC3+SC4 also land.
**Build:** GREEN -- Vulkan Debug True clean. Clean tree at the commit below.
**Tests:** boot unit gate **1924 ran / 1923 passed / 0 failed / 1 skipped** (SC2 added 11 T0 units: 10 `ZM_Encounter` + 1 `ZM_Data` rate-column, over the 1913 item-1 baseline); `zenith test Zenithmon --headless` **9 passed / 0 failed**. `.github/workflows/zm-tests.yml` baseline bumped **1913 -> 1924** (Zenithmon-only; engine default 1078 in `Tools/run_unit_gate.ps1` UNCHANGED -- SC2 added no engine units). TestPlan 5.5 + AgentBriefing updated.

## Current task

**S5 item 2, next sub-commit -- pick ONE (recommended order SC3 then SC1 then SC4):**
- **SC3 `ZM_TallGrassSystem` (ECS order 109):** own CPU density copy (reuse `Source/World/ZM_GrassDensityMap` -- has `Load`/`SampleWorld`(0.0 unloaded)/`BuildCanonicalPath`), 1 m tile quantization of the active player (`ZM_GameStateManager::TryGetUniqueActiveScenePlayerEntityID` -> `ResolveEntity` -> `Zenith_TransformComponent::GetPosition`), roll `ZM_EncounterZone::RollStepForScene` on each tile transition onto grass (density >= 0.5) and DISPATCH `ZM_OnWildEncounter` (already defined in `Source/World/ZM_EncounterEvents.h`; dispatcher = `Zenith/ZenithECS/Zenith_EventSystem.h`, POD, no macro). Shares the terrain entity (like `ZM_TerrainGrass` order 101). Test/debug rig: `#ifdef ZENITH_INPUT_SIMULATOR` `ForceEncounterOnNextTransitionForTests()`/`SetRngSeedForTests(u64)`; `#ifdef ZENITH_TOOLS` `zm_force_encounter` DebugVariable. T0 units on the pure helpers (QuantizeToTile floorf incl. negatives; IsTileTransition; density gate). Item 2 EMITS the event only -- it loads no battle (that is item 3).
- **SC1 engine E5 (grass-singleton reset hygiene):** `Flux_GrassImpl::Reset()` (`Zenith/Flux/Vegetation/Flux_Grass.cpp:144`) currently UNDER-clears (only chunks+counters) and has NO source callers -> extend it to the full `ClearSceneData()` clear (instances + generated/uploaded flags + density map), then add `g_xEngine.Grass().Reset();` to the `m_pfnResetRenderSystems` lambda in `Zenith/Core/Zenith_Engine.cpp:~617` (next to Terrain/Text/Particles/Skybox/Fog). ENGINE change -> AgentBriefing 6.4: engine units (new `Zenith/Flux/Vegetation/Flux_Grass.Tests.inl`, `#include`d under `#ifdef ZENITH_TESTING` at the bottom of Flux_Grass.cpp) + RenderTest boots green (its idempotent per-frame grass re-apply repopulates after the reset) + DP + CityBuilder suites green + a DecisionLog entry. **Baseline bookkeeping (CRITICAL):** engine units run in EVERY game's boot, so N new engine units bump BOTH the engine default `1078` in `Tools/run_unit_gate.ps1:15` (engine-gate + scaffold-smoke; engine-gate boots Combat) AND zm-tests `1924 -> 1924+N`; verify N is uniform by booting Combat (expect 1078+N) and Zenithmon (expect 1924+N).
- **SC4 windowed integration:** walk-into-grass -> rigged `ZM_OnWildEncounter` fires (subscribe a test callback) + grass cleared on entering an interior (per-scene instance-count assertion, needs SC1's E5). Author `ZM_TallGrassSystem` onto the Dawnmere terrain entity. `m_bRequiresGraphics` + RequestSkip.

Full item-2 plan + frozen API sketches: this session's Plan output (SC1-SC4 decomposition). Grep DecisionLog before re-deciding; the S5 gate (all 5 items) is the next VISUAL hard-stop.

## Last completed

**S5 item 2 SC2 -- `ZM_EncounterZone` + `ZM_OnWildEncounter` + WorldSpec rate (ZM-D-090), committed + pushed (see git log).** `Source/World/ZM_EncounterZone.{h,cpp}` (pure headless: rate gate -> weighted slot -> inclusive level, all via caller-owned `ZM_BattleRNG`; inert/miss steps don't perturb the RNG) + `Source/World/ZM_EncounterEvents.h` (`ZM_OnWildEncounter` POD) + `ZM_WorldSpec.m_uEncounterRatePer256` (Route1 = 40/256, all others 0) + 11 T0 units. Reviewer verdict SHIP (added the RNG-non-perturbation lock test it flagged).

## Notes for next agent

- **Item 1 (`ZM_BattleArena`) + item 2 SC2 (`ZM_EncounterZone`) are DONE.** Next-free ECS order is **109** (SC3's `ZM_TallGrassSystem` claims it).
- **S5 item 3 latent (reviewer-flagged, still open):** `ZM_BattleArena::BuildArena` spawns children into `GetActiveSceneData()`. Fine for item 1, but item 3's additive-battle-over-active-overworld path must retarget to the arena's own scene (the parent entity's `GetSceneData`) or children orphan into the overworld.
- **Gate ORDER + baselines (permanent):** `zenith test Zenithmon --headless` FIRST (heals DLLs). Boot unit gate hangs after the units line -> `pwsh -NoProfile -File Tools\run_unit_gate.ps1 -Exe <exe> -Baseline N -TimeoutSec 300` (now **1924**). Bump N in BOTH `zm-tests.yml` and (for engine-unit changes ONLY) `Tools\run_unit_gate.ps1:15` default (1078) together. New files -> `pwsh -NoProfile -File Build\regen.ps1`. Windowed capture `--filter <Test>`. Run from repo root; `git -C C:/dev/Zenith ...`.
- **Working model:** MASTER-ONLY (ZM-D-031); LOCAL gate is authority; `zm-tests` post-push backstop (fix forward on red). Orchestrated: only the orchestrator builds/tests/commits; subagents author, never build. Sweep stray `zenithmon.exe` before ending. NEVER commit baked assets (`Assets/`) or captures (`Build/artifacts/`).
- **Open Questions (non-blocking):** S2-era `Q-2026-07-12-*` overrides + the S5 bleed-through screenshot `Q-2026-07-09-003` (verified at the S5 gate).
