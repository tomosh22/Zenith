# Zenithmon Status

**Last updated:** 2026-07-16
**Stage:** S4 (Asset generators) -- in progress. **`ZM_BuildingGen`/`ZM_PropGen`: SC1+SC2 of 6 LANDED (ZM-D-079/080).** SC1 static-model foundation (`ZM_StaticMesh::AppendBox` + `ZM_ValidateGenMeshStatic` + 30-row `ZM_BuildingData` roster + frozen `ZM_BuildingGen` seam). SC2 real parametric shell (jittered body box + roof-by-kind GABLE/HIP/FLAT + wall/roof UV islands + MESH-domain jitter). **`ZM_HumanGen`/`ZM_CreatureGen`/`ZM_CreatureAnimGen` COMPLETE.** S1/S2/S3 COMPLETE.
**Build:** GREEN -- Vulkan_vs2022_Debug_Win64_True (SC2 is pure all-config; SC1's Debug_False non-tools link proof still holds -- SC2 added no tools-gated code). The full five-config matrix re-runs at the SC5 disk-bake commit (adds `ZM_GenBakeStaticMesh` + tools bake bodies) and the S4 gate.
**Tests:** boot unit gate **1893 ran / 1892 passed / 0 failed / 1 skipped** (was 1891; +2 `ZM_Gen` BuildingGen SC2 units -- ShellStructural + MeshSensitivity); `zenith test Zenithmon --headless` **7 passed / 0 failed**. The canonical boot-unit helper reached its known 300 s watchdog only after the successful unit tally. `.github/workflows/zm-tests.yml` baseline = **1893**.

## Current task

**`ZM_BuildingGen` SC3: facade window/door decals.** Replace SC2's flat per-palette facade fill with a real facade albedo: base palette + a window GRID + a door decal baked into the WALL UV island (island {0,0,1,0.72}) via the EXISTING `ZM_SynthStampRectDecal` (`ZM_TextureSynth.cpp` -- the header comment already names it "the building-gen window/door reuse contract"; NO new texture synth). Drive window cols/rows from `xR.m_uWindowCols/uWindowRows`; door centred at the base. Apply gym type-theme tint. `uZM_TEXTURESYNTH_VERSION` stays 2 (reuse). Add `BuildingGen_FacadeStructural` (non-empty, >=3 colours, decals inside the wall island) + `BuildingGen_FacadeDomainIsolation` (ALBEDO perturbs facade; MESH doesn't) units. Pure/all-config; no disk bake yet. Gate: Debug True build + boot unit gate + headless. `uZM_BUILDINGGEN_VERSION` stays 1. **SC4 (PropGen) is parallelizable** with SC3 once needed -- disjoint files.

## Last completed

**`ZM_BuildingGen` SC2 -- real parametric shell (ZM-D-080).** Rewrote `ZM_BuildBuildingMesh`: jittered body box (footprint W x D x storeys, grounded y=0, wall UV island {0,0,1,0.72}) + roof-by-`ZM_ROOF_KIND` (roof UV island {0,0.78,1,1.0}). Three new ADDITIVE `ZM_StaticMesh` emitters -- `AppendGableRoof` (2 pitch quads + 2 gable-end tris), `AppendHipRoof` (4-tri pyramid), `AppendFlatRoof` (parapet-cap box) -- plus a file-local auto-orienting `ZM_PushStaticTri` (stores the normal facing away from an interior point + matching winding, so winding-vs-normal AND outward hold by construction). MESH-domain jitter (4 fixed draws, MESH-only). Exact per-kind counts GABLE 38/18, HIP 36/16, FLAT 48/24. Both version constants stay 1. +2 units. Plan -> implement -> Debug True gate -> adversarial reviewer (CLEAN; hand-derived all windings + the 30-row roof-above-walls margin).

## Notes for next agent

- **Active S4 item = `ZM_BuildingGen`/`ZM_PropGen` (SC1+SC2 done; SC3-SC6 remain).** `ZM_HumanGen`/`ZM_CreatureGen`/`ZM_CreatureAnimGen` COMPLETE -- do NOT re-touch. After BuildingGen/PropGen -> `ZM_BakeManifest` closes S4 -> S4 gate (4-config matrix + windowed gallery + **visual sign-off** = the next VISUAL GATE, still a few SCs away).
- **SC breakdown (ZM-D-079/080):** SC1 foundation (DONE); SC2 parametric shell (DONE); SC3 facade window/door decals (reuse `ZM_SynthStampRectDecal` into the wall island); SC4 PropGen seam+roster+prims (parallelizable, disjoint files); SC5 tools disk bake for BOTH families -- adds a NEW additive tools-only `ZM_GenBakeStaticMesh` bridge (skeleton-less `.zmesh`+`.zmtrl`+skeleton-less `.zmodel`; `HasSkeleton()`==false engine static path; `uZM_GENCOMMON_VERSION` stays 1) + a bake smoke asserting the reloaded `.zmodel` has NO skeleton + 0 anims; SC6 coverage/totality + `AssetManifest` s3 + `TestPlan` reconcile.
- **Static-shell kit (SC1/SC2):** buildings/props are STATIC -- use `ZM_StaticMesh::AppendBox`/`AppendGableRoof`/`AppendHipRoof`/`AppendFlatRoof` (bone-free) + `ZM_PushStaticTri` for any triangle (it auto-orients outward via an interior reference point -- pass a point genuinely inside the solid). NEVER the skinned `ZM_MeshLoft`/`ZM_PushLoftVertex` or `ZM_ValidateGenMesh`. Winding = `cross(C-A,B-A).normal>0`, hard per-face normals (do NOT re-run `ZM_GenGenerateNormals`; only `ZM_GenGenerateTangents`). UV islands MUST be sub-rects of [0,1] (validator rejects out-of-range UVs). `ZM_GEN_DOMAIN` is PINNED -- reuse MESH+ALBEDO.
- **Reviewer NIT still open (by design, SC1):** SC1 bundles depend only on (dims, palette); distinct ids can be byte-identical until SC3 consumes the theme/seed into the facade. SC3 (facade tint by gym type + per-palette) should DIVERGE them. A future disk hash-gate (SC5/SC6) must NOT assume all 30 are distinct until then.
- **Doc cadence (matches humans):** CI baseline (`zm-tests.yml`) bumps EVERY SC; `TestPlan`/`AssetManifest` prose reconcile at item completion (SC6). `TestPlan`'s stated baseline lags at 1886 -- reconcile to the final count + add the full BuildingGen/PropGen `ZM_Gen` sections at SC6.
- **Gate ORDER + baseline (unchanged):** `zenith test Zenithmon --headless` FIRST (heals DLLs, else `0xC0000135`); boot unit gate hangs after the units line -> `run_unit_gate.ps1 -Baseline N -TimeoutSec 300` times out+kills but captures `Unit tests complete: N ran`. Non-default configs via msbuild ABSOLUTE sln path (`C:\Program Files\Microsoft Visual Studio\18\Insiders\MSBuild\Current\Bin\MSBuild.exe`), serial only. `git -C C:/dev/Zenith ...`.
- **Working model:** MASTER-ONLY (ZM-D-031); local gate is the authority; `zm-tests` is the post-push backstop, fix forward on red. Sweep stray `zenithmon.exe` before ending. Orchestrator+subagents this session (Plan -> Implement -> Reviewer per SC); only the orchestrator builds/tests/commits. A stray harness worktree exists at `.claude/worktrees/` -- NEVER regen/build/commit from it (Invariant 2).
