# Zenithmon Status

**Last updated:** 2026-07-18
**Stage:** **S5 (Battle integration slice) COMPLETE + gate-signed (ZM-D-112).** S0-S5 COMPLETE. The user reviewed the rendered before->arena->after bleed-through evidence and **APPROVED** the S5 visual gate (Q-2026-07-09-003 CLOSED -- no overworld bleed-through at the (0,-2000,0) offset). **S6 (Dialogue, menus, NPCs, shops) STARTING** -- first task: engine **E4 `Zenith_UIGridLayoutGroup`**.
**Build:** GREEN -- Zenithmon Vulkan Debug True clean. Master clean + pushed at the S5 sign-off docs commit (see git log).
**Tests:** boot unit gate **2025 ran / 2024 passed / 0 failed / 1 skipped** (re-confirmed first-party this session). `zenith test Zenithmon --headless` **22/0**; full windowed suite **22/0** incl. the 10-test battle suite. Engine default **1088** UNCHANGED. `zm-tests.yml` baseline **2025**.

## Current task

**S6 item 1 -- engine E4 `Zenith_UIGridLayoutGroup` (fixed columns / cell size).** This is the last of the pre-approved engine changes (E1-E5; E4 is the only one unshipped). It is an ENGINE change, so it needs: (a) engine unit tests for the new widget surface, (b) a RenderTest boot regression check (RenderTest is pre-existingly red on missing terrain -- Q-2026-07-16-001 -- so verify it is NO WORSE, not green), (c) Combat + DP + CityBuilder suites stay green, (d) a DecisionLog entry, and (e) bump BOTH the engine default baseline (1088) in `Tools/run_unit_gate.ps1` AND the affected CI workflow baselines in the SAME commit. After E4 lands, S6 continues with the game-side UI surfaces (`ZM_UI_DialogueBox`/`ZM_UI_MenuStack`/`ZM_UI_Party`/`ZM_UI_Bag`/`ZM_UI_Dex`/`ZM_UI_Shop`), `ZM_Interactable` + NPC graphs (`ZM_GraphAuthoring`) + `ZM_NpcWalker`, and the Care Center heal + mart buy/sell. S6 gate: UI-state unit tests + automated talk/buy/heal/open-every-menu via focus navigation (no visual hard-stop is listed for S6 -- the next visual gate is S8's vertical-slice go/no-go).

## Last completed

**S5 STAGE GATE SIGNED OFF (ZM-D-112) -- user APPROVED the overworld bleed-through visual check.** The rendered before->in-arena->after round trip (deterministic `Flux_Screenshot` dumps, presented as an Artifact) showed the additive Battle scene at Y=-2000 clean -- overworld grass cleared to 0, no Dawnmere terrain/grass bleeding through, only the shared global skybox (accepted). S5 (items 1-5, ZM-D-089..111) is COMPLETE. Prior: SC6 closer (ZM-D-111) set the GATE-WAIT after the automated gate went all-green; SC1-SC5 (ZM-D-106..110) shipped the item-5 GameState wiring (party model, GameStateManager, exp write-back, catch, loss->whiteout).

## Notes for next agent (S6)

- **S6 forward-notes carried from the S5 item-5 reviewers (act on these as the relevant surface lands):**
  1. **The Catch battle-menu item is shown/selectable UNCONDITIONALLY, ignoring `m_bCanCatch`** (`ZM_UI_BattleHUD` menu). Inert at S5 (all wild battles), but **S6/S7 TRAINER battles (`m_bCanCatch=false`) MUST gate the Catch item on a core-surfaced `m_bCanCatch`** or selecting it trips the engine `SubmitAction` assert (debug) / rolls an illegal trainer-catch (no-assert build). Recorded in Shortfalls.md 1.5.
  2. **The battle menu couples cursor-index to raw move-SLOT-index** -- fine for contiguous placeholder movesets, but real gapped movesets need a FILLED-SLOT index table (already compacted in the shipped menu per Shortfalls 1.6 -- verify before multi-member parties land).
  3. **All-0-PP active -> Fight submenu is a silent dead-end** (no Struggle; Q-2026-07-12-003 engine gap). Add Struggle if S6+ needs it.
- **Next-free ECS component order is 112** (100-111 used; item 5 added no component). New game components claim 112+.
- **HUD/menu seam:** `ZM_UI_BattleHUD` is owned BY VALUE in `ZM_BattleDirector` (`m_xHud`), not an ECS component. Re-resolve UI elements each frame (never cache). E4's grid widget is the engine primitive S6's menus compose on.
- **Gate ORDER + baselines (permanent):** `zenith test Zenithmon --headless` FIRST (heals DLLs). Boot unit gate hangs after the units line -> `pwsh -NoProfile -File Tools\run_unit_gate.ps1 -Exe <exe> -Baseline 2025 -TimeoutSec 300` (the 300s timeout-kill is EXPECTED). Windowed: `--filter ZM_Battle` runs all battle windowed tests in one boot. New files -> `Build\regen.ps1`. **E4 is an ENGINE change** -> bump the engine default (1088) in `run_unit_gate.ps1` AND the engine CI baseline + need Combat/RenderTest/DP/CityBuilder regression (RenderTest pre-existingly red -- Q-2026-07-16-001, verify no-worse). Diagnosing a windowed test: game `Zenith_Log(LOG_CATEGORY_UNITTEST)` diagnostics are NOT in harness stdout -- run the exe directly `<exe> --automated-test <Name> --exit-after-frames N` to see them (pair the flag with `--automated-test`; bare idles forever). Capture windowed evidence via `Flux_Screenshot::RequestDump` in a test Step (deterministic, phase-based) -> TGA -> convert; CLI `--screenshot-frame N` is fragile (global frame stalls during terrain streaming).
- **Working model:** MASTER-ONLY (ZM-D-031); LOCAL gate is authority; `zm-tests` post-push backstop. Orchestrated: only the orchestrator builds/tests/commits; subagents author, never build. Sweep stray `zenithmon.exe` before ending. NEVER commit baked assets or captures (Build/artifacts is git-ignored).
- **Open Questions:** Q-2026-07-17-001 (`BiomeForScene` hard-coded table -- WorldSpec column at S9/S10); Q-2026-07-16-001 (RenderTest terrain pre-existingly red); Q-2026-07-12-003 (Struggle engine gap). Q-2026-07-09-003 (S5 bleed-through) is CLOSED (ZM-D-112, APPROVED).
- **The next VISUAL hard-stop is the S8 vertical-slice go/no-go** (manual playthrough sign-off). S6/S7 have no visual gate -- the loop runs through them automatically.
