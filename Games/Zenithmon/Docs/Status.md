# Zenithmon Status

**Last updated:** 2026-07-19
**Stage:** **S6 (Dialogue, menus, NPCs, shops) IN PROGRESS.** S0-S5 COMPLETE + gate-signed. **S6 item 1 DONE (E4, ZM-D-113); item 2 SC1 DONE (`ZM_UI_MenuStack`, ZM-D-114); SC2 DONE (`ZM_UI_DialogueBox`, ZM-D-115); SC3 DONE (`ZM_Bag` + money, ZM-D-116).** Next: item 2 **SC4** (`ZM_UI_Party` screen -- a fixed <=6-slot pool populated from the real party, focus-navigable, confirm opens a per-member summary). Item 2 is the game-side UI screens (dialogue/menu-stack/party/bag/dex/shop) composed on E4 + the existing Zenith UI, then `ZM_Interactable` + NPC graphs + `ZM_NpcWalker`, then Care Center heal + mart. S6 has NO visual hard-stop (the next visual gate is S8's vertical-slice go/no-go).
**Build:** GREEN -- Zenithmon Vulkan Debug True clean (0 errors, 0 warnings). Master clean + pushed at the SC2 commit (see git log).
**Tests:** boot unit gate **2101 ran / 2100 passed / 0 failed / 1 skipped** (SC3 added +23 game `ZM_Bag` units; SC2 added +22 `ZM_DialogueBox`; engine default **1097** UNCHANGED -- neither SC touches engine code, so no cross-game regression was required; zm boot **2056 -> 2078 -> 2101**). `zenith test Zenithmon --headless` **24/0** and full WINDOWED suite **24/0** (SC2's windowed `ZM_DialogueTalk_Test` genuinely RUNS -- result json `skipped: false`, 50 frames). `zm-tests.yml` baseline **2101**; `run_unit_gate.ps1` default **1097**.

## Current task

**S6 item 2 -- the game-side UI screens. DECOMPOSED (2026-07-19):** durable plan on disk at `Build/artifacts/zm_s6_ui_plan/plan.md` (git-ignored). **9 sub-commits, test-first, each shippable:** **SC1** = `ZM_UI_MenuStack` ECS component (order **112**) on a NEW persistent `ZM_MenuRoot` entity (author in FrontEnd cloning `ZM_BattleTransitionRoot`/`ZM_ConfigureBattleHUD`) + a focus-navigable root pause menu (open on M/Tab in overworld, freeze player, confirm/cancel/close); **SC2** `ZM_UI_DialogueBox` (queue-of-strings modal, E3 typewriter, advance-on-confirm); **SC3** NEW pure `ZM_Bag`+`ZM_ItemStack` + `m_uMoney` on `ZM_GameState` (matches the pre-locked SaveFormat modules 6/7 so S7 serializes without reshaping; seed in `ZM_MakeStarterGameState`) -- unblocks Bag/Shop; **SC4** `ZM_UI_Party`; **SC5** `ZM_UI_Dex` (uses the new `Zenith_UIGridLayoutGroup`); **SC6** `ZM_UI_Bag`; **SC7** `ZM_UI_Shop` + `ZM_ShopLogic` buy/sell; **SC8** Care Center heal (reuse `ZM_Party::HealAllFull` + SC2 dialogue); **SC9** consolidated windowed gate (talk/buy/heal/open-every-menu via focus nav) + Roadmap tick. **SC1 DONE (ZM-D-114):** `ZM_UI_MenuStack` (order 112) + persistent `ZM_MenuRoot`: M/Tab opens a focus-navigable ROOT menu over the overworld, player frozen, arrow-nav + name-dispatch confirm + Escape pop; +22 units + windowed `ZM_MenuOpenClose_Test`. **SC1 also required an ENGINE FIX** (`Zenith_SceneData::CancelPendingStart` underflow tolerance -- a second persistent-singleton entity exposed a latent between-tests-cycling crash; fix mirrors the sibling `ProcessSinglePendingStart` tolerance; full cross-game regression green). **SC2 DONE (ZM-D-115):** `ZM_UI_DialogueBox` -- a NON-ECS presenter owned BY VALUE by `ZM_UI_MenuStack` (NO new ECS order; next-free stays 113): an 8-slot all-or-nothing line queue + the E3 typewriter (delegating to `ZM_UI_BattleHUD::ComputeVisibleGlyphCount` -- the 45 glyphs/sec rate stays single-sourced), the four-outcome `Confirm()` contract (complete-reveal -> next-line -> close), MODAL (Escape deliberately ignored), raised via `PushDialogueLines` / `TryPushDialogue` (push on an EMPTY stack freezes the player, so an NPC talks without the pause menu; over an open screen it stacks and pops back). `ZM_MENU_SCREEN_DIALOGUE` APPENDED (value 5, never reachable from a ROOT entry); `Menu_DialoguePanel`/`Menu_DialogueText` authored hidden on `ZM_MenuRoot` in the 9000/9001 band. +22 units + windowed `ZM_DialogueTalk_Test`. **SC3 DONE (ZM-D-116):** the pure economy model -- `ZM_Bag` (9 pockets x 64 `ZM_ItemStack` slots, each pocket kept sorted ASCENDING by `ZM_ITEM_ID` via a shifting insert/erase) + `m_uMoney` / `uZM_MONEY_CAP` / `AddMoney` (saturating, headroom-FIRST so it never wraps -- S7's loader assigns `m_uMoney` straight from the save uint32, so an over-cap balance must not corrupt) / `SpendMoney` on `ZM_GameState`; starter seeds 3000 money + 5 CatchOrb + 3 Salve through `AddMoney`. **Every mutator is ALL-OR-NOTHING (never clamp-and-succeed)** because SC7's shop does `SpendMoney(price)` THEN `bag.Add(...)` -- a partial add would silently eat the player's money. `Remove` ERASES a stack at zero, so a count-0 entry (which SaveFormat module 6 forbids writing) is structurally unstorable. Measured: `ZM_ITEM_COUNT`=90, largest pocket 25 (TM), so 64 slots make "pocket full" unreachable -- a unit walks the table so S9/S10 content growth past 64 fails loudly. +23 units. **NEXT SC = SC4 (`ZM_UI_Party` screen).** GOTCHA for SC4+: a new persistent-singleton is fine, but adding a game component/entity that opens over the overworld -- WIRE `ResetRuntimeStateForTests` into the between-tests hook (Zenithmon.cpp) so batched tests don't inherit its state. All screen presenters are NON-ECS `Source/UI/` classes owned BY VALUE by the MenuStack (re-resolve elements by name each frame -- the `ZM_UI_BattleHUD` idiom); dispatch menu actions by the FOCUSED ELEMENT'S NAME (never `SetOnClick(this)` -- a `this` userdata dangles on ECS pool relocation). **Scope rulings (best-guesses, loop PROCEEDS):** engine focus-nav for traversal + game confirm/cancel; new `ZM_MenuRoot` entity; starter 3000 money + 5 CatchOrb + 3 Salve (real tuning = S11). NB: the battle Catch item must be gated on `m_bCanCatch` once TRAINER battles arrive (S7) -- track then, not now.

## Last completed

**S6 item 2 SC3 -- the bag + money data model (ZM-D-116).** Pure, headless, no ECS/UI/IO and no new component order: `ZM_Bag` (one pocket per `ZM_ITEM_CATEGORY` x 64 `ZM_ItemStack` slots, sorted ascending by item id) plus `m_xBag` / `m_uMoney` / `AddMoney` / `SpendMoney` on `ZM_GameState`, shaped to the pre-locked SaveFormat modules 6 and 7 so S7 serializes without a reshape. All-or-nothing mutators (the SC7 shop deducts money *before* adding), erase-at-zero so a count-0 stack can never be stored, headroom-first money math that cannot wrap even on an over-cap loaded balance. Starter seeds 3000 money + 5 CatchOrb + 3 Salve. Pre-build review caught a genuinely vacuous unit (a pocket walk bounded by a count just asserted to be 0, so it ran zero iterations), the money headroom underflow, and an uncovered cap boundary; it also caught a DecisionLog id collision (SC3 is ZM-D-116, not 115). +23 units; zm baseline 2078 -> 2101; engine 1097 untouched. Prior: **S6 item 2 SC2 -- `ZM_UI_DialogueBox`, the modal dialogue screen (ZM-D-115).** A NON-ECS presenter owned BY VALUE by `ZM_UI_MenuStack` (the `ZM_BattleDirector` -> `ZM_UI_BattleHUD` seam), so it consumes NO new ECS order. Fixed 8-slot `std::string` queue (all-or-nothing accept), E3 typewriter reveal delegated to the existing `ZM_UI_BattleHUD::ComputeVisibleGlyphCount`, four-outcome `Confirm()` (complete-reveal / next-line / close / ignored), MODAL (cancel never dismisses it), raised via `PushDialogueLines`/`TryPushDialogue` and freezing the player when raised on an empty stack. Elements re-resolve by NAME every frame. Review: 4 pre-build lenses fixed 8 real defects (incl. a missing text `SetSize`/`SetAlignment` that would have run the line off the right of the screen, and two vacuous unit fixtures); 3 post-gate lenses raised 20 findings and **all 20 were refuted with file:line evidence** -- the recurring cluster is unreachable until S6 item 3's `ZM_Interactable` exists, and is carried as forward-notes in Shortfalls 1.6. Orchestrator then applied + re-gated two strictly-better fixes (a windowed assert that could have passed vacuously; two inaccurate "trivially movable" header comments). +22 units + windowed `ZM_DialogueTalk_Test`; zm baseline 2056 -> 2078; engine 1097 untouched. Prior: **S6 item 2 SC1 -- `ZM_UI_MenuStack` overworld pause menu + engine ECS fix (ZM-D-114).** New ECS component (order 112) on a persistent `ZM_MenuRoot`: M/Tab opens a focus-navigable ROOT menu over the overworld, freezes the player, arrow-nav + name-dispatch confirm + Escape pop. Discovered + fixed a latent ENGINE bug: a second persistent-singleton entity trips `CancelPendingStart`'s hard underflow assert during the harness's between-tests scene cycling (crashed all automated tests 22/0->0/24); fixed by adding the same underflow tolerance its sibling `ProcessSinglePendingStart` already has. +22 game units + windowed `ZM_MenuOpenClose_Test`; reviewer NO BLOCKERS (one SHOULD-FIX applied: between-tests reset); cross-game regression green (Combat 1097/0+14/0, DP 158/0, CB 45/0, RenderTest no-worse). Prior: S6 item 1 E4 `Zenith_UIGridLayoutGroup` (ZM-D-113); S5 STAGE GATE SIGNED OFF (ZM-D-112).

## Notes for next agent (S6)

- **NEW gotcha (paid for at E4): `zenith test <Game>` runs the EXISTING exe -- it does NOT relink a game after an ENGINE-lib change.** For cross-game engine regression you MUST `zenith build <Game>` FIRST (force the relink against the new `build/output/.../zenith.lib`), THEN `zenith test <Game>` / the unit gate -- otherwise you validate a STALE exe (a stale combat.exe showed the OLD 1088 unit count until force-rebuilt to 1097). The shared engine lib is at `build/output/win64/vulkan_vs2022_debug_win64_true/zenith.lib`; games relink against it.
- **E4 usage:** `Zenith_UIGridLayoutGroup` (`Zenith/UI/`, `UIElementType::GridLayoutGroup`). `SetColumns/SetCellSize/SetSpacing/SetPadding/SetFitToContent`; children placed row-major top-left, +Y down, LOCAL offsets; auto-sizes its own rect when fit-to-content. Canvas owns elements (allocate with `new`, add via `AddElement`/`AddChild`). Engine UI has focus navigation (`Zenith_UICanvas::SetFocusedElement`/`NavigateUp/Down/Left/Right`/`ActivateFocused`) -- the S6 menu gate wants "open-every-menu via focus navigation".
- **S6 forward-notes carried from the S5 item-5 reviewers (act as the relevant surface lands):** (1) the battle **Catch menu item ignores `m_bCanCatch`** -- S7 TRAINER battles MUST gate it on a core-surfaced flag or trip the engine `SubmitAction` assert / roll an illegal trainer-catch (Shortfalls 1.5). (2) the battle menu couples cursor to move-slot -- filled-slot compaction already shipped (Shortfalls 1.6); verify before multi-member parties. (3) all-0-PP Fight is a silent dead-end (no Struggle; Q-2026-07-12-003) -- add if needed.
- **Next-free ECS component order is 113** (100-112 used; SC1's `ZM_UI_MenuStack` took 112). New game components claim 113+. NOTE the SC2 pattern: a screen PRESENTER does NOT need an order at all -- `ZM_UI_DialogueBox` is a non-ECS class owned BY VALUE by `ZM_UI_MenuStack`, which is how SC4-SC7's Party/Dex/Bag/Shop screens should ship too (keep them trivially/noexcept movable so the host's defaulted move survives ECS pool relocation).
- **Gate ORDER + baselines (permanent):** `zenith test Zenithmon --headless` FIRST (heals DLLs). Boot unit gate hangs after the units line -> `pwsh -NoProfile -File Tools\run_unit_gate.ps1 -Exe <exe> -Baseline 2101 -TimeoutSec 300` (the 300s timeout-kill is EXPECTED). Windowed: `--filter ZM_Battle` runs all battle windowed tests in one boot; `--filter ZM_Dialogue` / `ZM_Menu` for the S6 UI ones. New files -> `Build\regen.ps1` (Sharpmake globs `/Zenith` + each game's tree; the generated `.vcxproj`/`.filters` are gitignored -- never hand-edit them). **GAME-only unit adds bump `zm-tests.yml` ONLY (now 2101) and need NO cross-game regression. Engine-unit changes bump `run_unit_gate.ps1` default (now 1097) + `zm-tests.yml` (now 2101) + need Combat/RenderTest/DP/CityBuilder regression with a FORCED per-game rebuild first** (RenderTest pre-existingly red on terrain -- Q-2026-07-16-001, verify no-worse). Diagnosing a windowed test: game `Zenith_Log(LOG_CATEGORY_UNITTEST)` diagnostics are NOT in harness stdout -- run the exe directly `<exe> --automated-test <Name> --exit-after-frames N` (pair the flag with `--automated-test`; bare idles forever). Capture windowed visual evidence via `Flux_Screenshot::RequestDump` in a test Step (deterministic, phase-based) -> TGA -> convert to PNG (System.Drawing, uncompressed 32-bit BGRA top-left); CLI `--screenshot-frame N` is fragile (global frame stalls during terrain streaming).
- **Working model:** MASTER-ONLY (ZM-D-031); LOCAL gate is authority; `zm-tests` post-push backstop. Orchestrated: only the orchestrator builds/tests/commits; subagents author, never build. Sweep stray game exes before ending. NEVER commit baked assets or captures (Build/artifacts git-ignored). A Combat test writes a stray `EnemyBase.zpfb` to the repo root during `zenith test Combat` (pre-existing Combat hygiene bug; flagged as a background task) -- delete it if it appears, never commit it.
- **Open Questions:** Q-2026-07-17-001 (`BiomeForScene` hard-coded -- WorldSpec column at S9/S10); Q-2026-07-16-001 (RenderTest terrain pre-existingly red); Q-2026-07-12-003 (Struggle engine gap). Q-2026-07-09-003 (S5 bleed-through) CLOSED (ZM-D-112).
- **The next VISUAL hard-stop is the S8 vertical-slice go/no-go** (manual playthrough sign-off). S6/S7 have no visual gate -- the loop runs through them automatically.
