# Zenithmon Status

**Last updated:** 2026-07-18
**Stage:** **S5 (Battle integration slice) IN PROGRESS.** S0-S4 COMPLETE. **S5 items 1+2+3 DONE; item 4 IN PROGRESS -- SC1 (E3 typewriter, ZM-D-100) + SC2 (`ZM_BattleDirectorCore`, ZM-D-101) + SC3 (`ZM_BattleDirector` order 111, ZM-D-102) + SC4 (`ZM_UI_BattleHUD`, ZM-D-103) DONE.** **NEXT is item 4 SC5** (interactive Fight/Run + move menu -> `SubmitPlayerAction`; the FIRST player-driven battle input). Then SC6 (windowed win gate ticks the item-4 box), then item 5, then the **S5 GATE = the next VISUAL hard-stop** (needs the user's sign-off).
**Build:** GREEN -- Zenithmon Vulkan Debug True clean. Master clean + pushed at the SC4 commit (see git log).
**Tests:** boot unit gate **1988 ran / 1987 passed / 0 failed / 1 skipped** (SC4 = **+21 pure game units** over the SC3 baseline of 1967; game-only, so `zm-tests.yml` bumped **1967 -> 1988** and the engine default `run_unit_gate.ps1` **1088** is UNCHANGED). `zenith test Zenithmon --headless` **17/0**. Windowed **6/0, zero skips**: the new `ZM_BattleHUD_Test` + the 5 coexistence tests (`ZM_BattleRoundTrip_Test`/`ZM_BattleEncounterLatch_Test`/`ZM_BattleArena_Test`/`ZM_BattleArenaOwnScene_Test`/`ZM_BattleDirectorRoundTrip_Test`) all genuinely ran against real baked assets.

## Current task

**S5 item 4 SC5 -- interactive Fight/Run (player battle input).** The first player-driven battle: a Fight/Run action menu + a move menu that feed `ZM_BattleDirectorCore::SubmitPlayerAction(...)` instead of the current hard-coded move-slot-0. Windowed input via `Zenith_InputSimulator` state-setters only (no reentrant loop). The catch/exp gate (item 5) and the S5 visual sign-off follow.

**PLANNED:** the durable item-4 plan is on disk at `Build/artifacts/zm_s5_item4_plan/plan.md` (git-ignored; regenerate if absent) -- see its SC5 section. Remaining item-4 SCs: **SC5** interactive Fight/Run + move menu -> `SubmitPlayerAction`; **SC6** windowed win gate ticks the item-4 box.

## Last completed

**S5 item 4 SC4 (ZM-D-103) -- `ZM_UI_BattleHUD`, the first visible battle UI.** `Source/UI/ZM_UI_BattleHUD.{h,cpp}`: a non-ECS presentation class owned BY VALUE by `ZM_BattleDirector` (seam Option B). It renders a `Zenith_UIComponent` (log `UIText` + 2 HP-panel `UIText` + 2 HP-bar `UIRect`) authored onto the `BattleDirector` entity by `ZM_ConfigureBattleHUD` (sort order 10002 > BattleFade 10001). The director drives it Setup/Update/Hide (hide before the end-fade). Pure static formatters (log line / HP panel / HP fraction / typewriter reveal). **Instant-mode fix** shipped in the same commit: added `ZM_BattleDirectorCore::PresentedEventCount()` + a presented-range backward scan so the log surfaces the final line under `zm_instant_battles` (where a single `Tick` drains all ops and `CurrentEvent()` is already null). +21 T0 units + windowed `ZM_BattleHUD_Test`; reviewer 3-lens pass (no blockers; 2 should-fixes + 1 invariant-nit closed this SC). zm-tests 1967->1988, engine default 1088 unchanged. Build + full gate green. Prior: SC3 (ZM-D-102) `ZM_BattleDirector`; SC2 (ZM-D-101) `ZM_BattleDirectorCore`; SC1 (ZM-D-100) engine E3 typewriter.

## Notes for next agent (S5 item 4 SC5)

- **SC5 = the FIRST interactive battle input.** Replace the director's hard-coded `SubmitPlayerAction({MOVE, slot 0})` (`ZM_BattleDirector.cpp` OnUpdate RUNNING block) with a Fight/Run menu that submits the player's chosen action. Reuse the HUD's UI authoring pattern (`ZM_ConfigureBattleHUD`) + sort order > 10001. The core already accepts `SubmitPlayerAction` only in `AWAIT_INPUT` (asserts otherwise) -- gate the menu on `m_xCore.IsAwaitingInput()`.
- **NOW FIX the SC3 reviewer nit (it becomes live in SC5):** `ZM_BattleDirector::OnUpdate` ignores `RequestBattleEnd()`'s bool return. SC5 adds a Run/flee path that can ALSO end the battle by a second route, so branch on the return (and decide the flee -> `RequestBattleEnd` seam). See ZM-D-102.
- **HUD seam is Option B:** `ZM_UI_BattleHUD` is owned by value in the director (`m_xHud`), NOT an ECS component. Re-resolve UI elements each frame (never cache). The HUD reads `ZM_BattleDirectorCore` read-through accessors; keep the director the single core owner.
- **Deferred HUD nit (revisit when it can bite):** the HUD's `strLine != m_strShownLine` latch will not re-type two ADJACENT identical log lines -- unreachable in the current 1v1 wild stream, but revisit when multi-hit/stat-change spam or multi-mon turns can emit adjacent identical constant lines.
- **Next-free ECS order is 112** (111 = `ZM_BattleDirector`; SC4's HUD added NO component). 
- **Gate ORDER + baselines (permanent):** `zenith test Zenithmon --headless` FIRST (heals DLLs). Boot unit gate hangs after the units line -> `pwsh -NoProfile -File Tools\run_unit_gate.ps1 -Exe <exe> -Baseline 1988 -TimeoutSec 300` (the 300s timeout-kill is EXPECTED; the units line is already logged). Windowed: `--filter ZM_Battle` runs all 6 battle windowed tests in one boot. New files -> `Build\regen.ps1`. ENGINE-unit changes bump BOTH `zm-tests.yml` AND `Tools\run_unit_gate.ps1`'s default (1088) + need Combat/RenderTest/DP/CityBuilder regression (RenderTest pre-existingly red -- Q-2026-07-16-001). Diagnosing a windowed test: the game's `Zenith_Log(LOG_CATEGORY_UNITTEST)` diagnostics are NOT in the harness stdout -- run the exe directly `<exe> --automated-test <Name> --exit-after-frames N` to see them.
- **Working model:** MASTER-ONLY (ZM-D-031); LOCAL gate is authority; `zm-tests` post-push backstop. Orchestrated: only the orchestrator builds/tests/commits; subagents author, never build. Sweep stray `zenithmon.exe` before ending. NEVER commit baked assets or captures.
- **Open Questions:** Q-2026-07-17-002 (S5 item-4 scoping -- SC3/SC4 acted on best-guesses (a) placeholder player team, (b) exp OFF, (d) GREEDY wild AI; SC5 introduces (c) the action-menu subset = Fight/Run); Q-2026-07-17-001 (`BiomeForScene` hard-coded table -- WorldSpec column at S9/S10); Q-2026-07-16-001 (RenderTest terrain); Q-2026-07-09-003 (S5 bleed-through -- verified/falsified only at the S5 SCREENSHOT gate).
- **The S5 gate is the next VISUAL hard-stop** (all 5 items: windowed encounter round-trip + catch + exp + exact resume + the bleed-through screenshot). Do not tick it or start S6 without the user's sign-off.
