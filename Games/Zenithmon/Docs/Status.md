# Zenithmon Status

**Last updated:** 2026-07-18
**Stage:** **S5 (Battle integration slice) IN PROGRESS.** S0-S4 COMPLETE. **S5 items 1+2+3+4 DONE; item 5 IN PROGRESS.** Item 4 closed via SC1-SC6 (ZM-D-100..105). **Item 5 SC1 (ZM-D-106) pure Party model; SC2 (ZM-D-107) `ZM_GameStateManager` owns the in-memory `ZM_GameState`; SC3 (ZM-D-108) the battle EXP WRITE-BACK -- ALL DONE.** SC3: a WON wild battle awards exp/levels to the real party lead + carries damaged HP (append-only `m_uCurHP` on `ZM_BattleMonsterSpec`, zero-perturbation vs the ~380 goldens) + the gapped-moveset menu fix. **NEXT is item 5 SC4** -- flip `m_bCanCatch`, a minimal Catch root-menu item -> `{ITEM, ball}`, catch write-back adds the caught monster to the party + marks the dex (gate: catch). Then SC5 (whiteout), SC6 (windowed gate), then the **S5 GATE = the next VISUAL hard-stop** (user sign-off; AFTER item 5).
**Build:** GREEN -- Zenithmon Vulkan Debug True clean. Master clean + pushed at the item-5 SC3 commit (see git log).
**Tests:** boot unit gate **2014 ran / 2013 passed / 0 failed / 1 skipped** (item-5 SC1 +14, SC3 +5 pure units; SC2 windowed-only; `zm-tests.yml` **2009 -> 2014**; engine default **1088** UNCHANGED). The ~380 `ZM_Battle` goldens are BYTE-IDENTICAL (SC3's `m_uCurHP` append is zero-perturbation). `zenith test Zenithmon --headless` **20/0**. Windowed: the item-4/SC3 battle suite **8/0** (`ZM_BattleMenuWin_Test` now asserts the persistent lead's EXP ROSE after a win) + `ZM_GameStatePersistence_Test` **1/0**.

## Current task

**S5 item 5 SC4 -- catch (add a caught wild monster to the party + dex).** Flip `m_bCanCatch=true` on the SC3 real-lead config; add a **Catch** item to the battle action root menu (alongside Fight/Run) that submits `{ZM_ACTION_ITEM, ZM_ITEM_CATCHORB}` (the engine's `DoItemAction` already rolls the catch + emits `CATCH_RESULT`); on a SUCCESSFUL catch, EXTEND the SC3 write-back adapter (`ZM_ApplyBattleResultToParty`) to add the caught wild monster to the player party (via `ZM_MonsterFromBattleMonster` on the enemy's final `ZM_BattleMonster`, if the party has room) and `MarkCaught` its species in the dex. Direct Catch button, fixed default ball, no Bag UI (Q-2026-07-18-001; S6 owns Bag). GATE PROOF: a windowed test throws a ball -> catches -> asserts the party grew + the species is caught + exact resume. Consult the durable plan's SC4 section. Confirm the catch action shape (`ZM_ACTION_ITEM`/`m_eItem`), how the core surfaces the CATCH_RESULT outcome (caught vs broke free), and the ball item id against the code before authoring.

**DECOMPOSED (2026-07-18):** durable plan on disk at `Build/artifacts/zm_s5_item5_plan/plan.md`. Item 5 is almost pure WIRING -- ZERO engine changes. **6 sub-commits:** **SC1 DONE (ZM-D-106)** pure `Source/Party/` model (+14 units); **SC2 DONE (ZM-D-107)** `ZM_GameStateManager` owns the in-memory `ZM_GameState` + static `TryGetGameState` + between-tests reseed; **SC3 DONE (ZM-D-108)** exp write-back: `RunSetup` reads the real lead, win-only `ZM_ApplyBattleResultToParty` (pure `Source/Party/ZM_BattleWriteBack`) writes exp/level/moves/PP/HP back, `m_uCurHP` appended to `ZM_BattleMonsterSpec` (zero-perturbation), gapped-moveset menu fix; **SC4** flip `m_bCanCatch`, a Catch root-menu item -> `{ITEM, ball}`, catch write-back adds to party + dex (gate: catch); **SC5** loss->whiteout (heal + warp to Dawnmere "TownCenter") (gate: faint->whiteout); **SC6** consolidated windowed `--filter ZM_Battle` gate + bleed-through screenshot (Q-2026-07-09-003) + docs -> sets the S5 VISUAL GATE-WAIT.

**Scope rulings logged as Q-2026-07-18-001** (best-guesses, loop PROCEEDS on them): single-LEAD battle (defer switch to S6+), fixed starter (Fernfawn L5), GameState IN-MEMORY only (no save until S7), direct Catch button (no Bag UI until S6), whiteout->Dawnmere. **NEXT SC = SC4**. Surface for SC4-SC6: `ZM_GameStateManager::TryGetGameState(ZM_GameState*&)`; `ZM_GameState{ m_xParty, m_xCaught(Mark/IsSet/Count), m_bPendingWhiteout }`; `ZM_Party::Lead()/Add()/Count()/IsFull()/AllFainted()/HealAllFull()`; `ZM_MonsterFromBattleMonster(const ZM_BattleMonster&)` (the caught-monster factory SC4 uses); `Source/Party/ZM_BattleWriteBack.h::ZM_ApplyBattleResultToParty(ZM_GameState&, const ZM_BattleDirectorCore&)` (SC4 EXTENDS this adapter with the catch-add); the SC5 menu (`ZM_UI_BattleHUD` `MenuConfirm`/root items) SC4 adds a Catch item to.

**Item-5 SC5 FORWARD NOTES (from the SC3 reviewer -- act on these in SC5, they are NOT bugs today, they are within SC3's win-only scope):** (1) **"flee = free heal"** -- SC3 carries the lead's damaged HP INTO battle but writes it back OUT only on a WIN, so a successful flee discards the in-battle HP damage; SC5 (loss/flee + HP persistence) should make HP persist through a flee even though exp does not. (2) A **fainted lead (record HP 0) would battle at 1 HP** (the `[1,maxHP]` clamp) with no guard in `RunSetup`; SC5's whiteout path should add a fainted-lead guard. Also carried: the SC2 nit (`ResetGameStateForTests` could resolve via `s_xSingletonEntityID` for symmetry).

## Last completed

**S5 item 5 SC3 (ZM-D-108) -- battle exp write-back + damaged-HP carry + gapped-moveset fix.** A WON wild battle awards exp/levels to the player's persistent party lead. `RunSetup` reads the real lead via `TryGetGameState` (placeholder fallback keeps headless director-core tests exp-OFF; exp is flipped ON via a LOCAL config copy only for a real lead -- `BuildBattleConfig()` stays exp-OFF so `Director_BattleConfigExpOff` is unchanged). New pure `Source/Party/ZM_BattleWriteBack` (`ZM_ApplyBattleResultToParty`, win-only, guarded) writes the post-battle lead back via SC1's `ZM_ApplyBattleMonsterToRecord`, called once on the RESOLVED edge. Appended `m_uCurHP` sentinel to `ZM_BattleMonsterSpec` (carry damaged HP in, clamp [1,maxHP]) -- **byte-identical to the ~380 `ZM_Battle` goldens**. Gapped-moveset menu compaction (`BuildFilledMoveMenu` + `MenuConfirm` raw-slot param). +5 T0 units + the windowed win test now asserts the persistent lead's exp rose. 3-lens reviewer NO BLOCKERS (test-coverage finding fixed; 2 scope-boundary findings are SC5 forward notes above). zm-tests 2009->2014. Prior: SC2 (ZM-D-107) GameState manager; SC1 (ZM-D-106) Party model; item 4 COMPLETE (ZM-D-100..105).

## Notes for next agent (S5 item 5)

- **Item 4 is CLOSED (all 6 SCs, ZM-D-100..105).** The windowed suite proves player-driven win (`ZM_BattleMenuWin_Test`) + flee (`ZM_BattleMenuRun_Test`) + presentation/resume (`ZM_BattleHUD_Test`, `ZM_BattleDirectorRoundTrip_Test`). Item 5 ("Catch / exp / faint / whiteout applied to GameState") is the LAST S5 item; the S5 STAGE gate (visual hard-stop, user sign-off) is AFTER it.
- **Item 5 is where PERSISTENT GameState first appears.** Item 4 deliberately used a deterministic placeholder player (`ZM_BattleDirector::BuildPlaceholderPlayerSpec`, discarded on unload) with exp OFF + no catch. Item 5 wires a real party/GameState, flips `m_bAwardExp`/`m_bCanCatch` (the EXP/LEVEL/CATCH events are already mapped -> minimal `ZM_BattleDirectorCore` change), and handles faint->whiteout. Plan it first (it touches SaveData/party persistence). No `ZM_GameState`/`ZM_Party` exists yet (verified at SC3).
- **Item-5 FORWARD NOTES from the SC5 reviewer (act on these WHEN item 5 lands real party/moves -- they are NOT bugs today):**
  1. **The battle menu couples cursor-index to raw move-SLOT-index** (`ZM_UI_BattleHUD.cpp` menu refresh + `MenuConfirm` submits `m_uMoveSlot = cursor`). Correct only while movesets pack contiguously from slot 0 (the placeholder player always does). Item 5's real party members can have GAPPED movesets -> switch the menu to a FILLED-SLOT index table (map cursor -> the Nth non-empty slot) before that lands, or a gapped active shows a blank unselectable button and the real move is unreachable.
  2. **All-0-PP active -> Fight submenu is a silent dead-end** (no Struggle, no feedback). Not a softlock (Run + 30 s deadline). Struggle is the pre-existing engine gap Q-2026-07-12-003; add it (engine) if item 5+ needs it.
  3. **The two H0-edited windowed tests prove resolution INDIRECTLY** (resume deadline < softlock). Consider adding a direct `GetCore().IsOver()` latch+assert to `ZM_BattleDirectorRoundTrip_Test`/`ZM_BattleHUD_Test` for robustness (the new menu tests already assert `GetWinner()`).
- **The SC3 `RequestBattleEnd`-return nit is DONE** (both sites branch on the bool now).
- **HUD/menu seam is Option B:** `ZM_UI_BattleHUD` owned by value in the director (`m_xHud`), NOT an ECS component. Re-resolve UI elements each frame (never cache). Keep the director the single `ZM_BattleDirectorCore` owner. Next-free ECS order is **112** (SC4/SC5 added NO component).
- **Gate ORDER + baselines (permanent):** `zenith test Zenithmon --headless` FIRST (heals DLLs). Boot unit gate hangs after the units line -> `pwsh -NoProfile -File Tools\run_unit_gate.ps1 -Exe <exe> -Baseline 1995 -TimeoutSec 300` (the 300s timeout-kill is EXPECTED). Windowed: `--filter ZM_Battle` runs all 8 battle windowed tests in one boot. New files -> `Build\regen.ps1`. ENGINE-unit changes bump BOTH `zm-tests.yml` AND `run_unit_gate.ps1`'s default (1088) + need Combat/RenderTest/DP/CityBuilder regression (RenderTest pre-existingly red -- Q-2026-07-16-001). Diagnosing a windowed test: the game's `Zenith_Log(LOG_CATEGORY_UNITTEST)` diagnostics are NOT in the harness stdout -- run the exe directly `<exe> --automated-test <Name> --exit-after-frames N` to see them.
- **Working model:** MASTER-ONLY (ZM-D-031); LOCAL gate is authority; `zm-tests` post-push backstop. Orchestrated: only the orchestrator builds/tests/commits; subagents author, never build. Sweep stray `zenithmon.exe` before ending. NEVER commit baked assets or captures.
- **Open Questions:** Q-2026-07-17-002 (S5 item-4 scoping -- SC3/SC4/SC5 acted on best-guesses (a) placeholder player team, (b) exp OFF, (c) Fight+Run only, (d) GREEDY wild AI); Q-2026-07-17-001 (`BiomeForScene` hard-coded table -- WorldSpec column at S9/S10); Q-2026-07-16-001 (RenderTest terrain); Q-2026-07-09-003 (S5 bleed-through -- verified/falsified only at the S5 SCREENSHOT gate).
- **The S5 gate is the next VISUAL hard-stop** (all 5 items: windowed encounter round-trip + catch + exp + exact resume + the bleed-through screenshot). Do not tick it or start S6 without the user's sign-off.
