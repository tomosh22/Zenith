# Zenithmon Status

**Last updated:** 2026-07-16
**Stage:** **S5 (Battle integration slice) IN PROGRESS -- item 1 (Battle scene) DONE (ZM-D-089).** S0/S1/S2/S3/S4 COMPLETE + gate-signed. The Battle scene (build index 1, world Y = -2000) + `ZM_BattleArena` component (order 108) shipped: an always-visible dome + 2 platforms + 6 per-biome dressing sets (`ZM_PROP_DRESSING_*`), exactly one shown via `SetBiome`. **Next: S5 item 2 -- `ZM_EncounterZone` + `ZM_TallGrassSystem` + engine E5 (grass-reset hygiene).**
**Build:** GREEN -- Vulkan Debug True built clean at tip after the review fixes. Clean tree at the commit below. (Full 5-config matrix NOT re-run this task -- item 1 is not a stage gate; the S5 gate runs the matrix.)
**Tests:** boot unit gate **1913 ran / 1912 passed / 0 failed / 1 skipped** (+5 T0 `ZM_BattleArena` units over the 1908 S4 baseline); `zenith test Zenithmon --headless` **9 passed / 0 failed** (graphics tests incl. `ZM_BattleArena_Test` skip headless); windowed `ZM_BattleArena_Test` **1 passed / 0 failed** (31 frames, real warm assets, end-to-end). `.github/workflows/zm-tests.yml` baseline bumped **1908 -> 1913** in the same commit. TestPlan section 5.5 updated.

## Current task

**S5 item 2: `ZM_EncounterZone` + `ZM_TallGrassSystem` + engine E5 grass-reset hygiene.** Per Roadmap.md S5 + MasterPlan.md S5: `ZM_TallGrassSystem` keeps its OWN CPU density copy, quantizes to 1 m tiles, and rolls a per-route encounter on tile transitions (`ZM_EncounterZone` selects the encounter table + rate with rigged-RNG-testable rolls). Engine **E5** = grass-singleton reset hygiene on scene load (additive/interior transitions must clear grass) -- an engine change: needs engine unit tests + a RenderTest boot regression + DP/CityBuilder suites green (AgentBriefing 6.4), plus a DecisionLog entry. Data tables are compiled C arrays; grass logic is testable headless where possible. Grep DecisionLog before re-deciding; the S5 gate (all 5 items) is the next VISUAL hard-stop.

## Last completed

**S5 item 1 -- Battle scene + `ZM_BattleArena` (ZM-D-089), committed + pushed (see git log).** New component `Components/ZM_BattleArena.{h,cpp}` (order 108) + Battle scene authoring in `Zenithmon.cpp` (`AddStep_CreateScene("Battle")`, arena root at Y=-2000, build index 1 registered non-tools-gated) + 5 T0 units + windowed `ZM_BattleArena_Test`. Reviewer SHIP-WITH-FIXES applied: camera Z-sign (was looking away from the arena), `IsFullyBuilt()` coverage accessor + test assertion, `ReadFromDataStream` reset-first nit.

## Notes for next agent (S5 item 2+)

- **Do NOT re-touch item 1.** `ZM_BattleArena` + the Battle scene are done and tested. Next-free ECS order is **109**.
- **KNOWN latent item for S5 item 3 (additive load), flagged by the reviewer:** `ZM_BattleArena::BuildArena` spawns its children into `g_xEngine.Scenes().GetActiveSceneData()`. Correct for item 1 (Battle is the active scene in both the game flow and the windowed test). But when item 3 loads the battle **additively over a still-active overworld**, `GetActiveSceneData()` is the overworld and the arena children would spawn there (orphaned, not unloaded with Battle). **Item 3 must switch `BuildArena` to the arena's own scene (the parent entity's `GetSceneData`) rather than the active scene.**
- **Asset/scene surface for the arena:** the 6 dressing props are `ZM_PROP_DRESSING_{MEADOW,VOLCANIC,COAST,WETLAND,SNOW,CANYON}` loaded via `ZM_PropAssetPath(id, ZM_PROP_ASSET_MODEL, ...)`. `Battle.zscen` is git-ignored, authored on each `*_True` boot (camera regenerates with the fix). Baked `Assets/` are warm in this checkout (props/creatures/humans/buildings + the 4 scenes present) -- windowed tests bake via `ZM_BakeAllAssets()` (fast when warm).
- **Gate ORDER + baseline (permanent):** `zenith test Zenithmon --headless` FIRST (heals DLLs, else `0xC0000135`); the boot unit gate hangs after the units line -> `pwsh -NoProfile -File Tools\run_unit_gate.ps1 -Exe <exe> -Baseline N -TimeoutSec 300` times out+kills but captures `Unit tests complete: N ran` (bump N here + in `.github/workflows/zm-tests.yml` together when ZM_* units change; now **1913**). New files -> `pwsh -NoProfile -File Build\regen.ps1`. Windowed capture: `--filter <Test>` (not `--headless`). Run from repo root; `git -C C:/dev/Zenith ...`.
- **Working model:** MASTER-ONLY (ZM-D-031); the LOCAL gate is the authority; `zm-tests` runs post-push as a backstop (fix forward on red). ORCHESTRATED sessions: only the orchestrator builds/tests/commits (OrchestratorPlaybook Invariant 1); subagents author, never build. Sweep stray `zenithmon.exe` (`Get-Process zenithmon`) before ending. NEVER commit baked assets (`Assets/`) or captures (`Build/artifacts/`).
- **Open Questions (non-blocking):** several S2-era best-guess `Q-2026-07-12-*` battle/breeding/AI overrides remain OPEN + the S5 bleed-through screenshot check `Q-2026-07-09-003` (verified at the S5 gate). None block S5 item 2.
