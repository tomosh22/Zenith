# Zenithmon Status

**Last updated:** 2026-07-16
**Stage:** S4 (Asset generators) -- in progress. **`ZM_HumanGen` COMPLETE (SC1-SC5, ZM-D-074..078).** The 34-row roster, shared fixed 16-bone skeleton, frozen public seam, six-part per-model mesh loft, deterministic 256x256 skin/hair/outfit albedo + six hair / five attachment silhouettes, shared nine-clip rotation-only set, AND the tools disk bake (SC5) are all done. **`ZM_CreatureGen` + `ZM_CreatureAnimGen` COMPLETE.** S1/S2/S3 COMPLETE.
**Build:** GREEN -- full five-config matrix all built for SC5: `Vulkan_vs2022_Debug/Release_Win64_{True,False}` + `D3D12_vs2022_Debug_Win64_False` null-backend link proof.
**Tests:** boot unit gate **1886 ran / 1885 passed / 0 failed / 1 skipped** (was 1884; +2 tools-only `ZM_Gen` HumanBake units, which RAN and PASSED -> end-to-end bake confirmed); `zenith test Zenithmon --headless` **7 passed / 0 failed**. The canonical boot-unit helper reached its known 300 s watchdog only after the successful unit tally. `.github/workflows/zm-tests.yml` baseline = **1886**.

## Current task

**`ZM_BuildingGen` / `ZM_PropGen`** -- the next unchecked S4 Roadmap item: ~30 building models + ~25 props (incl. ~6 battle-dome biome dressing sets). Parametric building shells (footprint / roof / facade texture with baked window + door decals; box colliders authored at SCENE time, not baked into the model). Props: fences, signs, lamps, bridges, ledge lips, cave rocks, interior furniture. Reuse the shipped `ZM_GenCommon` loft + `ZM_TextureSynth` + the disk-bake bridges; author pure all-config generation with `#ifdef ZENITH_TOOLS` disk bake, `ZM_Gen` determinism/structural units, then wire into the tools bake path. After that, `ZM_BakeManifest` (generator-version stamp + file-existence + byte-identical re-bake invariant) closes S4, then the S4 gate (4-config matrix + windowed gallery + visual sign-off on a sampled dozen -- the S4 gallery is the next VISUAL GATE; the creature gallery was already signed off ZM-D-067).

## Last completed

**`ZM_HumanGen` SC5 -- tools disk bake (ZM-D-078):** two additive tools-only `ZM_GenCommon` bridges (`ZM_GenBakeSkeleton` skeleton-only + `ZM_GenBakeMeshWithSharedSkeleton` mesh binding a SHARED skel ref, NO per-model `.zskel`) + filled `ZM_BakeHumanShared`/`ZM_BakeHuman`/`ZM_BakeAllHumans` bodies. Shared: 1 `.zskel` + 9 `.zanim` baked ONCE under `game:Humans/Shared/`. Per model: `.zmesh`/`_albedo.ztxtr`/`.zmtrl`/`.zmodel` under `game:Humans/<Name>/`, the `.zmodel` binding the shared skel + 9 shared clips (IDLE..FAINT). `ZM_GenBakeMesh` left byte-for-byte unchanged (creatures unaffected, no re-bake); `uZM_GENCOMMON_VERSION`/`uZM_HUMANGEN_VERSION` both stay 1. +2 tools-only bake units (new `Tests/ZM_Tests_HumanBake.cpp`). Plan -> implement -> 5-config gate -> adversarial reviewer (clean) -> docs. Evidence: the gate output above.

## Notes for next agent

- **`ZM_HumanGen`, `ZM_CreatureGen`, `ZM_CreatureAnimGen` are all COMPLETE.** Do NOT re-touch them. The remaining S4 items are `ZM_BuildingGen`/`ZM_PropGen` (this iteration's target) then `ZM_BakeManifest`, then the S4 gate.
- **Disk-bake bridges available (SC5):** `ZM_GenBakeMesh` (mesh + own `.zskel`), `ZM_GenBakeSkeleton` (shared `.zskel` only), `ZM_GenBakeMeshWithSharedSkeleton` (mesh binding a shared skel ref). Buildings/props are STATIC (no skeleton/anim) -> most likely bake via a mesh-only path (either `ZM_GenBakeMesh` with a trivial/empty skeleton, or a new skeleton-free static-mesh bridge if the asset pipeline needs one -- check whether `Zenith_ModelAsset` requires a skeleton ref for a static prop; creatures/humans are skinned, props are not). Decide + log if a new static-mesh bridge is warranted (additive, tools-only, keeps `uZM_GENCOMMON_VERSION` at 1 if no generated bytes move).
- **Bake conventions (unchanged):** owning-handle `Create<T>()`+`GetDirect()` for materials/models across Export; `create_directories` FIRST then `exists()` as the ONLY IO signal; `Flux_*::Export` + `SaveToFile` create NO dirs so ordering is load-bearing; per-family folder `game:<Family>/<Name>/`; never commit baked assets (git-ignored). `ZM_GenMesh` SoA stays a byte-mirror of `Zenith_MeshAsset`. `ZM_GetX Name` accessors return `const char*`.
- **Gate ORDER + baseline (unchanged):** `zenith test Zenithmon --headless` FIRST (heals DLLs, else `0xC0000135`); boot unit gate hangs after the units line -> `run_unit_gate.ps1 -Baseline N -TimeoutSec 300` times out+kills but captures `Unit tests complete: N ran`. Bump the count in `.github/workflows/zm-tests.yml` + Status.md + TestPlan.md together. Non-default configs via msbuild ABSOLUTE sln path (`MSBuild` = `C:\Program Files\Microsoft Visual Studio\18\Insiders\MSBuild\Current\Bin\MSBuild.exe`); serial only. `git -C C:/dev/Zenith ...`.
- **Working model:** MASTER-ONLY (ZM-D-031); local gate (build + boot unit gate + headless + the stage matrix) is the authority; `zm-tests` is the post-push backstop, fix forward on red. Sweep stray `zenithmon.exe` (`Get-Process zenithmon`) before ending each iteration. **NO visual gate until the S4 gallery** (after BuildingGen/PropGen/BakeManifest) -- then S8.
- **Orchestration (this session):** SC5 was run as orchestrator + subagents (Plan -> Implement -> Reviewer -> Doc Maintainer) per OrchestratorPlaybook; only the orchestrator built/tested/committed. A stray harness worktree exists at `.claude/worktrees/` -- NEVER regen/build/commit from it (Invariant 2); all work is on the main checkout `C:\dev\Zenith`.
