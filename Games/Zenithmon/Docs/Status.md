# Zenithmon Status

**Last updated:** 2026-07-16
**Stage:** S4 (Asset generators) -- in progress. **`ZM_BuildingGen`/`ZM_PropGen`: SC1-SC5 of 6 LANDED (ZM-D-079..083).** SC1 static foundation, SC2 building shell, SC3 building facade, SC4 `ZM_PropGen`, SC5 tools disk bake for BOTH families (skeleton-less static bundles). Both families now bake complete scene-loadable STATIC models to disk. Only SC6 (coverage/doc reconcile -> ticks the Roadmap box) remains. **`ZM_HumanGen`/`ZM_CreatureGen`/`ZM_CreatureAnimGen` COMPLETE.** S1/S2/S3 COMPLETE.
**Build:** GREEN -- **full 5-config matrix** re-run at SC5 (Vulkan Debug/Release x True/False + `D3D12_vs2022_Debug_Win64_False` null-backend link proof), all build.
**Tests:** boot unit gate **1905 ran / 1904 passed / 0 failed / 1 skipped** (was 1903; +2 tools-only `ZM_Gen` bake smokes -- BuildingBake + PropBake, which RAN + PASSED -> end-to-end static bake confirmed); `zenith test Zenithmon --headless` **7 passed / 0 failed**. The canonical boot-unit helper reached its known 300 s watchdog only after the successful unit tally. `.github/workflows/zm-tests.yml` baseline = **1905**.

## Current task

**`ZM_BuildingGen`/`ZM_PropGen` SC6: coverage/totality + doc reconcile (TICKS the Roadmap box).** The final sub-commit. (1) **AssetManifest.md section 3** -- replace the high-level buildings/props spec with the RESOLVED per-model file breakdown (mirror the human section 2 / creature section 1.2 tables): each STATIC building/prop bakes 4 files under `game:Buildings/<Name>/` / `game:Props/<Name>/` -- `<Name>.zmesh` (skeleton-less static mesh), `<Name>_facade.ztxtr` / `<Name>_albedo.ztxtr` (BC1), `<Name>.zmtrl`, `<Name>.zmodel` (single-submesh, NO skeleton/anim); family totals 30 buildings x 4 + 25 props x 4; note colliders are SCENE-authored (not baked); `uZM_BUILDINGGEN_VERSION`/`uZM_PROPGEN_VERSION`=1 stamps in section 6.1. (2) **TestPlan.md section 5.4** -- add the full BuildingGen (5+2+2 = 9 units) + PropGen (6+2 = 8 units) + the 2 bake smokes `ZM_Gen` subsections (mirroring the creature/human ones), and RECONCILE the stated boot baseline 1866 -> **1905** (it has lagged since the human SCs; SC6 is the reconcile point). (3) Optionally a byte-identical re-bake invariant test if cheap (the full manifest re-bake determinism check is the separate `ZM_BakeManifest` box). (4) Refresh Shortfalls.md if warranted. (5) TICK the `ZM_BuildingGen` / `ZM_PropGen` Roadmap box (`[ ]` -> `[x]`). Doc-heavy SC -> delegate the TestPlan/AssetManifest bodies to a Doc Maintainer; the orchestrator writes the living docs + ticks the box. Then `ZM_BakeManifest` (generator-version stamp + file-existence + byte-identical re-bake invariant) is the LAST S4 code box, then the S4 gate (4-config matrix + windowed gallery + **visual sign-off** = the next VISUAL GATE).

## Last completed

**`ZM_BuildingGen`/`ZM_PropGen` SC5 -- tools disk bake (ZM-D-083).** New additive tools-only `ZM_GenCommon` bridge `ZM_GenBakeStaticMesh` (`ZM_GenBakeMesh` minus the skin/skeleton -> skeleton-less `.zmesh`, `HasSkinning()`==false); filled `ZM_BakeBuilding`/`ZM_BakeAllBuildings` + `ZM_BakeProp`/`ZM_BakeAllProps` writing skeleton-less `.zmesh`/`.zmtrl`/`.zmodel` bundles (owning-handle `Create<T>()`+`GetDirect()`; NO SetSkeletonPath/AddAnimationPath; create_dirs-first + exists()-only). `ZM_GenBakeMesh` byte-untouched; all 4 version constants unchanged (creatures/humans don't re-bake). +2 tools bake smokes (assert the reloaded `.zmodel` is STATIC). Plan -> implement -> FULL 5-config matrix + 1905 unit gate (smokes RAN + PASSED) -> reviewer (clean; 1 stale-comment NIT fixed in-commit).

## Notes for next agent

- **Active S4 item = `ZM_BuildingGen`/`ZM_PropGen` (SC1-SC5 done; ONLY SC6 doc-reconcile remains, then the box ticks).** After it -> `ZM_BakeManifest` (last S4 code box) -> S4 gate (windowed gallery + **visual sign-off** = the next VISUAL GATE; the creature gallery was already signed off ZM-D-067, but buildings/props/humans are NEW asset families in the gallery). `ZM_HumanGen`/`ZM_CreatureGen`/`ZM_CreatureAnimGen` COMPLETE.
- **Baked static bundle (SC5, git-ignored):** per building = `game:Buildings/<Name>/` {`.zmesh` skeleton-less, `_facade.ztxtr` BC1, `.zmtrl`, `.zmodel` no-rig}; per prop = `game:Props/<Name>/` {`.zmesh`, `_albedo.ztxtr`, `.zmtrl`, `.zmodel`}. Baked via `ZM_GenBakeStaticMesh` + `ZM_SynthBakeAlbedoBC1` + owning-handle material/model. NEVER commit these (bake code ships, output does not).
- **Static-mesh kit reference (all shipped):** `ZM_StaticMesh::AppendBox`/`AppendGableRoof`/`AppendHipRoof`/`AppendFlatRoof` + `ZM_PushStaticTri` (bone-free); `ZM_ValidateGenMeshStatic`; the FOUR bake bridges `ZM_GenBakeMesh` (own skel) / `ZM_GenBakeSkeleton` (shared skel) / `ZM_GenBakeMeshWithSharedSkeleton` (bind shared skel) / `ZM_GenBakeStaticMesh` (NO skel). `ZM_GEN_DOMAIN` PINNED (MESH mesh / ALBEDO texture).
- **Reviewer NIT still open (SC4, harmless):** LAMP props' roster `m_fWidth`/`m_fDepth` are dead data. No action.
- **Doc cadence:** CI baseline (`zm-tests.yml`) bumped EVERY SC (now 1905). `TestPlan`/`AssetManifest` prose reconcile at SC6 (this task) -- TestPlan baseline lags at 1866, reconcile to 1905 + add the BuildingGen/PropGen sections; AssetManifest section 3 gets the resolved file breakdown.
- **Gate ORDER + baseline (unchanged):** `zenith test Zenithmon --headless` FIRST (heals DLLs, else `0xC0000135`); boot unit gate hangs after the units line -> `run_unit_gate.ps1 -Baseline N -TimeoutSec 300` times out+kills but captures `Unit tests complete: N ran`. New files need `pwsh -NoProfile -File Build\regen.ps1`. Non-default configs via msbuild ABSOLUTE sln path (`C:\Program Files\Microsoft Visual Studio\18\Insiders\MSBuild\Current\Bin\MSBuild.exe`), serial only. `git -C C:/dev/Zenith ...`.
- **Working model:** MASTER-ONLY (ZM-D-031); local gate is the authority; `zm-tests` is the post-push backstop, fix forward on red. Sweep stray `zenithmon.exe` before ending. Orchestrator+subagents this session (Plan -> Implement -> Reviewer per SC; Doc Maintainer for reference docs); only the orchestrator builds/tests/commits. A stray harness worktree exists at `.claude/worktrees/` -- NEVER regen/build/commit from it (Invariant 2).
