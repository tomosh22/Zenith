# Zenithmon Status

**Last updated:** 2026-07-16
**Stage:** **S4 (Asset generators) COMPLETE + visually gate-signed (ZM-D-088).** All five generator families (creatures v3, creature-anims, humans v1, buildings v1, props v1) + `ZM_BakeManifest` are shipped and bake deterministic disk bundles under a per-family manifest guard; the full-family `ZM_AssetGallery_Test` was approved by the user (2026-07-16). S0/S1/S2/S3 COMPLETE. **Next stage: S5 (Battle integration slice) -- critical path.**
**Build:** GREEN -- last full 5-config matrix at `ZM_BakeManifest`; the gallery test builds Vulkan Debug True + Debug False. Clean tree, pushed to master (tip `f23e3264` + the S4-signoff doc commit).
**Tests:** boot unit gate **1908 ran / 1907 passed / 0 failed / 1 skipped**; `zenith test Zenithmon --headless` **7 passed / 0 failed** (`ZM_AssetGallery_Test` + other graphics tests skip headless). `.github/workflows/zm-tests.yml` baseline = **1908**. TestPlan/AssetManifest reconciled at 1908.

## Current task

**S5 (Battle integration slice) -- first item: the Battle scene.** Author the battle scene (build index 1; world offset **(0,-2000,0)**; an enclosing dome + platforms; **~6 baked biome prop dressing sets swapped at runtime** -- these are the `ZM_PROP_DRESSING_{MEADOW,VOLCANIC,COAST,WETLAND,SNOW,CANYON}` props S4 just shipped, baked via `ZM_BakeAllProps`/`ZM_BakeAllAssets`). Then the rest of S5 in Roadmap order: `ZM_EncounterZone` + `ZM_TallGrassSystem` (own CPU density copy, 1 m tile quantization, per-route encounter rolls) + engine **E5** grass-reset hygiene; additive load / `SetScenePaused` / camera+HUD switch round trip (grass cleared entering interiors/battle); `ZM_BattleDirector` (event-stream interpreter over the shipped headless `ZM_BattleEngine`, `zm_instant_battles` DebugVariable) + `ZM_UI_BattleHUD` + engine **E3** typewriter; catch/exp/faint/whiteout applied to GameState. **S5 gate is a VISUAL check** (walk grass -> rigged encounter -> additive battle at the -2000 offset -> scripted win -> assert exp + exact overworld resume; catch test; bleed-through screenshot at the offset -- Questions.md Q-2026-07-09-003). Read Roadmap.md S5 + MasterPlan.md S5 for the full detail; grep DecisionLog.md before re-deciding.

## Last completed

**S4 gate SIGNED OFF -> S4 COMPLETE (ZM-D-088).** The user APPROVED the full-family `ZM_AssetGallery_Test` visual gate (front/left/right captures under `Build/artifacts/zenithmon/s4/gallery/`, git-ignored) after the ZM-D-087 building-overlap fix (height-only scale -> width-budget `AGFitScale`). S4 shipped the entire procedural asset pipeline; assets are baked to git-ignored `Assets/` under the `ZM_BakeManifest` per-family guard.

## Notes for next agent (S5)

- **S4 is DONE -- do NOT re-touch the generators.** All four families + `ZM_BakeManifest` are gate-signed. If S5 scenes reference baked models, call the tools-only `ZM_BakeAllAssets()` (from `ZM_BakeManifest.h`) in the scene/test Setup to warm-bake (the manifest makes re-runs fast); it is NOT wired into the unconditional boot by design (would slow every unit-gate/CI boot).
- **S4 asset surface for S5:** per-family model refs via `ZM_CreatureAssetPath`/`ZM_HumanAssetPath`/`ZM_BuildingAssetPath`/`ZM_PropAssetPath` (`*_ASSET_MODEL` kind) -> `Zenith_ModelComponent::LoadModel`. Creatures/humans are skinned (add a `Zenith_AnimatorComponent` for animation; without one they render bind-pose). Buildings/props are static. The 6 battle-dome biome dressing props are `ZM_PROP_DRESSING_*`. Component orders 100-107 used; **next free ECS order is 108** (see AgentBriefing 3.1).
- **Engine changes queued for S5:** E3 (`Zenith_UIText` typewriter reveal) + E5 (grass-reset hygiene) land in S5; E4 (`Zenith_UIGridLayoutGroup`) in S6. Each engine change needs unit tests + a RenderTest boot regression + DP/CityBuilder suites green (AgentBriefing 6.4).
- **Gate ORDER + baseline (permanent):** `zenith test Zenithmon --headless` FIRST (heals DLLs, else `0xC0000135`); boot unit gate hangs after the units line -> `pwsh -NoProfile -File Tools\run_unit_gate.ps1 -Exe <exe> -Baseline N -TimeoutSec 300` times out+kills but captures `Unit tests complete: N ran` (bump N here + in `.github/workflows/zm-tests.yml` together when ZM_* units change). New files need `pwsh -NoProfile -File Build\regen.ps1`. Non-default configs via msbuild ABSOLUTE sln path (`C:\Program Files\Microsoft Visual Studio\18\Insiders\MSBuild\Current\Bin\MSBuild.exe`), serial only. `git -C C:/dev/Zenith ...`. Run WINDOWED tests from the repo root. Windowed capture: `--filter <Test>` (not `--headless`) -> in-test `Flux_Screenshot` TGA dump under `Build/artifacts/`; TGA->PNG via PIL.
- **Working model:** MASTER-ONLY (ZM-D-031); the LOCAL gate is the authority; `zm-tests` runs post-push as a backstop (fix forward on red). Sweep stray `zenithmon.exe` (`Get-Process zenithmon`) before ending. NEVER commit baked assets (`Assets/`) or captures (`Build/artifacts/`). A stray harness worktree may exist at `.claude/worktrees/` -- NEVER regen/build/commit from it. Orchestrated sessions: only the orchestrator builds/tests/commits (OrchestratorPlaybook Invariant 1); subagents author, never build; the orchestrator does interactive visual/camera tuning directly.
- **Open Questions (non-blocking, user-override-optional):** several S2-era best-guess rulings remain OPEN in Questions.md (various `Q-2026-07-12-*` battle/breeding/AI overrides, all additive) + the S5 bleed-through screenshot check `Q-2026-07-09-003` (verified at the S5 gate). None block S5.
