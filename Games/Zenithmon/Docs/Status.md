# Zenithmon Status

**Last updated:** 2026-07-14
**Stage:** S4 (Asset generators) -- in progress. **`ZM_CreatureAnimGen` SC3 of 6 LANDED (ZM-D-070):** SERPENT + AQUATIC clip builders added (both LIMBLESS -- per-vertebra phase-offset travelling spine undulation + fin sweeps) + wired. **5 of 8 archetypes now animate** (Quadruped/Biped/Avian/Serpent/Aquatic). Clips are 6 per archetype (Idle/Walk/Attack/Special/Hit/Faint), pure `f(archetype,clip-id)`, rotation-only, byte-identical across every species of an archetype; reuses the engine headerless `Flux_AnimationClip::Export` (no engine change). `ZM_CreatureGen` COMPLETE + gate-signed (ZM-D-067). S1/S2/S3 COMPLETE.
**Build:** GREEN -- Vulkan_vs2022_Debug_Win64_True. (Cross-config purity of the anim library proven at SC1 across Debug False + the D3D12 Debug False null-backend link proof; sub-commits SC2-SC5 add only more pure builders + tests, so the full matrix re-runs at the SC6 gate that edits `ZM_CreatureGen.*`.)
**Tests:** boot unit gate **1861 ran / 1860 passed / 0 failed / 1 skipped** (was 1859; +2 new `ZM_Gen` per-archetype units); `zenith test Zenithmon --headless` **7 passed / 0 failed**. `.github/workflows/zm-tests.yml` baseline bumped 1859 -> **1861** (same commit).

## Current task

**`ZM_CreatureAnimGen` SC4: Insectoid + Blob clip builders (the extremes).** Add `Source/Gen/ZM_CreatureAnimArchetype_Insectoid.cpp` (~19 bones, nearest the bone cap -- author a metachronal TRIPOD gait: alternate the two leg tripods, plus antennae/head/spine motion) and `_Blob.cpp` (~4 bones: Spine00-02 + Nub -- the SPARSEST; rotation-only spine-flexion sway/pulse + Nub bob, NO head/legs). Wire both into `ZM_GetArchetypeAnimBuilder`, add per-archetype structural tests. FIRST read `Source/Gen/ZM_CreatureArchetype_Insectoid.cpp` / `_Blob.cpp` for the EXACT frozen bone names (confirm the leg-row naming for the tripod + that Blob is exactly Spine00-02+Nub). The generic harness auto-exercises them once wired (its FaintSettlesAndClamps is bone-name-agnostic, so spineless/4-bone Blob is fine). Mirror the SC1-SC3 pattern; reuse the `ZM_CreatureAnimCommon` kit. Still NO disk bake / NO `ZM_CreatureGen.*` edit (SC6). Rebuild, boot unit gate at the new measured count, headless, commit + push. Remaining after SC4: SC5 FloaterPlantoid + all-8 totality gate, SC6 bundle-bake wiring (ticks the Roadmap box), SC7 optional windowed playability gate.

## Last completed

**`ZM_CreatureAnimGen` SC3 (ZM-D-070):** SERPENT builder (12-bone: Spine00-05, Head, Tail00-02, HornL, HornR) + AQUATIC builder (8-bone: Spine00-02, Head, FinDorsal, FinPecL, FinPecR, FinCaudal), wired into `ZM_GetArchetypeAnimBuilder`; new per-vertebra phase-offset travelling-undulation idiom composed from the existing `ZM_AnimAddRotCurve` kit (no new kit primitive); `Tests/ZM_Tests_CreatureAnimArchetype_Serpent.cpp` + `_Aquatic.cpp` structural locks (Serpent Horn=Special-only, Aquatic no-FinCaudal-on-Special negative lock). Committed + pushed to master.

## Notes for next agent

- **The per-archetype authoring pattern is proven 3x (SC1 Quadruped, SC2 Biped/Avian, SC3 Serpent/Aquatic).** Per archetype: read the archetype's `ZM_CreatureArchetype_<Name>.cpp` for its EXACT frozen bone names + hierarchy, author 6 rotation-only clips against those names (only key bones that exist), wire `ZM_GetArchetypeAnimBuilder`, add a per-archetype structural test (positive + negative bone-signature locks). The generic `ZM_Tests_CreatureAnimGen.cpp` harness is `HasAnimBuilder`-gated + archetype-generalized, so it auto-covers new archetypes (ChannelsMatchSkeleton, ValidationPasses, byte-identity, determinism, clips-distinct, loop-wrap, one-shot-neutral, faint-clamp) -- do NOT edit it per archetype.
- **Per-archetype bone counts** (confirm exact NAMES from the code): QUADRUPED 18, BIPED 14, AVIAN 13, SERPENT 12, AQUATIC 8 (all DONE); INSECTOID 19, BLOB 4, FLOATER-PLANTOID 10 (remaining). BLOB (Spine00-02 + Nub) is sparsest, NO head/legs -- rotation-only spine sway + Nub bob. FLOATER's crown bone is literally named "Head".
- **Rotation-only is load-bearing** -- channels REPLACE bind-local TRS (not deltas); bind-local rotation is identity + scale unit for ALL species so absolute rotation is species-safe, but position varies per species (size scale) so NO position/scale channels. Express bob/hop/collapse via spine-flexion rotation. Travelling undulation = `ZM_AnimAddRotCurve` per bone with a per-bone phase offset inside a full-2pi sine (stays loop-closed).
- **Gotchas (paid for once):** quats are glm WXYZ (`glm::angleAxis(glm::radians(deg),axis)`); `SetDuration` is SECONDS but keyframe times are TICKS (tick = t01*durSec*24) -- use `ZM_AnimTicksForT01`; looping clips must key0==keyN per channel (Idle/Walk); one-shot clips non-looping, Attack/Special/Hit end at ~identity, Faint holds its collapsed final pose. Byte-equality uses `Zenith_DataStream::GetCursor()` not `GetCapacity()`. Fn-ptr nullness via `!= nullptr`.
- **SC6 (the box-ticking commit) touches `ZM_CreatureGen.*`:** append 6 ANIM values to `ZM_CREATURE_ASSET_KIND` + 6 `ZM_CreatureBasenameFmt` cases (`%s_Idle.zanim`..`%s_Faint.zanim`); implement `ZM_BakeCreatureClips` (`#ifdef ZENITH_TOOLS`); call it inside `ZM_BakeCreature` AFTER the texture bakes (Export creates no dirs); `AddAnimationPath` x6 in BOTH `.zmodel` blocks; bump `uZM_CREATUREGEN_VERSION` 2->3. The enum extension + the writes MUST land together (existing `CreatureBake_BundleFilesLand` loop auto-covers the new kinds). Tools bake smoke = ONE species (Fernfawn) x 6 clips. SC6 also updates TestPlan.md + AssetManifest.md and re-runs the full 4-config + D3D12 matrix (it edits `ZM_CreatureGen.*`).
- **Gate ORDER + baseline gotchas (unchanged):** `Tools\run_unit_gate.ps1` boots the exe DIRECTLY -- run `zenith test Zenithmon --headless` FIRST to heal DLLs, else `0xC0000135`. The exe hangs after the units line so the gate times out + kills but captures `Unit tests complete: N ran`; pass the CURRENT baseline + `-TimeoutSec 300`. Bump the count in BOTH `.github/workflows/zm-tests.yml` AND Status.md in the SAME commit. Build non-default configs via msbuild with the ABSOLUTE sln path. `git -C C:/dev/Zenith ...` in Bash calls.
- **Working model:** MASTER-ONLY (ZM-D-031); local gate is the authority; `zm-tests` is the post-push backstop, fix forward on red. Sweep stray `zenithmon.exe` before ending each iteration. The remaining S4 items have NO visual gate until the S8 vertical-slice go/no-go.
