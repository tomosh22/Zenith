# Zenithmon Status

**Last updated:** 2026-07-14
**Stage:** S4 (Asset generators) -- in progress. **`ZM_HumanGen` SC1 of 5 LANDED (ZM-D-074).** Roster data (`ZM_HumanData`, 34 rows: GDD-canonical named cast + original trainer/townsfolk types) + the shared 16-bone humanoid skeleton (`ZM_AppendSharedHumanBones`, identity bind rotation, feet at y~=0) + the frozen `ZM_HumanGen.h` seam (recipe/asset-paths/determinism/validation + 9-clip enum + golden metadata) + a MINIMAL valid mesh. **`ZM_CreatureGen` + `ZM_CreatureAnimGen` COMPLETE.** S1/S2/S3 COMPLETE.
**Build:** GREEN -- Vulkan_vs2022_Debug_Win64_True. (ZM_HumanGen adds new pure all-config TUs; the full 5-config matrix re-runs at the SC5 gate that adds the tools disk bake + edits `ZM_GenCommon`.)
**Tests:** boot unit gate **1872 ran / 1871 passed / 0 failed / 1 skipped** (was 1866; +6 `ZM_Gen` HumanGen units); `zenith test Zenithmon --headless` **7 passed / 0 failed**. `.github/workflows/zm-tests.yml` baseline = **1872**.

## Current task

**`ZM_HumanGen` SC2: per-model humanoid mesh loft.** Replace SC1's minimal placeholder `ZM_BuildHumanMesh` with the real proportioned humanoid loft (`Source/Gen/ZM_HumanMesh.cpp`): call `ZM_AppendSharedHumanBones` (bones 0..15) then loft torso/head+neck/2 arms/2 legs as `ZM_LoftRing`/`ZM_MeshLoft::AppendPart` parts, each ring skinned to <=2 SHARED bone indices (e.g. elbow ring 50/50 LeftUpperArm/LeftLowerArm); apply the recipe's BUILD (girth via ring m_fRx/m_fRz + m_fSuperEllipse) + a MODEST height factor (keep extremity rings near their fixed bones -- big height deltas drift mesh from the shared skeleton); finalise in the FIXED order `ZM_GenGenerateTangents` -> `ZM_GenNormalizeSkinWeights` (do NOT regenerate analytic loft normals). Transcribe the StickFigure golden ring proportions as authored `ZM_LoftRing` data (the survey confirmed they're directly transcribable). Wire `ZM_BuildHuman` + finalise `ZM_ValidateHuman`. Add tests to `Tests/ZM_Tests_HumanGen.cpp`: StructuralInvariants (all ids -- winding/bounds/weights/influences/cap), PerModelBonesMatchShared (every model's bone count+names+order == the shared skeleton; every vertex bone index < 16), SameSeedDeterminism (build twice -> `ZM_HumanBuildEqual` + equal `ZM_HumanContentHash`), Sensitivity (two distinct ids differ). Draw ALL proportion RNG up front via `ZM_MakeGenRNG(recipe, ZM_GEN_DOMAIN_MESH)` in a FIXED order (never SKELETON). Still NO texture beyond the SC1 placeholder / NO disk bake. Rebuild, boot unit gate at the new measured count, headless, commit + push. Remaining after SC2: SC3 texture + attachment silhouette, SC4 the shared 9-clip curves, SC5 tools disk bake (ticks the Roadmap box), SC6 optional windowed showcase.

## Last completed

**`ZM_HumanGen` SC1 (ZM-D-074):** `ZM_HumanData.{h,cpp}` (append-only `ZM_HUMAN_ID` + 34-row const table + accessors; named cast = GDD §3 canonical, generic types original) + `ZM_HumanGen.{h,cpp}` (the frozen seam: `uZM_HUMANGEN_VERSION=1`, `uZM_HUMAN_BONE_COUNT=16`, `ZM_HumanRecipe`+`ZM_ResolveHumanRecipe`, `ZM_MakeGenRNG`, `ZM_AppendSharedHumanBones`, `ZM_BuildHumanMesh` [minimal], `ZM_BuildHuman`, determinism/validation helpers, per-model + shared asset-path fns, the 9-clip enum + golden accessors, tools bake decls + no-op stubs) + `ZM_Tests_HumanGen.cpp` (6 units). Committed + pushed to master.

## Notes for next agent

- **`ZM_CreatureGen` + `ZM_CreatureAnimGen` are COMPLETE** (152 species: mesh/skeleton/albedo/shiny/icon/material/model + 6 anim clips each, baked bundles). Do NOT re-touch them. `ZM_HumanGen` is the CURRENT S4 work; then `ZM_BuildingGen`/`ZM_PropGen` + `ZM_BakeManifest` close S4.
- **ZM_HumanGen leverage:** ONE shared 16-bone skeleton + ONE shared 9-clip set for ALL ~35 models. Per-model variation is mesh-loft + texture ONLY; the skeleton is FIXED (no per-model SKELETON RNG). The 9 clips (SC4) are authored ONCE against the shared bone NAMES (StickFigure descriptive: Root/Spine/Neck/Head/Left+Right UpperArm/LowerArm/Hand + UpperLeg/LowerLeg/Foot) via the linkable `ZM_CreatureAnimCommon` kit -- rotation-only, pure `f(clip)`, byte-identical across models. Reuse `ZM_CreatureClipBytesEqual`/`ZM_CreatureClipContentHash`/`ZM_ValidateCreatureClip` for the clip gates.
- **Height-vs-shared-skeleton gotcha:** with ONE fixed `.zskel`, big per-model height deltas make extremity mesh drift from the fixed bones (elbow/knee crease misaligns). Keep height MODEST; push silhouette variety into BUILD girth + OUTFIT recolour + attachment meshes (SC3). Bound it at the SC6 visual check.
- **SC5 (box-ticking) adds two ADDITIVE tools-only `ZM_GenCommon` bridges** -- `ZM_GenBakeSkeleton` (skeleton-only) + `ZM_GenBakeMeshWithSharedSkeleton` (mesh that `SetSkeletonPath`s a SHARED ref, writes NO `.zskel`) -- because the existing `ZM_GenBakeMesh` writes a per-model `.zskel` (which humans must NOT do). Both are append-only, tools-only, change NO generated bytes -> `uZM_GENCOMMON_VERSION` stays 1, no creature re-bake. SC5 re-runs the full 5-config matrix + the creature `ZM_Gen` suite to prove creatures are unaffected. `ZM_BakeHumanShared` bakes the skeleton + 9 clips ONCE; `ZM_BakeHuman` writes per-model mesh/albedo/`.zmtrl`/`.zmodel` binding the shared refs (SetSkeletonPath shared `.zskel` + AddAnimationPath x9 shared `.zanim`). Owning-handle discipline for materials/models (`Create<T>()+GetDirect()` across Export). `create_directories` first; `exists()` is the only IO signal.
- **Gotchas (unchanged):** quats glm WXYZ; ticks = t01*durSec*24; looping clips key0==keyN; one-shots end ~identity (Faint holds); byte-equality uses `GetCursor()`; fn-ptr nullness `!= nullptr`; `Zenith_Assert` real vs `ZENITH_ASSERT` no-op. `ZM_GenMesh` SoA must stay a byte-mirror of `Zenith_MeshAsset` (no human-only vertex attrs). Bone names fit 48 chars (`uZM_GEN_BONE_NAME_MAX`).
- **Gate ORDER + baseline (unchanged):** `zenith test Zenithmon --headless` FIRST (heals DLLs, else `0xC0000135`); boot unit gate hangs after the units line -> times out+kills but captures `Unit tests complete: N ran`; pass CURRENT baseline + `-TimeoutSec 300`. Bump the count in `.github/workflows/zm-tests.yml` + Status.md (+ TestPlan.md at SC5) together. Non-default configs via msbuild ABSOLUTE sln path. `git -C C:/dev/Zenith ...`. Never commit baked assets.
- **Working model:** MASTER-ONLY (ZM-D-031); local gate is the authority; `zm-tests` is the post-push backstop, fix forward on red. Sweep stray `zenithmon.exe` before ending each iteration. NO visual gate until S8.
