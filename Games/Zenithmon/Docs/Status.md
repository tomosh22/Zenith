# Zenithmon Status

**Last updated:** 2026-07-15
**Stage:** S4 (Asset generators) -- in progress. **`ZM_HumanGen` SC1-SC2 of 5 LANDED (ZM-D-074/075).** The 34-row roster, shared fixed 16-bone skeleton, frozen public seam, and real six-part per-model humanoid loft are complete. **`ZM_CreatureGen` + `ZM_CreatureAnimGen` COMPLETE.** S1/S2/S3 COMPLETE.
**Build:** GREEN -- regeneration/check + target-only Vulkan_vs2022_Debug_Win64_True build. (SC2 adds one new pure all-config TU; the full 5-config matrix re-runs at the SC5 gate that adds the tools disk bake + edits `ZM_GenCommon`.)
**Tests:** boot unit gate **1876 ran / 1875 passed / 0 failed / 1 skipped** (was 1872; +4 `ZM_Gen` HumanGen units); `zenith test Zenithmon --headless` **7 passed / 0 failed**. The canonical helper reached its 300 s watchdog only after the successful tally and all 105 post-unit editor-authoring steps completed. `.github/workflows/zm-tests.yml` baseline = **1876**.

## Current task

**`ZM_HumanGen` SC3: texture + attachment silhouette.** Replace the SC1 placeholder albedo with deterministic per-model skin/hair/outfit synthesis and add recipe-selected attachment meshes for silhouette variety, while keeping the shared skeleton and bone mapping fixed and preserving per-domain RNG isolation. Add structural/determinism/sensitivity coverage. Still NO shared 9-clip curves (SC4) / NO disk bake (SC5). Remaining after SC3: SC4 shared 9-clip curves, SC5 tools disk bake (ticks the Roadmap box), SC6 optional windowed showcase.

## Last completed

**`ZM_HumanGen` SC2 (ZM-D-075):** `ZM_HumanMesh.cpp` replaced SC1's minimal placeholder with the StickFigure-derived torso/head+neck/2-arm/2-leg loft, skinned to unchanged shared bones 0..15, with modest height, BUILD attenuation, six fixed-order MESH-domain draws, and tangent-then-weight-normalization finalisation. Four units added: StructuralInvariants, PerModelBonesMatchShared, SameSeedDeterminism, and Sensitivity (including non-MESH-domain isolation). Public header, shared skeleton, version 1, texture/clip/bake outputs unchanged. Regeneration/build, unit and headless gates green; independent reviewer clean.

## Notes for next agent

- **`ZM_CreatureGen` + `ZM_CreatureAnimGen` are COMPLETE** (152 species: mesh/skeleton/albedo/shiny/icon/material/model + 6 anim clips each, baked bundles). Do NOT re-touch them. `ZM_HumanGen` is the CURRENT S4 work; then `ZM_BuildingGen`/`ZM_PropGen` + `ZM_BakeManifest` close S4.
- **ZM_HumanGen leverage:** ONE shared 16-bone skeleton + ONE shared 9-clip set for ALL ~35 models. Per-model variation is mesh-loft + texture ONLY; the skeleton is FIXED (no per-model SKELETON RNG). The 9 clips (SC4) are authored ONCE against the shared bone NAMES (StickFigure descriptive: Root/Spine/Neck/Head/Left+Right UpperArm/LowerArm/Hand + UpperLeg/LowerLeg/Foot) via the linkable `ZM_CreatureAnimCommon` kit -- rotation-only, pure `f(clip)`, byte-identical across models. Reuse `ZM_CreatureClipBytesEqual`/`ZM_CreatureClipContentHash`/`ZM_ValidateCreatureClip` for the clip gates.
- **Height-vs-shared-skeleton gotcha:** with ONE fixed `.zskel`, big per-model height deltas make extremity mesh drift from the fixed bones (elbow/knee crease misaligns). Keep height MODEST; push silhouette variety into BUILD girth + OUTFIT recolour + attachment meshes (SC3). Bound it at the SC6 visual check.
- **SC5 (box-ticking) adds two ADDITIVE tools-only `ZM_GenCommon` bridges** -- `ZM_GenBakeSkeleton` (skeleton-only) + `ZM_GenBakeMeshWithSharedSkeleton` (mesh that `SetSkeletonPath`s a SHARED ref, writes NO `.zskel`) -- because the existing `ZM_GenBakeMesh` writes a per-model `.zskel` (which humans must NOT do). Both are append-only, tools-only, change NO generated bytes -> `uZM_GENCOMMON_VERSION` stays 1, no creature re-bake. SC5 re-runs the full 5-config matrix + the creature `ZM_Gen` suite to prove creatures are unaffected. `ZM_BakeHumanShared` bakes the skeleton + 9 clips ONCE; `ZM_BakeHuman` writes per-model mesh/albedo/`.zmtrl`/`.zmodel` binding the shared refs (SetSkeletonPath shared `.zskel` + AddAnimationPath x9 shared `.zanim`). Owning-handle discipline for materials/models (`Create<T>()+GetDirect()` across Export). `create_directories` first; `exists()` is the only IO signal.
- **Gotchas (unchanged):** quats glm WXYZ; ticks = t01*durSec*24; looping clips key0==keyN; one-shots end ~identity (Faint holds); byte-equality uses `GetCursor()`; fn-ptr nullness `!= nullptr`; `Zenith_Assert` real vs `ZENITH_ASSERT` no-op. `ZM_GenMesh` SoA must stay a byte-mirror of `Zenith_MeshAsset` (no human-only vertex attrs). Bone names fit 48 chars (`uZM_GEN_BONE_NAME_MAX`).
- **Gate ORDER + baseline (unchanged):** `zenith test Zenithmon --headless` FIRST (heals DLLs, else `0xC0000135`); boot unit gate hangs after the units line -> times out+kills but captures `Unit tests complete: N ran`; pass CURRENT baseline + `-TimeoutSec 300`. Bump the count in `.github/workflows/zm-tests.yml` + Status.md (+ TestPlan.md at SC5) together. Non-default configs via msbuild ABSOLUTE sln path. `git -C C:/dev/Zenith ...`. Never commit baked assets.
- **Working model:** MASTER-ONLY (ZM-D-031); local gate is the authority; `zm-tests` is the post-push backstop, fix forward on red. Sweep stray `zenithmon.exe` before ending each iteration. NO visual gate until S8.
