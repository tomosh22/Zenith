# Zenithmon Status

**Last updated:** 2026-07-14
**Stage:** S4 (Asset generators) -- in progress. **`ZM_CreatureAnimGen` SC1 of 6 LANDED (ZM-D-068):** the frozen pure ALL-CONFIG anim-gen seam + shared curve kit + pure driver + the QUADRUPED clip builder. Clips are 6 per archetype (Idle/Walk/Attack/Special/Hit/Faint), authored ONCE per archetype against the frozen bone sets and byte-identical across every species of an archetype (pure `f(archetype,clip-id)`, rotation-only, no RNG). Reuses the engine's headerless `Flux_AnimationClip::Export` -- no engine change. `ZM_CreatureGen` COMPLETE + gate-signed (ZM-D-067). S1/S2/S3 COMPLETE.
**Build:** GREEN -- Vulkan_vs2022_Debug_Win64_True + Vulkan_vs2022_Debug_Win64_False + the D3D12_vs2022_Debug_Win64_False null-backend link proof (all-config purity of the new anim library confirmed; Release configs not re-run for this sub-commit).
**Tests:** boot unit gate **1857 ran / 1856 passed / 0 failed / 1 skipped** (was 1847; +10 new `ZM_Gen` anim units); `zenith test Zenithmon --headless` **7 passed / 0 failed**. `.github/workflows/zm-tests.yml` baseline bumped 1847 -> **1857** (same commit).

## Current task

**`ZM_CreatureAnimGen` SC2: Biped + Avian clip builders.** Add `Source/Gen/ZM_CreatureAnimArchetype_Biped.cpp` (arm+leg gait) and `_Avian.cpp` (wing flap, tucked legs), wire both into `ZM_GetArchetypeAnimBuilder` (the switch that currently returns the Quadruped fn only), and add any kit primitives they need (arm swing / wing flap). Author the 6 clips against each archetype's FROZEN bone-name set (BIPED = 14 bones, AVIAN = 13 -- confirm exact names from `Source/Gen/ZM_CreatureArchetype_Biped.cpp` / `_Avian.cpp`). Land `Tests/ZM_Tests_CreatureAnimArchetype_Biped.cpp` + `_Avian.cpp` (structural per-archetype locks, mirroring the Quadruped one). The generic `ZM_Tests_CreatureAnimGen.cpp` harness is `HasAnimBuilder`-gated, so it auto-exercises the two new archetypes once wired -- no generic-test edits needed. Still NO disk bake / NO `ZM_CreatureGen.*` edit (that is SC6). Rebuild, boot unit gate at the NEW measured count, headless, then commit + push directly to master. The remaining SCs: SC3 Serpent+Aquatic, SC4 Insectoid+Blob, SC5 FloaterPlantoid + all-8 totality gate, SC6 bundle-bake wiring (ticks the Roadmap box), SC7 optional windowed playability gate.

## Last completed

**`ZM_CreatureAnimGen` SC1 (ZM-D-068):** frozen seam header (`ZM_CreatureAnimGen.h` -- `uZM_CREATUREANIMGEN_VERSION=1`, pinned `ZM_ANIM_CLIP` enum, golden metadata accessors, `ZM_ArchetypeAnimFn` typedef + 8 builder decls + `ZM_GetArchetypeAnimBuilder`, pure `ZM_BuildCreatureClip`, `ZM_CreatureClipBytesEqual/ContentHash`, `ZM_ValidateCreatureClip`, tools `ZM_BakeCreatureClips` decl + non-tools no-op), shared curve kit (`ZM_CreatureAnimCommon.{h,cpp}`), pure driver, QUADRUPED builder (18-bone set), + 10 `ZM_Gen` units. Other 7 archetype builders declared-but-unwired (nullptr); tools bake is a `return false` stub (real bundle bake = SC6). Committed + pushed to master this iteration.

## Notes for next agent

- **The design spec is authoritative and complete.** The full ZM_CreatureAnimGen build spec (engine `.zanim` API facts, seam, per-clip semantics, SC1-SC7 breakdown, risks) was produced by a design workflow and is captured in ZM-D-068 + the per-archetype clip motions live in the SC1 quadruped builder as the pattern to copy. Follow the SAME pattern per archetype: read the archetype's `ZM_CreatureArchetype_<Name>.cpp` for its EXACT frozen bone names, author 6 rotation-only clips against those names (only key bones that exist), wire `ZM_GetArchetypeAnimBuilder`, add a per-archetype structural test.
- **Per-archetype bone counts** (confirm exact NAMES from the code): QUADRUPED 18 (done), BIPED 14, AVIAN 13, SERPENT 12, AQUATIC 8, INSECTOID 19, BLOB 4, FLOATER-PLANTOID 10. Limbless archetypes (SERPENT/AQUATIC/BLOB/FLOATER) drive spine-undulation / fin / tendril rotations instead of leg gait; BLOB (Spine00-02 + Nub) is sparsest; FLOATER's crown bone is literally named "Head".
- **Rotation-only is load-bearing** -- channels REPLACE bind-local TRS (not deltas); bind-local rotation is identity + scale unit for ALL species so absolute rotation is species-safe, but position varies per species (size scale) so NO position/scale channels. Express bob/hop/collapse via spine-flexion rotation.
- **Gotchas (paid for once):** quats are glm WXYZ (`glm::angleAxis(glm::radians(deg),axis)`); `SetDuration` is SECONDS but keyframe times are TICKS (tick = t01*durSec*24) -- use `ZM_AnimTicksForT01`; looping clips must key0==keyN per channel (Idle/Walk); one-shot clips non-looping, Attack/Special/Hit end at ~identity (clip-end clamp holds neutral), Faint holds its collapsed final pose (do NOT return to bind). Byte-equality uses `Zenith_DataStream::GetCursor()` (bytes written) not `GetCapacity()`. Fn-ptr nullness via `!= nullptr` (MSVC rejects `ZENITH_ASSERT_NULL` on fnptrs).
- **SC6 (the box-ticking commit) touches `ZM_CreatureGen.*`:** append 6 ANIM values to `ZM_CREATURE_ASSET_KIND` + 6 `ZM_CreatureBasenameFmt` cases (`%s_Idle.zanim`..`%s_Faint.zanim`); implement `ZM_BakeCreatureClips` (under `#ifdef ZENITH_TOOLS`); call it inside `ZM_BakeCreature` AFTER the texture bakes (Export creates no dirs -- relies on the species folder those bakes made); `AddAnimationPath` x6 in BOTH `.zmodel` blocks; bump `uZM_CREATUREGEN_VERSION` 2->3 (baked `.zmodel` bytes change). The enum extension + the `ZM_BakeCreatureClips` writes MUST land together (the existing `CreatureBake_BundleFilesLand` loop auto-covers the new kinds). Tools bake smoke = ONE species (Fernfawn) x 6 clips, matching `CreatureBake`'s scope.
- **Gate ORDER + baseline gotchas (unchanged):** `Tools\run_unit_gate.ps1` boots the exe DIRECTLY -- run `zenith test Zenithmon --headless` FIRST to heal DLLs, else `0xC0000135`. The exe hangs after the units line so the gate times out + kills but captures `Unit tests complete: N ran`; pass the CURRENT baseline + `-TimeoutSec 300`. When ZM_ boot units change, bump the count in BOTH `.github/workflows/zm-tests.yml` AND Status.md (and TestPlan.md/Roadmap.md at SC6) in the SAME commit. Build non-default configs via msbuild with the ABSOLUTE sln path `C:\dev\Zenith\Games\Zenithmon\zenithmon_win64.sln`. `git -C C:/dev/Zenith ...` in Bash calls.
- **Working model:** MASTER-ONLY (ZM-D-031); local gate is the authority; `zm-tests` is the post-push backstop, fix forward on red. Sweep stray `zenithmon.exe` before ending each iteration. The remaining S4 items have NO visual gate until the S8 vertical-slice go/no-go.
