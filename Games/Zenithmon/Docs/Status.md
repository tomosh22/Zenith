# Zenithmon Status

**Last updated:** 2026-07-14
**Stage:** S4 (Asset generators) -- in progress. `ZM_GenCommon`/`ZM_TextureSynth` foundation (ZM-D-059) + `ZM_CreatureGen` **SC1** (ZM-D-060) COMPLETE. S1/S2/S3 COMPLETE (S3 visual gate SIGNED OFF 2026-07-13).
**Build:** passing -- Vulkan_vs2022_Debug_Win64_True green (regen picks up the 5 new `Source/Gen` + `Tests` files via the game's source glob).
**Tests:** boot unit gate **1824 ran / 1823 passed / 0 failed / 1 skipped** (baseline bumped 1804 -> 1824, in `.github/workflows/zm-tests.yml` too); `zenith test Zenithmon --headless` **6 passed / 0 failed**.

## Current task

`ZM_CreatureGen` is split into sub-commits against the now-FROZEN `Source/Gen/ZM_CreatureGen.h` seam. SC1 (QUADRUPED reference) landed this commit. **Next unchecked: SC2 -- BIPED + AVIAN archetype builders** (`ZM_CreatureArchetype_Biped.cpp` + `_Avian.cpp` + their per-archetype tests), authored disjointly against the frozen header + `ZM_CreatureArchetypeCommon.{h,cpp}` kit. Then SC3 (SERPENT+AQUATIC), SC4 (INSECTOID+BLOB), SC5 (FLOATER-PLANTOID + all-152 coverage gate + `.zmtrl`/`.zmodel` bundle bake + the windowed species-gallery visual gate). The S4 GATE ends with that gallery visual check -- a HARD-STOP for user sign-off (do NOT tick S4 without it).

## Last completed

**S4 `ZM_CreatureGen` SC1** (this commit, ZM-D-060). The frozen public seam + core driver (`Source/Gen/ZM_CreatureGen.{h,cpp}`), the shared append-kit (`ZM_CreatureArchetypeCommon.{h,cpp}`), the QUADRUPED reference builder (`ZM_CreatureArchetype_Quadruped.cpp`), and the full generic test suite (`Tests/ZM_Tests_CreatureGen.cpp` + `ZM_Tests_CreatureArchetype_Quadruped.cpp`). 2,475 new lines; +20 `ZM_Gen` creature units (1804 -> 1824). Reviewer verdict: no blockers/majors.

## Notes for next agent

- **The header is FROZEN (append-only, tech-lead-only).** To land an archetype: author `Source/Gen/ZM_CreatureArchetype_<Name>.cpp` (its builder `void ZM_BuildArchetype_<Name>(ZM_GenMesh&, const ZM_CreatureRecipe&)`), add ONE case to the EXPLICIT switch in `ZM_GetArchetypeBuilder` (`ZM_CreatureGen.cpp`), and add `Tests/ZM_Tests_CreatureArchetype_<Name>.cpp` with a structural bone assert. The generic harness (`ZM_Tests_CreatureGen.cpp`) AUTO-GROWS the 12 universal invariants over any species whose builder is non-null -- no harness edit needed. `nullptr`-until-landed means un-authored archetypes are skipped, never assert.
- **Reuse the kit** (`ZM_CreatureArchetypeCommon`): `ZM_AppendSpineTube/Limb/Tail/Horn/EllipsoidHead` + `ZM_SizeClassScale` + `ZM_FormatBoneName`. Each authored ring binds a SINGLE bone; the loft subdivision is the only <=2-influence source. Draw randomness ONLY via `ZM_MakeGenRNG(recipe, ZM_GEN_DOMAIN_MESH/SKELETON)`, ALL draws up-front in a FIXED order (independent of emission order). Bone topology IDENTICAL across evo stages (names/order/count) so clips transfer; elaboration = `m_uEvoStage-1` scales detail (e.g. horn size), never topology. Scale extents by `m_fSizeScale`. Do NOT finalise/bake in a builder (the driver does).
- **Golden pins are frozen** (ZM-D-060): size-scale curve, primary-type->pattern table, shiny band [80,280), eye placement, dex-icon method, `game:Creatures/<Name>/...` asset scheme, QUADRUPED proportions. Any generation-algorithm change bumps `uZM_CREATUREGEN_VERSION`. Keep ALBEDO domain driving `ZM_SynthCreatureAlbedo`; PATTERN stays reserved unless you add explicit pattern draws.
- **SC5 owes:** `.zmtrl` (normal + shiny child material) + `.zmodel` bundle writes (currently a `// SC5:` TODO in `ZM_BakeCreature`), the all-152 coverage gate, and the windowed species-gallery visual gate (set a `GATE-WAIT: S4 visual sign-off` marker + capture evidence, then STOP).
- **Gate ORDER gotcha (this cost two false failures):** `Tools\run_unit_gate.ps1` boots the exe DIRECTLY and does NOT heal DLLs. Run `Import-Module Build\zenith_buildsystem.psm1; Repair-ZenithRuntimeDlls -ExeDir <exeDir>` (or `zenith test --headless`) FIRST, else the exe dies instantly with exit `0xC0000135` / -1073741515 (STATUS_DLL_NOT_FOUND -- assimp-vc143-mtd/minizip/poly2tri/pugixml/zlibd1 are copied from sibling game output dirs). Then the exe HANGS after the units-at-boot line (scene/shutdown), so `run_unit_gate` always times out and kills -- but the `Unit tests complete: N ran...` line is captured BEFORE the kill, so that "failure" is only the baseline-count mismatch. Use `-TimeoutSec 300`; to discover a new count, pass the OLD baseline and read the actual reported line.
- **Workflow `args` gotcha:** passing paths via the Workflow tool's `args` object arrived as the literal string `"undefined"` inside the script this session -- inline paths as string literals in the workflow script body instead of relying on `args`.
- **Format-doc debt still deferred to the S4 GATE** (per ZM-D-059's at-stage-gate policy): full `Docs/Shortfalls.md` verdict/count refresh, the `ZM_Gen` creature test group in `Docs/TestPlan.md`, and the creature-bundle catalogue/budgets in `Docs/AssetManifest.md` land in the S4 stage-gate commit.
- **Working model unchanged:** MASTER-ONLY (ZM-D-031); the local gate (build + boot unit gate + headless) is the authority; `zm-tests` is the post-push backstop, fix forward on red. Sweep stray `zenithmon.exe` (`Get-Process zenithmon`) before ending each iteration.
