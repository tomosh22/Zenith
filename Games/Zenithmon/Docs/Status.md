# Zenithmon Status

**Last updated:** 2026-07-14
**Stage:** S4 (Asset generators) -- **ZM_CreatureGen COMPLETE; PARKED at the S4 species-gallery visual gate (GATE-WAIT, awaiting human sign-off).** All 8 archetype builders (SC1-SC5a, ZM-D-060..064) + the .zmtrl/.zmodel bundle bake (SC5b, ZM-D-065) + the windowed species-gallery gate test (SC5c, ZM-D-066) COMPLETE. Every one of the 152 species builds + bakes a scene-loadable 9-file bundle. (Foundation = ZM-D-059.) S1/S2/S3 COMPLETE (S3 visual gate SIGNED OFF 2026-07-13).
**Build:** GREEN across the S4-gate matrix -- Vulkan_vs2022_{Debug,Release}_Win64_{True,False} all build + the D3D12_vs2022_Debug_Win64_False null-backend link proof.
**Tests:** boot unit gate **1847 ran / 1846 passed / 0 failed / 1 skipped**; `zenith test Zenithmon --headless` **6 passed / 0 failed** (the windowed ZM_CreatureGallery_Test skips headless by design); windowed `ZM_CreatureGallery_Test` **PASSED** (3 TGAs captured). `.github/workflows/zm-tests.yml` baseline = 1847.

## Current task

**GATE-WAIT: S4 visual sign-off.** ZM_CreatureGen is fully implemented and every automated S4-gate item is green (matrix + unit + headless + the windowed gallery). The autonomous loop is PARKED here awaiting the human's visual sign-off (StartPrompts.md prompt 4). Do NOT proceed past this gate, tick S4, or start ZM_CreatureAnimGen without the sign-off.

**Visual evidence** -- windowed `ZM_CreatureGallery_Test` renders a diverse dozen (>=1 per archetype + Zenithrax shown SHINY) as a 4x3 grid on a neutral backdrop, 3 angles:
- `Build/artifacts/zenithmon/s4/visual/gallery_01.tga` (+ `.png`) -- front elevated
- `Build/artifacts/zenithmon/s4/visual/gallery_02.tga` (+ `.png`) -- left 3/4
- `Build/artifacts/zenithmon/s4/visual/gallery_03.tga` (+ `.png`) -- right 3/4
(TGA = the engine's `Flux_Screenshot::RequestDump`; PNG = a viewing conversion. Sampled: Fernfawn/Sylvastag QUAD, Pyroclast BIPED, Tidesabre AQUATIC, Stratavis AVIAN, Aurelwing/Verdantis INSECTOID, Zenithrax SERPENT [shiny], Monolode BLOB, Zephyrbloom FLOATER_PLANTOID, Auricorn QUAD, Emberkoi AQUATIC.)

**Orchestrator's read of the captures (for the reviewer):** all 12 baked creatures load + render as distinct, correctly-proportioned models spanning all 8 archetypes, each with working eyes + its own type-palette colour + the shiny variant; clean neutral background, no scene bleed. **Known aesthetic note:** the palette reads SOFT/PASTEL (low measured saturation). If you want punchier colours it is a quick follow-up -- dim the gallery lights further / disable IBL-diffuse for the render, or raise saturation in `ZM_SynthCreatureAlbedo` for the actual baked assets (a generator-version bump). Creature ANIM is a separate later box (ZM_CreatureAnimGen), so the gallery shows a static bind pose.

**On APPROVE (prompt 4):** append the sign-off to DecisionLog, tick the `ZM_CreatureGen` line in Roadmap.md, clear this GATE-WAIT, then resume the loop at the next S4 Roadmap item (`ZM_CreatureAnimGen`). **On REJECT:** log the rework (e.g. punchier palette, per-species fixes) as the current task and execute it directly on master.

## Last completed

**S4 `ZM_CreatureGen` SC5c** (this commit, ZM-D-066): the windowed species-gallery visual-gate test `Tests/ZM_AutoTests_Gallery.cpp` (`ZM_CreatureGallery_Test`) -- bakes a diverse dozen, renders a framed 4x3 grid (bloom off, fixed exposure, neutral backdrop, FrontEnd bleed suppressed via GraphicsOptions saved/restored), captures 3 TGAs, asserts all 12 models render + all 3 files land. Plus the S4-gate matrix (all green) + the deferred format-doc refresh (TestPlan/AssetManifest creature catalogue, Shortfalls verdict). SET this GATE-WAIT and STOPPED for sign-off. (SC5a-b landed the builder + bundle bake; ZM_CreatureGen mesh/skeleton/texture/material/model generation is feature-complete for the full dex.)

## Notes for next agent

- **You are PARKED at a visual gate.** Per the loop protocol (StartPrompts prompt 0 step 1): while this GATE-WAIT marker is present and DecisionLog has no matching S4 sign-off, report "waiting for S4 visual sign-off" (evidence paths above) and END the iteration -- do nothing else. The user unblocks with StartPrompts prompt 4.
- **After sign-off, the next S4 item is `ZM_CreatureAnimGen`** (6 clips/archetype instantiated per species, ~900 .zanim), then ZM_HumanGen, ZM_BuildingGen/ZM_PropGen, and ZM_BakeManifest (the byte-identical re-bake invariant -- SC5b's bake smoke only checks the 9 files land, not re-bake determinism).
- **Creature generator recap (all done):** all 8 `ZM_BuildArchetype_*` wired via the explicit switch in `ZM_CreatureGen.cpp`; the generic 12-invariant harness (`ZM_Tests_CreatureGen.cpp`) covers all 152 species; per-archetype structural tests + the all-152 gate + golden-locks + the tools-gated bake smoke (`ZM_Tests_CreatureBake.cpp`); `ZM_BakeCreature` writes the full 9-file bundle. Golden pins (size-scale, type->pattern, shiny band, asset scheme, per-archetype proportions) are frozen -- any change bumps `uZM_CREATUREGEN_VERSION` + forces a cold re-bake.
- **Gallery/visual-capture recipe (reusable for S8's go/no-go + future visual gates):** a windowed `ZENITH_AUTOMATED_TEST_REGISTER` test with `m_bRequiresGraphics=true` (auto-skips headless); tune the look via `Zenith_GraphicsOptions::Get()` SAVED in Setup + RESTORED in cleanup (bloom, skybox on/off + colour, auto-exposure, IBL, `m_bTextEnabled`/`m_bQuadsEnabled` to kill UI bleed); at FIXED exposure 1.0 directional light intensity is O(1) lux (anchor on Flux_MaterialPreview's sun=3.0); capture with `Flux_Screenshot::RequestDump(fsTgaPath)` (full-swapchain TGA; create the dir first). Run windowed via `zenith test Zenithmon --filter <TestName>` (NO --headless). `dbg_fHDRExposure` is a static -- NOT settable from a test (pinned at 1.0); calibrate via light intensity, not exposure.
- **Gate ORDER gotcha:** `Tools\run_unit_gate.ps1` boots the exe DIRECTLY without healing DLLs -- run `Repair-ZenithRuntimeDlls -ExeDir <exeDir>` (or `zenith test --headless`) FIRST, else `0xC0000135` (STATUS_DLL_NOT_FOUND). The exe hangs after the units-at-boot line, so the gate times out + kills but captures the `Unit tests complete: N ran...` line; pass the CURRENT baseline (`-TimeoutSec 300`); on a mismatch it "fails" but prints the real count. Bump `-Baseline` in the gate cmd AND `.github/workflows/zm-tests.yml` together when ZM_ boot units change.
- **Matrix gotcha:** build non-default configs via msbuild with the ABSOLUTE sln path (`C:\dev\Zenith\Games\Zenithmon\zenithmon_win64.sln`) -- a relative path fails MSB1009 in a background job.
- **Working model:** MASTER-ONLY (ZM-D-031); local gate is the authority; `zm-tests` is the post-push backstop, fix forward on red. Sweep stray `zenithmon.exe` before ending each iteration.
