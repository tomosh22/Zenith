# Zenithmon Status

**Last updated:** 2026-07-16
**Stage:** S4 (Asset generators) -- in progress. **`ZM_BuildingGen`/`ZM_PropGen`: SC1-SC4 of 6 LANDED (ZM-D-079..082).** SC1 static-model foundation, SC2 parametric building shell, SC3 building facade decals, SC4 `ZM_PropGen` (25-prop generator). Both generator families now produce complete in-memory static bundles (mesh + texture); only the DISK BAKE (SC5) + coverage/docs (SC6) remain. **`ZM_HumanGen`/`ZM_CreatureGen`/`ZM_CreatureAnimGen` COMPLETE.** S1/S2/S3 COMPLETE.
**Build:** GREEN -- Vulkan_vs2022_Debug_Win64_True + Vulkan_vs2022_Debug_Win64_False (non-tools link proof). The full five-config matrix re-runs at SC5 (the disk-bake commit that adds `ZM_GenBakeStaticMesh` + tools bake bodies) and the S4 gate.
**Tests:** boot unit gate **1903 ran / 1902 passed / 0 failed / 1 skipped** (was 1895; +8 `ZM_Gen` PropGen units -- 6 structural + 2 domain-isolation); `zenith test Zenithmon --headless` **7 passed / 0 failed**. The canonical boot-unit helper reached its known 300 s watchdog only after the successful unit tally. `.github/workflows/zm-tests.yml` baseline = **1903**.

## Current task

**`ZM_BuildingGen`/`ZM_PropGen` SC5: tools disk bake for BOTH families.** (1) Add ONE new additive tools-only `ZM_GenCommon` bridge `ZM_GenBakeStaticMesh(const ZM_GenMesh&, const char* szMeshPath)` -- element-wise copies the pure buffers into a `Zenith_MeshAsset`, `ComputeBounds`, Exports ONLY the `.zmesh`; calls NO `SetSkeletonPath`, NO `SetVertexSkinning`, builds NO `Zenith_SkeletonAsset` (so the on-disk mesh is a pure STATIC mesh, `HasSkinning()`==false). Copy the vertex/tri block from `ZM_GenBakeMesh` MINUS the skin/skeleton parts; `#ifdef ZENITH_TOOLS` + non-tools no-op inline. `uZM_GENCOMMON_VERSION` stays 1 (additive, no existing byte moves). (2) Fill the `ZM_BakeBuilding`/`ZM_BakeAllBuildings` + `ZM_BakeProp`/`ZM_BakeAllProps` stubs (mirror `ZM_BakeCreature`/`ZM_BakeHuman`: `ZM_GenBakeStaticMesh` for `.zmesh` + `ZM_SynthBakeAlbedoBC1` for the albedo + owning-handle `Create<T>()`+`GetDirect()` `.zmtrl` + a SKELETON-LESS `.zmodel` [SetName + AddMeshByPath + one material; NO SetSkeletonPath, NO AddAnimationPath] + Export; `create_directories` first; `exists()` the only IO signal). (3) Add tools-only bake smokes (`ZM_Tests_BuildingBake.cpp` + `ZM_Tests_PropBake.cpp`, `#ifdef ZENITH_TOOLS`, `ZENITH_SKIP` when bake env absent): bake one building + one prop, assert the 4 files land non-empty, and hermetically load the `.zmodel` asserting `HasSkeleton()`==false + 0 animations. Gate: regen + FULL 5-config matrix (Vulkan Debug/Release x True/False + D3D12 Debug False link proof) + boot unit gate + headless. Bump baseline for the +N bake smokes.

## Last completed

**`ZM_PropGen` SC4 (ZM-D-082).** New `ZM_PropGen` family (5 files) mirroring `ZM_BuildingGen`: 25-prop `ZM_PropData` roster (fences/signs/lamps/bridges/ledges/rocks/furniture + 6 biome dressing sets), frozen seam (`uZM_PROPGEN_VERSION`=1), per-kind STATIC box composition via the shipped `ZM_StaticMesh::AppendBox` (NO new emitter -> `uZM_GENCOMMON_VERSION` stays 1), 128^2 ALBEDO-domain placeholder albedo, `game:Props/<Name>/` scheme, tools bake STUBS. 8 units (6 structural + 2 domain-isolation added to close a reviewer NIT). Plan -> implement -> Debug True + Debug False gate -> reviewer (clean; verified no degenerate box after jitter, all grounded).

## Notes for next agent

- **Active S4 item = `ZM_BuildingGen`/`ZM_PropGen` (SC1-SC4 done; SC5 disk bake + SC6 coverage/docs remain).** `ZM_HumanGen`/`ZM_CreatureGen`/`ZM_CreatureAnimGen` COMPLETE. After this item -> `ZM_BakeManifest` closes S4 -> S4 gate (4-config matrix + windowed gallery + **visual sign-off** = the next VISUAL GATE).
- **SC5 static-bake reference:** `ZM_BakeCreature` (`ZM_CreatureGen.cpp` ~486-634) + `ZM_BakeHuman` (`ZM_HumanGen.cpp` ~445-513, ZM-D-078) are the templates for the owning-handle `.zmtrl`/`.zmodel` write; SC5 differs ONLY in that props/buildings are STATIC -> the `.zmodel` has NO skeleton path + NO animation paths, and the mesh bakes via the NEW `ZM_GenBakeStaticMesh` (not `ZM_GenBakeMesh`, which writes a per-model `.zskel`). The existing bridges (`ZM_GenBakeMesh`/`ZM_GenBakeSkeleton`/`ZM_GenBakeMeshWithSharedSkeleton`) all force a skeleton -- hence the new static one. Bake smoke asserts the reloaded `.zmodel` `HasSkeleton()`==false + `GetNumAnimations()`==0 (contrast the creature/human smokes). `ZM_BuildingGen.h`/`ZM_PropGen.h` already declare the bake entry points (currently stubs returning false).
- **Static-shell kit:** `ZM_StaticMesh::AppendBox`/`AppendGableRoof`/`AppendHipRoof`/`AppendFlatRoof` (bone-free) + `ZM_PushStaticTri` (auto-orients outward via an interior point). `ZM_ValidateGenMeshStatic` for static meshes (NOT the skinned validator). Winding `cross(C-A,B-A).normal>0`, hard normals (only `ZM_GenGenerateTangents`). UV islands sub-rects of [0,1]. Draw all RNG up-front, fixed order, one domain (MESH mesh / ALBEDO texture). `ZM_GEN_DOMAIN` PINNED.
- **Reviewer NIT still open (SC4, harmless):** LAMP props' roster `m_fWidth`/`m_fDepth` (0.4/0.4) are dead data (the lamp mesh uses fixed X/Z sizes). No action needed.
- **Doc cadence:** CI baseline (`zm-tests.yml`) bumps EVERY SC; `TestPlan`/`AssetManifest` prose reconcile at item completion (SC6). `TestPlan` baseline lags at 1886 -- reconcile to the final count + add the full BuildingGen/PropGen `ZM_Gen` sections + AssetManifest section 3 file breakdown at SC6.
- **Gate ORDER + baseline (unchanged):** `zenith test Zenithmon --headless` FIRST (heals DLLs, else `0xC0000135`); boot unit gate hangs after the units line -> `run_unit_gate.ps1 -Baseline N -TimeoutSec 300` times out+kills but captures `Unit tests complete: N ran`. New files need `pwsh -NoProfile -File Build\regen.ps1`. Non-default configs via msbuild ABSOLUTE sln path (`C:\Program Files\Microsoft Visual Studio\18\Insiders\MSBuild\Current\Bin\MSBuild.exe`), serial only. `git -C C:/dev/Zenith ...`.
- **Working model:** MASTER-ONLY (ZM-D-031); local gate is the authority; `zm-tests` is the post-push backstop, fix forward on red. Sweep stray `zenithmon.exe` before ending. Orchestrator+subagents this session (Plan -> Implement -> Reviewer per SC); only the orchestrator builds/tests/commits. A stray harness worktree exists at `.claude/worktrees/` -- NEVER regen/build/commit from it (Invariant 2). Never commit baked assets (SC5 writes to git-ignored `Assets/` -- the bake code ships, not the output).
