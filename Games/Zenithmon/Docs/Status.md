# Zenithmon Status

**Last updated:** 2026-07-18
**Stage:** **S5 (Battle integration slice) IN PROGRESS.** S0-S4 COMPLETE. **S5 items 1+2+3 DONE; item 4 IN PROGRESS -- SC1 (engine E3 typewriter, ZM-D-100) + SC2 (`ZM_BattleDirectorCore`, ZM-D-101) + SC3 (`ZM_BattleDirector` ECS component order 111, ZM-D-102) DONE.** **NEXT is item 4 SC4** (`ZM_UI_BattleHUD` presentation -- the battle text log via the E3 typewriter + HP panels; the FIRST visible battle UI). Then SC5 (interactive Fight/Run + move menu -> `SubmitPlayerAction`), SC6 (windowed win gate ticks the item-4 box), then item 5, then the **S5 GATE = the next VISUAL hard-stop** (needs the user's sign-off).
**Build:** GREEN -- Zenithmon Vulkan Debug True clean. Master clean + pushed at the SC3 commit (see git log).
**Tests:** boot unit gate **1967 ran / 1966 passed / 0 failed / 1 skipped** (SC3 = **+5 pure `ZM_BattleDirector` game units**; game-only, so `zm-tests.yml` bumped **1962 -> 1967** and the engine default `run_unit_gate.ps1` **1088** is UNCHANGED). `zenith test Zenithmon --headless` **16/0**. Windowed **5/0, zero skips**: the new `ZM_BattleDirectorRoundTrip_Test` + the 4 coexistence tests (`ZM_BattleRoundTrip_Test`/`ZM_BattleEncounterLatch_Test`/`ZM_BattleArena_Test`/`ZM_BattleArenaOwnScene_Test`) all genuinely ran against real baked assets.

## Current task

**S5 item 4 SC4 -- `ZM_UI_BattleHUD` presentation** (Roadmap.md:86). The FIRST visible battle UI: a text log that reveals lines with the engine E3 typewriter (`Zenith_UIText::SetVisibleGlyphCount`, shipped SC1/ZM-D-100) + HP panels for both actives. It presents the `ZM_BattleDirectorCore`'s `ZM_BattlePresentationOp` stream (the director already drives it; SC4 renders it). Still AI-vs-AI + no interaction -- interactive Fight/Run is SC5.

**PLANNED:** the durable item-4 plan is on disk at `Build/artifacts/zm_s5_item4_plan/plan.md` (git-ignored; regenerate if absent) -- see its SC4 section. Summary of remaining item-4 SCs: **SC4** `ZM_UI_BattleHUD` (text log w/ E3 typewriter + HP panels); **SC5** interactive Fight/Run + move menu -> `SubmitPlayerAction`; **SC6** windowed win gate ticks the item-4 box.

## Last completed

**S5 item 4 SC3 (ZM-D-102) -- `ZM_BattleDirector` ECS component (order 111).** `Components/ZM_BattleDirector.{h,cpp}`: an entity in the additively-loaded Battle scene that watches the persistent `ZM_BattleTransition`; on IN_BATTLE it one-shot `Begin`s the deep-owned SC2 `ZM_BattleDirectorCore` (deterministic placeholder player + `ZM_BuildWildEnemySpec` GREEDY enemy, exp OFF), best-effort places two creature models on the arena platforms (`GetChildEntityID(1)`=player/`(2)`=enemy), drives the AI-vs-AI battle turn by turn, and calls `ZM_BattleTransition::RequestBattleEnd()` **exactly once** on resolution (latched; 30 s wall-clock deadline backstop). Authored into the Battle-scene recipe after `BattleArena`. +5 T0 units + the windowed `ZM_BattleDirectorRoundTrip_Test` (proves the DIRECTOR ends the battle, item-3 resume invariants intact). zm-tests 1962->1967, engine default 1088 unchanged. Build + gate + reviewer (no blockers) green. Prior: SC2 (ZM-D-101) `ZM_BattleDirectorCore`; SC1 (ZM-D-100) engine E3 typewriter.

## Notes for next agent (S5 item 4 SC4)

- **`ZM_UI_BattleHUD` sort order MUST be > 10001** or the `BattleFade` overlay swallows it (the fade's full-canvas text clip hides every lower-order UIText). WarpFade = 10000, BattleFade = 10001 (ZM-D-097).
- **The director already exists and drives the battle** (`ZM_BattleDirector`, order 111). SC4 does NOT re-drive it -- it READS the `ZM_BattleDirectorCore`'s presentation state to render. Decide the HUD<->director seam: either the HUD queries the director/core (`GetState`/`CurrentOp`/`SideActiveHP`/`SideActiveMaxHP`/`SideActiveLevel`/`SideActiveSpecies` are on `ZM_BattleDirectorCore`), or the director exposes a small read accessor. Keep the director the single owner of the core (do not construct a second core in the HUD).
- **E3 typewriter is shipped** (`Zenith_UIText::SetVisibleGlyphCount(int)` / `GetVisibleGlyphCount` / `GetTotalGlyphCount` / static `ClipToVisibleGlyphs`; default -1 = fully revealed; ZM-D-100). Use it for the text log's per-line reveal; it does NOT reflow the box.
- **Reviewer nit carried from SC3 (address in SC5, not SC4):** `ZM_BattleDirector::OnUpdate` ignores `RequestBattleEnd()`'s bool return -- safe today (director is the sole in-Battle caller), but when SC5 adds an interactive Run/flee path that can also end the battle, branch on the return. Also: SC3's `Write`/`ReadFromDataStream` (bare version stamp) has no dedicated T0 unit by design -- it is covered by the windowed scene-load path.
- **Next-free ECS order is 112** (111 = `ZM_BattleDirector`, 110 = `ZM_BattleTransition`, 109 = `ZM_TallGrassSystem`, 108 = `ZM_BattleArena`).
- **Gate ORDER + baselines (permanent):** `zenith test Zenithmon --headless` FIRST (heals DLLs). Boot unit gate hangs after the units line -> `pwsh -NoProfile -File Tools\run_unit_gate.ps1 -Exe <exe> -Baseline 1967 -TimeoutSec 300` (the 300s timeout-kill is EXPECTED; the units line is already logged). Windowed: `--filter <Test>` (or `--filter ZM_Battle` runs all 5 battle windowed tests in one boot). New files -> `Build\regen.ps1`. ENGINE-unit changes bump BOTH `zm-tests.yml` AND `Tools\run_unit_gate.ps1`'s default (1088) + need Combat/RenderTest/DP/CityBuilder regression (RenderTest pre-existingly red -- Q-2026-07-16-001).
- **Working model:** MASTER-ONLY (ZM-D-031); LOCAL gate is authority; `zm-tests` post-push backstop. Orchestrated: only the orchestrator builds/tests/commits; subagents author, never build. Sweep stray `zenithmon.exe` before ending. NEVER commit baked assets or captures.
- **Open Questions:** Q-2026-07-17-002 (S5 item-4 scoping -- SC3 acted on best-guesses (a) placeholder player team, (b) exp OFF, (d) GREEDY wild AI; SC4/SC5 continue on these); Q-2026-07-17-001 (`BiomeForScene` hard-coded table -- propose a WorldSpec biome column at S9/S10); Q-2026-07-16-001 (RenderTest terrain); Q-2026-07-09-003 (S5 bleed-through -- verified/falsified only at the S5 SCREENSHOT gate).
- **The S5 gate is the next VISUAL hard-stop** (all 5 items: windowed encounter round-trip + catch + exp + exact resume + the bleed-through screenshot). Do not tick it or start S6 without the user's sign-off.
