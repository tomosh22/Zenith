# Zenithmon Status

**Last updated:** 2026-07-16
**Stage:** S4 (Asset generators) -- in progress. **`ZM_BuildingGen`/`ZM_PropGen`: SC1+SC2+SC3 of 6 LANDED (ZM-D-079/080/081).** SC1 static-model foundation (`ZM_StaticMesh` bone-free kit + `ZM_ValidateGenMeshStatic` + 30-row `ZM_BuildingData` roster + frozen `ZM_BuildingGen` seam). SC2 real parametric shell (jittered body box + roof-by-kind GABLE/HIP/FLAT + wall/roof UV islands). SC3 real facade (wall band palette+gym-theme-tint+window-grid+door + roof band, ALBEDO-domain only). **`ZM_HumanGen`/`ZM_CreatureGen`/`ZM_CreatureAnimGen` COMPLETE.** S1/S2/S3 COMPLETE.
**Build:** GREEN -- Vulkan_vs2022_Debug_Win64_True (SC2/SC3 are pure all-config; SC1's Debug_False non-tools link proof still holds -- SC2/SC3 added no tools-gated code). The full five-config matrix re-runs at the SC5 disk-bake commit (adds `ZM_GenBakeStaticMesh` + tools bake bodies) and the S4 gate.
**Tests:** boot unit gate **1895 ran / 1894 passed / 0 failed / 1 skipped** (was 1893; +2 `ZM_Gen` BuildingGen SC3 units -- FacadeStructural + FacadeDomainIsolation); `zenith test Zenithmon --headless` **7 passed / 0 failed**. The canonical boot-unit helper reached its known 300 s watchdog only after the successful unit tally. `.github/workflows/zm-tests.yml` baseline = **1895**.

## Current task

**`ZM_BuildingGen`/`ZM_PropGen` SC4: PropGen seam + roster + prop prims.** Author `ZM_PropData` (Source/Data/, mirroring `ZM_HumanData`/`ZM_BuildingData`: save-stable append-only `ZM_PROP_ID` enum + compiled const C-array + accessors) covering ~25 props (fences, signs, lamps, bridges, ledge lips, cave rocks, interior furniture, + the ~6 battle-dome biome dressing sets grouped by a biome axis) + the frozen `ZM_PropGen` seam (`uZM_PROPGEN_VERSION`=1) + a pure driver emitting simple STATIC bone-free prims via the shared `ZM_StaticMesh` kit (`AppendBox` + a cylinder/panel prim if needed) + a per-palette/simple prop texture + FNV hash/equality + `game:Props/<Name>/` path scheme + tools bake STUBS returning false (real bake = SC5). New `Tests/ZM_Tests_PropGen.cpp` with `PropGen_RosterTotality`/`RecipePurity`/`AssetPathScheme`/`BuildDeterminism`/`StaticMeshContract`/`BiomeDressingCoverage`. NEW FILES -> `zenith regen`. Pure/all-config; no disk bake yet. Gate: regen + Debug True build + boot unit gate + headless. This is PARALLELIZABLE with the (done) building track -- disjoint files. `uZM_GENCOMMON_VERSION` stays 1 (reuse the static kit; add a cylinder emitter only if a prop needs it, additive).

## Last completed

**`ZM_BuildingGen` SC3 -- facade window/door decals (ZM-D-081).** Rewrote `ZM_BuildBuildingFacade`: wall band (image V in [0,0.72], ground->V0/eave->V0.72) = per-palette base + gym theme tint (`ZM_SynthTypePalette(theme).m_xBase` blended 35%) + `cols x rows` window grid + door; roof band (V>=0.75) = per-palette roof colour. ALBEDO-domain ONLY (4 fixed jitter draws; MESH-seed change can't perturb it). Reuses existing `ZM_SynthFillSolid`/`ZM_SynthStampRectDecal`/`ZM_SynthTypePalette` -- no new synth, all 3 version constants unchanged. RESOLVES the SC1 same-palette byte-collision NIT (gyms via theme tint, non-gyms via name-seed ALBEDO jitter). +2 units. Plan -> implement -> Debug True gate -> adversarial reviewer (CLEAN; full mesh->image V-orientation derivation, windows/door provably in the wall band).

## Notes for next agent

- **Active S4 item = `ZM_BuildingGen`/`ZM_PropGen` (SC1+SC2+SC3 done; SC4-SC6 remain).** `ZM_HumanGen`/`ZM_CreatureGen`/`ZM_CreatureAnimGen` COMPLETE -- do NOT re-touch. After BuildingGen/PropGen -> `ZM_BakeManifest` closes S4 -> S4 gate (4-config matrix + windowed gallery + **visual sign-off** = the next VISUAL GATE).
- **SC breakdown (ZM-D-079/080/081):** SC1 foundation (DONE); SC2 parametric shell (DONE); SC3 facade decals (DONE); SC4 PropGen seam+roster+prims (THIS TASK -- disjoint files from the building track, so it was always parallelizable); SC5 tools disk bake for BOTH families -- adds a NEW additive tools-only `ZM_GenBakeStaticMesh` bridge (skeleton-less `.zmesh`+`.zmtrl`+skeleton-less `.zmodel`; `HasSkeleton()`==false engine static path; `uZM_GENCOMMON_VERSION` stays 1) + bake smokes asserting the reloaded `.zmodel` has NO skeleton + 0 anims; SC6 coverage/totality + `AssetManifest` s3 + `TestPlan` reconcile = ticks the Roadmap box.
- **Static-mesh kit (reuse for props):** `ZM_StaticMesh::AppendBox`/`AppendGableRoof`/`AppendHipRoof`/`AppendFlatRoof` (bone-free) + `ZM_PushStaticTri` for any triangle (auto-orients outward via an interior reference point). Facade synth: reuse `ZM_SynthFillSolid`/`ZM_SynthStampRectDecal`/`ZM_SynthTypePalette` (normalized [0,1] UV; image V=0=row0). NEVER the skinned loft/validator. Winding = `cross(C-A,B-A).normal>0`, hard per-face normals (only `ZM_GenGenerateTangents`, never `ZM_GenGenerateNormals`). UV islands MUST be sub-rects of [0,1]. `ZM_GEN_DOMAIN` PINNED -- reuse MESH+ALBEDO. Draw ALL RNG up-front in a FIXED order from ONE domain (catch/flee gotcha).
- **SC1 reviewer NIT: RESOLVED by SC3** (distinct-palette facades now diverge). If PropGen has an analogous risk (props with identical dims+palette), diverge them via a name-seed ALBEDO jitter the same way.
- **Doc cadence (matches humans):** CI baseline (`zm-tests.yml`) bumps EVERY SC; `TestPlan`/`AssetManifest` prose reconcile at item completion (SC6). `TestPlan`'s stated baseline lags at 1886 -- reconcile to the final count + add the full BuildingGen/PropGen `ZM_Gen` sections at SC6.
- **Gate ORDER + baseline (unchanged):** `zenith test Zenithmon --headless` FIRST (heals DLLs, else `0xC0000135`); boot unit gate hangs after the units line -> `run_unit_gate.ps1 -Baseline N -TimeoutSec 300` times out+kills but captures `Unit tests complete: N ran`. New files need `pwsh -NoProfile -File Build\regen.ps1`. Non-default configs via msbuild ABSOLUTE sln path (`C:\Program Files\Microsoft Visual Studio\18\Insiders\MSBuild\Current\Bin\MSBuild.exe`), serial only. `git -C C:/dev/Zenith ...`.
- **Working model:** MASTER-ONLY (ZM-D-031); local gate is the authority; `zm-tests` is the post-push backstop, fix forward on red. Sweep stray `zenithmon.exe` before ending. Orchestrator+subagents this session (Plan -> Implement -> Reviewer per SC); only the orchestrator builds/tests/commits. A stray harness worktree exists at `.claude/worktrees/` -- NEVER regen/build/commit from it (Invariant 2).
