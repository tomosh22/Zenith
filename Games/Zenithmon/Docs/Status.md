# Zenithmon Status

**Last updated:** 2026-07-18
**Stage:** **S5 (Battle integration slice) IN PROGRESS.** S0-S4 COMPLETE. **S5 items 1+2+3+4 DONE; item 5 IN PROGRESS.** Item 4 closed via SC1-SC6 (ZM-D-100..105). **Item 5 SC1 (ZM-D-106) pure Party model; SC2 (ZM-D-107) `ZM_GameStateManager`; SC3 (ZM-D-108) EXP write-back; SC4 (ZM-D-109) CATCH -- ALL DONE.** SC4: a Catch battle-menu action (root now Fight/Catch/Run) throws a ball; a successful catch adds the wild monster to the party + marks the dex (winner==PLAYER on a catch, so the SC3 lead write-back carries HP with no exp double-fire). **NEXT is item 5 SC5** -- loss -> WHITEOUT (heal the party full + warp to Dawnmere "TownCenter"; also address the SC3/SC4 forward notes: HP persist through flee, fainted-lead guard). Then SC6 (windowed gate), then the **S5 GATE = the next VISUAL hard-stop** (user sign-off; AFTER item 5).
**Build:** GREEN -- Zenithmon Vulkan Debug True clean. Master clean + pushed at the item-5 SC4 commit (see git log).
**Tests:** boot unit gate **2019 ran / 2018 passed / 0 failed / 1 skipped** (item-5 SC1 +14, SC3 +5, SC4 +5 pure units; SC2 windowed-only; `zm-tests.yml` **2014 -> 2019**; engine default **1088** UNCHANGED). The ~380 `ZM_Battle` goldens stay BYTE-IDENTICAL. `zenith test Zenithmon --headless` **21/0**. Windowed: the battle suite **9/0** (`ZM_BattleMenuWin_Test` asserts exp rose; `ZM_BattleMenuCatch_Test` asserts the party grew 1->2 + the species is caught) + `ZM_GameStatePersistence_Test` **1/0**.

## Current task

**S5 item 5 SC5 -- loss -> WHITEOUT (the last item-5 gameplay effect).** On a LOSS (battle winner == `ZM_SIDE_ENEMY`, i.e. the whole single-lead party fainted), the director sets a whiteout-pending flag on the GameState; the persistent `ZM_GameStateManager` (or a small handler) detects it once the overworld is back, HEALS the party full (`ZM_Party::HealAllFull`), clears the flag, and warps the player to Dawnmere ("TownCenter" spawn tag) via the existing warp seam. **Also fold in the SC3/SC4 reviewer FORWARD NOTES that live in the loss/whiteout path:** (a) make the lead's damaged HP PERSIST THROUGH A FLEE (SC3 win-only write-back discards it -- "flee = free heal"); (b) add a FAINTED-LEAD GUARD (a record at 0 HP currently battles at 1 HP via the clamp). GATE PROOF: a windowed test loses a battle (rig a strong enemy / a 1-HP fainted-ish lead) -> asserts whiteout warp to Dawnmere + party healed + exact state. Consult the durable plan's SC5 section. Confirm the warp seam (`ZM_GameStateManager::RequestWarp`?/`ZM_WarpTrigger` path), the loss detection (winner==ENEMY at the RESOLVED edge), and `HealAllFull` against the code before authoring.

**DECOMPOSED (2026-07-18):** durable plan on disk at `Build/artifacts/zm_s5_item5_plan/plan.md`. Item 5 is almost pure WIRING -- ZERO engine changes. **6 sub-commits:** **SC1 DONE (ZM-D-106)** pure `Source/Party/` model (+14 units); **SC2 DONE (ZM-D-107)** `ZM_GameStateManager` owns the in-memory `ZM_GameState` + static `TryGetGameState` + between-tests reseed; **SC3 DONE (ZM-D-108)** exp write-back + `m_uCurHP` carry + gapped-moveset fix; **SC4 DONE (ZM-D-109)** catch: Fight/Catch/Run menu, `m_bCanCatch=true`, catch write-back adds to party + dex, PRIMEORB test seam; **SC5** loss->whiteout (heal + warp to Dawnmere "TownCenter"; + flee-HP-persist + fainted-lead guard) (gate: faint->whiteout); **SC6** consolidated windowed `--filter ZM_Battle` gate + bleed-through screenshot (Q-2026-07-09-003) + docs -> sets the S5 VISUAL GATE-WAIT.

**Scope rulings logged as Q-2026-07-18-001** (best-guesses, loop PROCEEDS on them): single-LEAD battle (defer switch to S6+), fixed starter (Fernfawn L5), GameState IN-MEMORY only (no save until S7), direct Catch button (no Bag UI until S6), whiteout->Dawnmere. **NEXT SC = SC5** (whiteout, the LAST item-5 SC before SC6's gate). Surface for SC5-SC6: `ZM_GameStateManager::TryGetGameState(ZM_GameState*&)` + its `DontDestroyOnLoad` warp seam (find how S3 warps -- `ZM_WarpTrigger`/`RequestWarp`/spawn-tag "TownCenter", build index 2 = Dawnmere); `ZM_GameState::m_bPendingWhiteout`; `ZM_Party::AllFainted()/HealAllFull()`; `Source/Party/ZM_BattleWriteBack.h::ZM_ApplyBattleResultToParty` (SC5 handles the winner==ENEMY loss path -- set `m_bPendingWhiteout`; and fixes the flee-HP-persist + fainted-lead notes below).

**FORWARD NOTES (from the SC2/SC3/SC4 reviewers -- NOT bugs today, act on them at the noted SC):** **[SC5]** (1) **"flee = free heal"** -- SC3 carries the lead's damaged HP INTO battle but writes it back OUT only on a WIN, so a successful flee discards the in-battle HP damage; SC5 should make HP persist through a flee even though exp does not. (2) A **fainted lead (record HP 0) would battle at 1 HP** (the `[1,maxHP]` clamp) with no guard in `RunSetup`; SC5's whiteout path should add a fainted-lead guard. **[S6]** (3) **The Catch menu item is shown/selectable UNCONDITIONALLY, ignoring `m_bCanCatch`** (SC4 deliberately deferred the flag-threading). Inert at S5 (all battles wild, `m_bCanCatch=true`), but when TRAINER battles arrive (S6+, `m_bCanCatch=false`) selecting Catch trips the engine `SubmitAction` `m_bCanCatch` assert (debug) or rolls an illegal trainer-catch (no-assert build) -- S6 must gate the Catch item on a core-surfaced `m_bCanCatch`, and record it in Shortfalls.md at the SC6 gate. **[misc]** the SC2 nit (`ResetGameStateForTests` could resolve via `s_xSingletonEntityID` for symmetry); the SC4 `CATCH_RESULT` scan's broke-free discrimination is only end-to-end-covered by the guaranteed-catch happy path.

## Last completed

**S5 item 5 SC4 (ZM-D-109) -- catch (add a caught wild monster to the party + dex).** The battle action root menu grows `Fight(0)/Catch(1)/Run(2)`; a Catch confirm submits `{ZM_ACTION_ITEM, ZM_ITEM_CATCHORB}`; `BuildBattleConfig()` sets `m_bCanCatch=true` (director-only config -> zero golden perturbation). A SUCCESSFUL catch sets winner==PLAYER, so the SC3 lead write-back carries the lead's HP (no exp double-fire -- engine awards none on a catch turn); the extended `ZM_ApplyBattleResultToParty` scans for `CATCH_RESULT(m_iAmount==1)` and calls the new pure `ZM_ApplyCatchToGameState` (MarkCaught always + `Add(ZM_MonsterFromBattleMonster(enemy))` if `!IsFull`; full-party marks-but-drops, boxes=S7). Windowed determinism via a `ZM_SetCatchBallForTests(ZM_ITEM_PRIMEORB)` seam (guaranteed capture, zero RNG). +5 T0 units + 3 updated menu-count units + windowed `ZM_BattleMenuCatch_Test` (party 1->2 + Kindlet caught). Focused reviewer NO BLOCKERS (1 S6 forward-note: Catch item unconditional vs trainer configs -- see FORWARD NOTES). zm-tests 2014->2019. Prior: SC3 (ZM-D-108) exp write-back; SC2 (ZM-D-107) GameState manager; SC1 (ZM-D-106) Party model; item 4 COMPLETE (ZM-D-100..105).

## Notes for next agent (S5 item 5)

- **Item 4 is CLOSED (all 6 SCs, ZM-D-100..105).** The windowed suite proves player-driven win (`ZM_BattleMenuWin_Test`) + flee (`ZM_BattleMenuRun_Test`) + presentation/resume (`ZM_BattleHUD_Test`, `ZM_BattleDirectorRoundTrip_Test`). Item 5 ("Catch / exp / faint / whiteout applied to GameState") is the LAST S5 item; the S5 STAGE gate (visual hard-stop, user sign-off) is AFTER it.
- **Item 5 is where PERSISTENT GameState first appears.** Item 4 deliberately used a deterministic placeholder player (`ZM_BattleDirector::BuildPlaceholderPlayerSpec`, discarded on unload) with exp OFF + no catch. Item 5 wires a real party/GameState, flips `m_bAwardExp`/`m_bCanCatch` (the EXP/LEVEL/CATCH events are already mapped -> minimal `ZM_BattleDirectorCore` change), and handles faint->whiteout. Plan it first (it touches SaveData/party persistence). No `ZM_GameState`/`ZM_Party` exists yet (verified at SC3).
- **Item-5 FORWARD NOTES from the SC5 reviewer (act on these WHEN item 5 lands real party/moves -- they are NOT bugs today):**
  1. **The battle menu couples cursor-index to raw move-SLOT-index** (`ZM_UI_BattleHUD.cpp` menu refresh + `MenuConfirm` submits `m_uMoveSlot = cursor`). Correct only while movesets pack contiguously from slot 0 (the placeholder player always does). Item 5's real party members can have GAPPED movesets -> switch the menu to a FILLED-SLOT index table (map cursor -> the Nth non-empty slot) before that lands, or a gapped active shows a blank unselectable button and the real move is unreachable.
  2. **All-0-PP active -> Fight submenu is a silent dead-end** (no Struggle, no feedback). Not a softlock (Run + 30 s deadline). Struggle is the pre-existing engine gap Q-2026-07-12-003; add it (engine) if item 5+ needs it.
  3. **The two H0-edited windowed tests prove resolution INDIRECTLY** (resume deadline < softlock). Consider adding a direct `GetCore().IsOver()` latch+assert to `ZM_BattleDirectorRoundTrip_Test`/`ZM_BattleHUD_Test` for robustness (the new menu tests already assert `GetWinner()`).
- **The SC3 `RequestBattleEnd`-return nit is DONE** (both sites branch on the bool now).
- **HUD/menu seam is Option B:** `ZM_UI_BattleHUD` owned by value in the director (`m_xHud`), NOT an ECS component. Re-resolve UI elements each frame (never cache). Keep the director the single `ZM_BattleDirectorCore` owner. Next-free ECS order is **112** (SC4/SC5 added NO component).
- **Gate ORDER + baselines (permanent):** `zenith test Zenithmon --headless` FIRST (heals DLLs). Boot unit gate hangs after the units line -> `pwsh -NoProfile -File Tools\run_unit_gate.ps1 -Exe <exe> -Baseline 1995 -TimeoutSec 300` (the 300s timeout-kill is EXPECTED). Windowed: `--filter ZM_Battle` runs all 8 battle windowed tests in one boot. New files -> `Build\regen.ps1`. ENGINE-unit changes bump BOTH `zm-tests.yml` AND `run_unit_gate.ps1`'s default (1088) + need Combat/RenderTest/DP/CityBuilder regression (RenderTest pre-existingly red -- Q-2026-07-16-001). Diagnosing a windowed test: the game's `Zenith_Log(LOG_CATEGORY_UNITTEST)` diagnostics are NOT in the harness stdout -- run the exe directly `<exe> --automated-test <Name> --exit-after-frames N` to see them.
- **Working model:** MASTER-ONLY (ZM-D-031); LOCAL gate is authority; `zm-tests` post-push backstop. Orchestrated: only the orchestrator builds/tests/commits; subagents author, never build. Sweep stray `zenithmon.exe` before ending. NEVER commit baked assets or captures.
- **Open Questions:** Q-2026-07-17-002 (S5 item-4 scoping -- SC3/SC4/SC5 acted on best-guesses (a) placeholder player team, (b) exp OFF, (c) Fight+Run only, (d) GREEDY wild AI); Q-2026-07-17-001 (`BiomeForScene` hard-coded table -- WorldSpec column at S9/S10); Q-2026-07-16-001 (RenderTest terrain); Q-2026-07-09-003 (S5 bleed-through -- verified/falsified only at the S5 SCREENSHOT gate).
- **The S5 gate is the next VISUAL hard-stop** (all 5 items: windowed encounter round-trip + catch + exp + exact resume + the bleed-through screenshot). Do not tick it or start S6 without the user's sign-off.
