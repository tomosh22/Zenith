# Zenithmon Status

**Last updated:** 2026-07-17
**Stage:** **S5 (Battle integration slice) IN PROGRESS.** S0-S4 COMPLETE. **S5 items 1+2+3 DONE.** Item 3 closed with SC5 (ZM-D-099), the windowed round-trip gate. **NEXT is S5 item 4** (`ZM_BattleDirector` + `ZM_UI_BattleHUD` + engine E3 typewriter). Then item 5, then the **S5 GATE = the next VISUAL hard-stop** (needs the user's sign-off).
**Build:** GREEN -- Vulkan Debug True clean. Clean tree at the SC5 commit (see git log).
**Tests:** boot unit gate **1946 ran / 1945 passed / 0 failed / 1 skipped** (SC5 = +0 units, windowed-only; baseline UNCHANGED; `zm-tests.yml` stays 1946). `zenith test Zenithmon --headless` **15/0**. Windowed all PASS with the machine LIVE: **`ZM_BattleRoundTrip_Test` (146 frames, the item-3 gate)**, `ZM_BattleEncounterLatch_Test` (207), `ZM_BattleArena_Test` (31), `ZM_BattleArenaOwnScene_Test` (2). Engine default 1081 (`Tools/run_unit_gate.ps1`) UNCHANGED -- **item 3 touched NO engine code, and SC5 touched NO product code (test-only)**.

## Current task

**S5 item 4 -- `ZM_BattleDirector` (event-stream interpreter, `zm_instant_battles` DebugVariable) + `ZM_UI_BattleHUD` + engine E3 typewriter** (Roadmap.md:86). This is the FIRST piece of visible battle UI. It owns battle resolution and, crucially, is the CALLER of `ZM_BattleTransition::RequestBattleEnd()` -- item 3 shipped that seam but nothing calls it in play yet.

## Last completed

**S5 item 3 SC5 (ZM-D-099) -- the windowed round-trip gate; item 3 COMPLETE.** `ZM_BattleRoundTrip_Test` (in `Tests/ZM_AutoTests_BattleTransition.cpp`, `m_bRequiresGraphics`, maxFrames 2200, fixed dt 1/30) drives the WHOLE slice once through the shipped machinery: walk Dawnmere grass -> forced FERNFAWN L5 encounter -> assert opaque-fade-gated additive load -> in battle: active build index 1, battle camera live, overworld paused, grass 0, arena `IsFullyBuilt()` with all 9 children in the Battle scene, biome MEADOW, 1 issued load -> `RequestBattleEnd()` -> EXACT resume: build index 2, unpaused, Battle unloaded, 0 arenas, movement re-enabled, 0 aborts, grass restored, parked-body drift < 0.05 m. Test-only, zero product churn. Earlier item-3 SC chain: SC4 (ZM-D-098) the 9-state machine; SC3b (ZM-D-097) scene-inert component + latch; SC3a (ZM-D-096) shared `ZM_FadeOverlay::Apply` + `IsWarpInProgress` rename; SC2 (ZM-D-095) `RegenerateForSceneResume()`; SC1 (ZM-D-094) own-scene arena build.

## Notes for next agent (S5 item 4)

- **`ZM_BattleTransition::RequestBattleEnd()` is item 4's seam AND the SOLE exit from `ZM_BATTLE_TRANSITION_IN_BATTLE`.** There is NO timer. `GetBattleSpecies()/GetBattleLevel()/GetSourceScene()/GetBattleScene()` carry the latched encounter payload item 4 reads to build the battle. Item 4 must call `RequestBattleEnd()` exactly once when its battle resolves.
- **The encounter path is NOT live in play yet, BY DESIGN.** `ZM_TallGrassSystem` is registered (`Zenithmon.cpp:139`) but authored into NO scene (zero `AddStep_AddComponent` hits), and Dawnmere carries `nullptr, 0, 0` encounters (`ZM_WorldSpec.cpp:52`); Route1 (the sole encounter-bearing row) is not a registered build index. **Authoring `ZM_TallGrassSystem` into any playable scene is BLOCKED until item 4 lands `RequestBattleEnd`'s caller** -- do it before, and a player who walks into grass has no way out of the arena (softlock). The gate tests FORCE the encounter via `ForceEncounterOnNextTransitionForTests`, so they are unaffected.
- **`ZM_UI_BattleHUD` must use a sort order ABOVE 10001** or the `BattleFade` overlay swallows it (the fade's full-canvas text clip hides every lower-order UIText). WarpFade = 10000, BattleFade = 10001 (ZM-D-097).
- **Placing combatants:** `ZM_BattleArena::GetChildEntityID(u)` / `uCHILD_COUNT`(9) enumerate the arena's root children (0 = dome, 1..2 = platforms [player, enemy], 3..8 = per-biome dressing sets in `ZM_BATTLE_BIOME` order). The children are NOT parented -- they are ROOT entities at absolute world coords around Y = -2000.
- **DO NOT touch the parked overworld player.** It is parked with velocity zeroed + gravity factor 0 for the whole battle (`GetParkedPlayerPosition()` is the drift baseline the gate holds to < 0.05 m). Item 4 operates entirely in the Battle scene.
- **`ZM_GameStateManager::ShouldFreezePlayerOnStart` was RENAMED to `IsWarpInProgress` in SC3** (all callers migrated; old name deleted).
- **Next-free ECS order is 111** (110 = `ZM_BattleTransition`; 108 = `ZM_BattleArena`, 109 = `ZM_TallGrassSystem`).
- **Gate ORDER + baselines (permanent):** `zenith test Zenithmon --headless` FIRST (heals DLLs). Boot unit gate hangs after the units line -> `run_unit_gate.ps1 -Exe <exe> -Baseline N -TimeoutSec 300` (now **1946**; the 300s timeout-kill is EXPECTED, the units line is already logged). Windowed: `--filter <Test>`. New files -> `Build\regen.ps1`. ENGINE-unit changes bump BOTH `zm-tests.yml` AND `Tools\run_unit_gate.ps1`'s default (1081) + need Combat/RenderTest/DP/CityBuilder regression (RenderTest pre-existingly red -- Q-2026-07-16-001).
- **Working model:** MASTER-ONLY (ZM-D-031); LOCAL gate is authority; `zm-tests` post-push backstop. Orchestrated: only the orchestrator builds/tests/commits; subagents author, never build. Sweep stray `zenithmon.exe` before ending. NEVER commit baked assets or captures.
- **Open Questions:** NEW Q-2026-07-17-001 (`BiomeForScene` is a hard-coded table, not a `ZM_WorldSpec` column -- propose a WorldSpec biome column at S9/S10); Q-2026-07-16-001 (RenderTest terrain); S2-era `Q-2026-07-12-*`; Q-2026-07-09-003 (S5 bleed-through -- item 3 now clears overworld grass on battle entry, strictly reducing the surface, but it is verified/falsified only at the S5 SCREENSHOT gate).
- **The S5 gate is the next VISUAL hard-stop** (all 5 items: windowed encounter round-trip + catch + exp + exact resume + the bleed-through screenshot). Do not tick it or start S6 without the user's sign-off.
