# Zenithmon Status

**Last updated:** 2026-07-14
**Stage:** S4 (Asset generators) -- in progress. **`ZM_CreatureAnimGen` SC4 of 6 LANDED (ZM-D-071):** INSECTOID (19-bone metachronal tripod) + BLOB (4-bone, sparsest) clip builders added + wired. **7 of 8 archetypes now animate** (all but FloaterPlantoid). Clips are 6 per archetype (Idle/Walk/Attack/Special/Hit/Faint), pure `f(archetype,clip-id)`, rotation-only, byte-identical across every species of an archetype; reuses the engine headerless `Flux_AnimationClip::Export` (no engine change). `ZM_CreatureGen` COMPLETE + gate-signed (ZM-D-067). S1/S2/S3 COMPLETE.
**Build:** GREEN -- Vulkan_vs2022_Debug_Win64_True. (Cross-config purity of the anim library proven at SC1 across Debug False + the D3D12 Debug False null-backend link proof; the full matrix re-runs at the SC6 gate that edits `ZM_CreatureGen.*`.)
**Tests:** boot unit gate **1863 ran / 1862 passed / 0 failed / 1 skipped** (was 1861; +2 new `ZM_Gen` per-archetype units); `zenith test Zenithmon --headless` **7 passed / 0 failed**. `.github/workflows/zm-tests.yml` baseline bumped 1861 -> **1863** (same commit).

## Current task

**`ZM_CreatureAnimGen` SC5: FloaterPlantoid clip builder -- all 8 wired + totality gate.** Add `Source/Gen/ZM_CreatureAnimArchetype_FloaterPlantoid.cpp` (~10 bones; its crown bone is literally named `Head`; expect `Tendril0-5` -- a floating plant/jellyfish: rotation-only bob expressed as spine/crown flex + tendril paddle/wave, NO legs) and wire it into `ZM_GetArchetypeAnimBuilder` so the dispatch is TOTAL (every archetype returns a non-null builder). FIRST read `Source/Gen/ZM_CreatureArchetype_FloaterPlantoid.cpp` for the EXACT frozen bone names. Add `Tests/ZM_Tests_CreatureAnimArchetype_FloaterPlantoid.cpp` (structural lock) AND add a new totality test to `Tests/ZM_Tests_CreatureAnimGen.cpp` -- `CreatureAnimGen_AllArchetypesHaveAnimBuilder`: `ZM_GetArchetypeAnimBuilder(a) != nullptr` for every `a` in `[0, ZM_ARCHETYPE_COUNT)` (fn-ptr compared with `!= nullptr`, NOT ZENITH_ASSERT_NULL). This is the twin of `ZM_CreatureGen`'s `AllSpeciesBuildable`/`ArchetypeDispatch` gate. Also update the dispatch block comment + the header's FloaterPlantoid `// SC5` decl comment. Still NO disk bake / NO `ZM_CreatureGen.*` edit (that is SC6). Rebuild, boot unit gate at the new measured count, headless, commit + push. After SC5, EVERY buildable species yields 6 valid rotation-only clips headless -- then SC6 wires the disk bundle bake (ticks the Roadmap box) + SC7 optional windowed gate.

## Last completed

**`ZM_CreatureAnimGen` SC4 (ZM-D-071):** INSECTOID builder (19-bone: Spine00-03, Head, Leg{L,R}{0,1,2}{Up,Lo}, AntennaL, AntennaR -- metachronal tripod gait, phases {0,.5,0,.5,0,.5} over L0,L1,L2,R0,R1,R2) + BLOB builder (4-bone: Spine00-02, Nub -- rotation-only spine-flexion, 6 clips engineered pairwise-distinct despite only 4 bones), wired; `Tests/ZM_Tests_CreatureAnimArchetype_Insectoid.cpp` + `_Blob.cpp` structural locks; refreshed stale dispatch/decl comments. Committed + pushed to master.

## Notes for next agent

- **The per-archetype authoring pattern is proven 4x (SC1-SC4).** Per archetype: read the archetype's `ZM_CreatureArchetype_<Name>.cpp` for its EXACT frozen bone names, author 6 rotation-only clips against those names (only key bones that exist), wire `ZM_GetArchetypeAnimBuilder`, add a per-archetype structural test (positive + negative bone-signature locks). The generic `ZM_Tests_CreatureAnimGen.cpp` harness is `HasAnimBuilder`-gated + archetype-generalized, so it auto-covers new archetypes -- do NOT edit it per archetype (EXCEPT SC5 adds the one totality test).
- **Per-archetype bone counts** (confirm exact NAMES from the code): QUADRUPED 18, BIPED 14, AVIAN 13, SERPENT 12, AQUATIC 8, INSECTOID 19, BLOB 4 (all DONE); FLOATER-PLANTOID 10 (remaining -- crown bone named "Head", expect Tendril0-5).
- **Rotation-only is load-bearing** -- channels REPLACE bind-local TRS; bind-local rotation is identity + scale unit for ALL species so absolute rotation is species-safe, but position varies per species so NO position/scale channels. Express bob/hop/collapse via spine/crown-flexion rotation; travelling waves = `ZM_AnimAddRotCurve` per bone with a per-bone phase offset inside a full-2pi sine (stays loop-closed).
- **Gotchas (paid for once):** quats glm WXYZ (`glm::angleAxis(glm::radians(deg),axis)`); `SetDuration` SECONDS but keyframe times TICKS (tick = t01*durSec*24, use `ZM_AnimTicksForT01`); looping clips key0==keyN per channel; one-shots end ~identity (Faint holds collapse); byte-equality uses `Zenith_DataStream::GetCursor()`; fn-ptr nullness via `!= nullptr`. Watch clip DISTINCTNESS for sparse skeletons (the harness asserts Idle!=Walk, Attack!=Special, Attack!=Hit, Hit!=Faint by content hash) -- vary keyed nodes/axis/frequency/key-count/end-pose.
- **SC6 (the box-ticking commit) touches `ZM_CreatureGen.*`:** append 6 ANIM values to `ZM_CREATURE_ASSET_KIND` + 6 `ZM_CreatureBasenameFmt` cases (`%s_Idle.zanim`..`%s_Faint.zanim`); implement `ZM_BakeCreatureClips` (`#ifdef ZENITH_TOOLS`); call it inside `ZM_BakeCreature` AFTER the texture bakes (Export creates no dirs); `AddAnimationPath` x6 in BOTH `.zmodel` blocks; bump `uZM_CREATUREGEN_VERSION` 2->3. The enum extension + the writes MUST land together (existing `CreatureBake_BundleFilesLand` loop auto-covers the new kinds). Tools bake smoke = ONE species (Fernfawn) x 6 clips. SC6 also updates TestPlan.md + AssetManifest.md and re-runs the full 4-config + D3D12 matrix (it edits `ZM_CreatureGen.*`).
- **Gate ORDER + baseline gotchas (unchanged):** `Tools\run_unit_gate.ps1` boots the exe DIRECTLY -- run `zenith test Zenithmon --headless` FIRST to heal DLLs, else `0xC0000135`. The exe hangs after the units line so the gate times out + kills but captures `Unit tests complete: N ran`; pass the CURRENT baseline + `-TimeoutSec 300`. Bump the count in BOTH `.github/workflows/zm-tests.yml` AND Status.md in the SAME commit. Build non-default configs via msbuild with the ABSOLUTE sln path. `git -C C:/dev/Zenith ...` in Bash calls.
- **Working model:** MASTER-ONLY (ZM-D-031); local gate is the authority; `zm-tests` is the post-push backstop, fix forward on red. Sweep stray `zenithmon.exe` before ending each iteration. The remaining S4 items have NO visual gate until the S8 vertical-slice go/no-go.
