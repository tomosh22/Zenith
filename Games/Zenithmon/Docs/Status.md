# Zenithmon Status

**Last updated:** 2026-07-14
**Stage:** S4 (Asset generators) -- in progress. **`ZM_CreatureAnimGen` ROADMAP ITEM COMPLETE (SC1-SC6, ZM-D-068..073).** All 8 archetypes author 6 pure rotation-only clips (Idle/Walk/Attack/Special/Hit/Faint) byte-identical across species; SC6 wired the disk bundle bake so every one of the 152 species bakes its 6 `.zanim` (912 total) into its bundle (now 15 files) and each `.zmodel` self-lists its clips via `AddAnimationPath`. `uZM_CREATUREGEN_VERSION` 2->3. `ZM_CreatureGen` COMPLETE + gate-signed (ZM-D-067). S1/S2/S3 COMPLETE.
**Build:** GREEN across the full matrix -- Vulkan_vs2022_{Debug,Release}_Win64_{True,False} + the D3D12_vs2022_Debug_Win64_False null-backend link proof (all built at the SC6 gate).
**Tests:** boot unit gate **1866 ran / 1865 passed / 0 failed / 1 skipped** (ZM_CreatureAnimGen added +19 over the pre-anim 1847: 10 generic + 8 per-archetype + 1 tools bake smoke); `zenith test Zenithmon --headless` **7 passed / 0 failed**. `.github/workflows/zm-tests.yml` baseline = **1866**.

## Current task

**`ZM_CreatureAnimGen` SC7 (OPTIONAL windowed playability gate) -- then `ZM_HumanGen`.** The Roadmap `ZM_CreatureAnimGen` box is already ticked (SC6); SC7 is a strengthening pass that proves a BAKED creature actually animates in the scene loader (the one thing the bind-pose gallery + headless units cannot show). Author `Tests/ZM_AutoTests_CreatureAnim.cpp`: a `Zenith_AutomatedTestRunner` windowed test (`m_bRequiresGraphics = true` so it auto-skips headless; `RequestSkip` -- NOT ZENITH_SKIP -- when `Assets/Creatures/<Name>` is absent). Bake a couple species, add a `Zenith_AnimatorComponent`, `AddClipFromFile` the baked Idle `.zanim`, advance a few frames, and assert the `Flux_SkeletonInstance` bone transforms DEVIATE from the `.zskel` bind pose. It is NOT a boot unit (does not move the -Baseline). If SC7 is judged unnecessary, skip straight to the next unchecked S4 Roadmap item: **`ZM_HumanGen`** (one shared generalized-StickFigure skeleton + one shared 9-clip set; ~35 human `.zmodel` all binding the same `.zskel` + reusing the same `.zanim` -- see AssetManifest section 2; NO per-model clips). Either way: subagent-authored, orchestrator builds/gates serially, reviewer, commit + push to master.

## Last completed

**`ZM_CreatureAnimGen` SC6 (ZM-D-073):** disk bundle-bake wiring -- 6 anim asset kinds appended to `ZM_CREATURE_ASSET_KIND` (9->15, IDLE..FAINT), 6 `ZM_CreatureBasenameFmt` cases, `uZM_CREATUREGEN_VERSION` 2->3; real `ZM_BakeCreatureClips`/`ZM_BakeAllCreatureClips` (build clip -> `ZM_CreatureAssetPath` + `Zenith_AssetRegistry::ResolvePath` -> `Export` -> `exists`); `ZM_BakeCreature` calls it after the texture bakes + `AddAnimationPath` x6 in both `.zmodel` blocks; new tools-only bake smoke `CreatureAnimBake_ClipsLandAndModelReferences`. Full 5-config matrix green; boot 1866/0-fail; TestPlan.md + AssetManifest.md refreshed. Committed + pushed to master.

## Notes for next agent

- **`ZM_CreatureAnimGen` is DONE** (SC1-SC6). Do NOT re-touch the anim builders or the bake seam unless fixing a bug. The clip<->kind<->filename contract is aligned: `ZM_ANIM_CLIP` IDLE..FAINT = `ZM_CreatureClipName` "Idle".."Faint" = basename `"%s_Idle.zanim".."%s_Faint.zanim"` = asset kinds `ZM_CREATURE_ASSET_ANIM_IDLE.._FAINT` (contiguous, `(ZM_CREATURE_ASSET_KIND)(ANIM_IDLE + eClip)`).
- **Version bump side effect:** `uZM_CREATUREGEN_VERSION` is now 3, so the first `*_True` boot after a clean checkout COLD-re-bakes all creature bundles (mesh+texture+6 clips x 152) -- expect a longer first tools boot; warm boots skip via the manifest.
- **ZM_HumanGen leverage (next item):** unlike creatures (per-archetype skeletons), humans share ONE generalized-StickFigure skeleton + ONE shared 9-clip set (AssetManifest section 2). So author the human skeleton + 9 clips ONCE and every one of the ~35 human `.zmodel` binds the same `.zskel` + reuses the same `.zanim` files (no per-model clips). Survey the StickFigure export tool (`Zenith/Tools/Zenith_Tools_TestAssetExport.cpp`) + the existing `ZM_CreatureAnimGen`/`ZM_CreatureGen` bake patterns before designing the `ZM_HumanGen` seam. Consider a small design workflow first (as was done for ZM_CreatureAnimGen) if the shape is unclear.
- **Gate ORDER + baseline gotchas (unchanged):** `Tools\run_unit_gate.ps1` boots the exe DIRECTLY -- run `zenith test Zenithmon --headless` FIRST to heal DLLs, else `0xC0000135`. Exe hangs after the units line -> gate times out+kills but captures `Unit tests complete: N ran`; pass the CURRENT baseline + `-TimeoutSec 300`. Bump the count in `.github/workflows/zm-tests.yml` + Status.md (+ TestPlan.md) together. Build non-default configs via msbuild with the ABSOLUTE sln path `C:\dev\Zenith\Games\Zenithmon\zenithmon_win64.sln`. `git -C C:/dev/Zenith ...` in Bash calls. Windowed playability/visual runs go through `zenith test Zenithmon --filter <Test>` (NO --headless). Never commit baked assets (git-ignored).
- **Working model:** MASTER-ONLY (ZM-D-031); local gate is the authority; `zm-tests` is the post-push backstop, fix forward on red. Sweep stray `zenithmon.exe` before ending each iteration. Remaining S4 items (`ZM_HumanGen`, `ZM_BuildingGen`/`ZM_PropGen`, `ZM_BakeManifest`) have NO visual gate until the S8 vertical-slice go/no-go.
