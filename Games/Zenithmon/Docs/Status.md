# Zenithmon Status

**Last updated:** 2026-07-18
**Stage:** **S5 (Battle integration slice) IN PROGRESS.** S0-S4 COMPLETE. **S5 items 1+2+3 DONE; item 4 IN PROGRESS -- SC1 (E3 typewriter, ZM-D-100) + SC2 (`ZM_BattleDirectorCore`, ZM-D-101) + SC3 (`ZM_BattleDirector` order 111, ZM-D-102) + SC4 (`ZM_UI_BattleHUD`, ZM-D-103) + SC5 (interactive Fight/Run menu, ZM-D-104) DONE.** **NEXT is item 4 SC6** -- the windowed win-gate sub-commit that TICKS the item-4 Roadmap box (a full player-driven encounter->battle->win/flee->resume proven windowed; largely a verification/gate SC over SC1-SC5). Then **item 5** (Catch / exp / faint / whiteout applied to GameState), then the **S5 GATE = the next VISUAL hard-stop** (needs the user's sign-off).
**Build:** GREEN -- Zenithmon Vulkan Debug True clean. Master clean + pushed at the SC5 commit (see git log).
**Tests:** boot unit gate **1995 ran / 1994 passed / 0 failed / 1 skipped** (SC5 = **+7 pure `ZM_BattleHUD` menu units**; game-only, so `zm-tests.yml` bumped **1988 -> 1995** and the engine default `run_unit_gate.ps1` **1088** is UNCHANGED). `zenith test Zenithmon --headless` **19/0**. Windowed **8/0, zero skips**: `ZM_BattleMenuWin_Test` + `ZM_BattleMenuRun_Test` (the two new player-driven tests) + `ZM_BattleHUD_Test` + `ZM_BattleDirectorRoundTrip_Test` (both H0-fixed with an ENTER drive) + the 4 arena/roundtrip/latch coexistence tests, all against real baked assets.

## Current task

**S5 item 4 SC6 -- the windowed win gate that ticks the item-4 box.** Item-4's plan (`Build/artifacts/zm_s5_item4_plan/plan.md`, SC6 section) closes item 4 with a windowed proof + the item-4 Roadmap tick. Most of the coverage already exists (`ZM_BattleMenuWin_Test`/`ZM_BattleMenuRun_Test` prove player-driven win + flee end-to-end; the HUD + director round-trip prove presentation + resume). SC6 should: confirm the item-4 acceptance criteria are met by the shipped windowed suite (add any missing windowed assertion the plan's SC6 lists), tick the item-4 Roadmap box, and hand off to item 5. If SC6 is purely a gate/doc tick with no new code, land it as a small `docs`/`test` commit.

**PLANNED:** durable item-4 plan on disk at `Build/artifacts/zm_s5_item4_plan/plan.md` (git-ignored; regenerate if absent) -- see its SC6 section for the exact item-4 acceptance list.

## Last completed

**S5 item 4 SC5 (ZM-D-104) -- interactive Fight/Run battle menu (first player-driven input).** `ZM_UI_BattleHUD` gained a pure menu state machine (`ACTION_ROOT{Fight,Run} -> MOVE_SELECT{0..3}`; static `MenuItemCount`/`MenuMoveCursor`/`MenuConfirm`/`MenuCancel` + instance `UpdateMenu`/`HideMenu`), 7 authored menu elements (sort 10003), and `ZM_InputActions::ReadMenuVertical`. `ZM_BattleDirector` drives it (submits the player's Fight-move or Run instead of hard-coded move-0), sets `m_bCanFlee=true`, and hardens both `RequestBattleEnd()` sites on the bool return. **Run routes through the engine** (`SubmitPlayerAction({ZM_ACTION_RUN})` -> flee -> OVER), so KO and flee share the ONE `RequestBattleEnd` latch. +7 T0 menu units + `ZM_BattleMenuWin_Test`/`ZM_BattleMenuRun_Test`; the two delegating windowed tests got an ENTER drive (H0). Reviewer 3-lens: no blockers (all nits are item-5 forward notes). zm-tests 1988->1995, engine default 1088 unchanged. Full gate green. Prior: SC4 (ZM-D-103) `ZM_UI_BattleHUD`; SC3 (ZM-D-102) `ZM_BattleDirector`; SC2 (ZM-D-101) `ZM_BattleDirectorCore`; SC1 (ZM-D-100) engine E3 typewriter.

## Notes for next agent (S5 item 4 SC6 -> item 5)

- **SC6 is the item-4 CLOSER, not a big feature.** The windowed suite already proves player-driven win (`ZM_BattleMenuWin_Test`) + flee (`ZM_BattleMenuRun_Test`) + presentation/resume (`ZM_BattleHUD_Test`, `ZM_BattleDirectorRoundTrip_Test`). Read the plan's SC6 acceptance list; add only what's missing, then TICK the item-4 Roadmap box (line 86). Do NOT tick the S5 STAGE gate (that is the visual hard-stop after item 5).
- **Item-5 FORWARD NOTES from the SC5 reviewer (act on these WHEN item 5 lands real party/moves -- they are NOT bugs today):**
  1. **The battle menu couples cursor-index to raw move-SLOT-index** (`ZM_UI_BattleHUD.cpp` menu refresh + `MenuConfirm` submits `m_uMoveSlot = cursor`). Correct only while movesets pack contiguously from slot 0 (the placeholder player always does). Item 5's real party members can have GAPPED movesets -> switch the menu to a FILLED-SLOT index table (map cursor -> the Nth non-empty slot) before that lands, or a gapped active shows a blank unselectable button and the real move is unreachable.
  2. **All-0-PP active -> Fight submenu is a silent dead-end** (no Struggle, no feedback). Not a softlock (Run + 30 s deadline). Struggle is the pre-existing engine gap Q-2026-07-12-003; add it (engine) if item 5+ needs it.
  3. **The two H0-edited windowed tests prove resolution INDIRECTLY** (resume deadline < softlock). Consider adding a direct `GetCore().IsOver()` latch+assert to `ZM_BattleDirectorRoundTrip_Test`/`ZM_BattleHUD_Test` for robustness (the new menu tests already assert `GetWinner()`).
- **The SC3 `RequestBattleEnd`-return nit is DONE** (both sites branch on the bool now).
- **HUD/menu seam is Option B:** `ZM_UI_BattleHUD` owned by value in the director (`m_xHud`), NOT an ECS component. Re-resolve UI elements each frame (never cache). Keep the director the single `ZM_BattleDirectorCore` owner. Next-free ECS order is **112** (SC4/SC5 added NO component).
- **Gate ORDER + baselines (permanent):** `zenith test Zenithmon --headless` FIRST (heals DLLs). Boot unit gate hangs after the units line -> `pwsh -NoProfile -File Tools\run_unit_gate.ps1 -Exe <exe> -Baseline 1995 -TimeoutSec 300` (the 300s timeout-kill is EXPECTED). Windowed: `--filter ZM_Battle` runs all 8 battle windowed tests in one boot. New files -> `Build\regen.ps1`. ENGINE-unit changes bump BOTH `zm-tests.yml` AND `run_unit_gate.ps1`'s default (1088) + need Combat/RenderTest/DP/CityBuilder regression (RenderTest pre-existingly red -- Q-2026-07-16-001). Diagnosing a windowed test: the game's `Zenith_Log(LOG_CATEGORY_UNITTEST)` diagnostics are NOT in the harness stdout -- run the exe directly `<exe> --automated-test <Name> --exit-after-frames N` to see them.
- **Working model:** MASTER-ONLY (ZM-D-031); LOCAL gate is authority; `zm-tests` post-push backstop. Orchestrated: only the orchestrator builds/tests/commits; subagents author, never build. Sweep stray `zenithmon.exe` before ending. NEVER commit baked assets or captures.
- **Open Questions:** Q-2026-07-17-002 (S5 item-4 scoping -- SC3/SC4/SC5 acted on best-guesses (a) placeholder player team, (b) exp OFF, (c) Fight+Run only, (d) GREEDY wild AI); Q-2026-07-17-001 (`BiomeForScene` hard-coded table -- WorldSpec column at S9/S10); Q-2026-07-16-001 (RenderTest terrain); Q-2026-07-09-003 (S5 bleed-through -- verified/falsified only at the S5 SCREENSHOT gate).
- **The S5 gate is the next VISUAL hard-stop** (all 5 items: windowed encounter round-trip + catch + exp + exact resume + the bleed-through screenshot). Do not tick it or start S6 without the user's sign-off.
