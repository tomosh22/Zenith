# Zenithmon Status

**Last updated:** 2026-07-18
**Stage:** **S5 (Battle integration slice) IN PROGRESS.** S0-S4 COMPLETE. **S5 items 1+2+3+4 DONE; item 5 IN PROGRESS.** Item 4 closed via SC1-SC6 (ZM-D-100..105). **Item 5 SC1 DONE (ZM-D-106)** -- pure `Source/Party/` model; **SC2 DONE (ZM-D-107)** -- `ZM_GameStateManager` (order 104) owns the in-memory `ZM_GameState` (seeded Fernfawn L5 starter, `DontDestroyOnLoad`) + the static `TryGetGameState` accessor + a between-tests reseed. **NEXT is item 5 SC3** -- flip `m_bAwardExp`, `RunSetup` reads the real party lead (replaces `BuildPlaceholderPlayerSpec`), pure `ZM_ApplyBattleResultToParty` writes exp/level/moves/PP/HP back + the gapped-moveset menu fix (gate: win->exp+resume). Then SC4-SC6, then the **S5 GATE = the next VISUAL hard-stop** (user sign-off; AFTER item 5).
**Build:** GREEN -- Zenithmon Vulkan Debug True clean. Master clean + pushed at the item-5 SC2 commit (see git log).
**Tests:** boot unit gate **2009 ran / 2008 passed / 0 failed / 1 skipped** (item-5 SC1 = +14 pure `ZM_Party` units; SC2 added NO T0 units -- windowed-only -- so `zm-tests.yml` stays **2009**; engine default **1088** UNCHANGED). `zenith test Zenithmon --headless` **20/0** (SC2 registered `ZM_GameStatePersistence_Test`, auto-skipped headless). Windowed: `ZM_GameStatePersistence_Test` **1/0** (seed -> mutate -> scene load -> survived, a DontDestroyOnLoad proof) + the item-4 battle suite **8/0** (no manager regression).

## Current task

**S5 item 5 SC3 -- the battle exp write-back (the first item-5 gameplay effect).** Flip `ZM_BattleDirector::BuildBattleConfig()` `m_bAwardExp = true`; in `RunSetup`, replace `BuildPlaceholderPlayerSpec()` with a read of the REAL party lead via `ZM_GameStateManager::TryGetGameState` -> `pxGS->m_xParty.Lead()` -> `ZM_MonsterToBattleSpec(...)`; on battle end (win or resolve), write the post-battle lead state back with a pure `ZM_ApplyBattleResultToParty` (built on SC1's `ZM_ApplyBattleMonsterToRecord`) -- exp/level/moves+PP/curHP. **APPEND `m_uCurHP` (+ a "use it" flag) to `ZM_BattleMonsterSpec`** so the spec carries the lead's DAMAGED HP into battle (SC1 deferred this -- `ZM_MonsterToBattleSpec` currently builds full-HP); wire `ZM_BuildBattleMonster` to honor it. **Also FIX the gapped-moveset menu coupling** (the SC5 reviewer forward note: the move menu maps cursor->raw slot; a real party member can have gapped movesets -> compact to a filled-slot index table). GATE PROOF: a windowed test walks grass -> wins -> asserts the party lead's exp/level rose + exact overworld resume. Single-LEAD only (Q-2026-07-18-001). Consult the durable plan's SC3 section.

**DECOMPOSED (2026-07-18):** durable plan on disk at `Build/artifacts/zm_s5_item5_plan/plan.md`. Item 5 is almost pure WIRING -- ZERO engine changes. **6 sub-commits:** **SC1 DONE (ZM-D-106)** pure `Source/Party/` model (+14 units); **SC2 DONE (ZM-D-107)** `ZM_GameStateManager` owns the in-memory `ZM_GameState` (DontDestroyOnLoad, seeded starter) + static `TryGetGameState` + between-tests reseed (windowed persistence test); **SC3** flip `m_bAwardExp`, `RunSetup` reads the real lead (replaces `BuildPlaceholderPlayerSpec`), pure `ZM_ApplyBattleResultToParty` writes exp/level/moves/PP/HP back + append `m_uCurHP` to `ZM_BattleMonsterSpec` + the gapped-moveset menu fix (gate: win->exp+resume); **SC4** flip `m_bCanCatch`, a minimal Catch root-menu item -> `{ITEM, ball}`, catch write-back adds to party + dex (gate: catch); **SC5** loss->whiteout (heal + warp to Dawnmere "TownCenter") (gate: faint->whiteout); **SC6** consolidated windowed `--filter ZM_Battle` gate + bleed-through screenshot (Q-2026-07-09-003) + docs -> sets the S5 VISUAL GATE-WAIT.

**Scope rulings logged as Q-2026-07-18-001** (best-guesses, loop PROCEEDS on them): single-LEAD battle (defer switch to S6+), fixed starter (Fernfawn L5), GameState IN-MEMORY only (no save until S7), direct Catch button (no Bag UI until S6), whiteout->Dawnmere. **NEXT SC = SC3**. SC1+SC2 surface for SC3-SC6: `ZM_GameStateManager::TryGetGameState(ZM_GameState*&)` (static, cross-scene, mutable); `ZM_GameState{ m_xParty, m_xCaught(`Mark`/`IsSet`/`Count`), m_bPendingWhiteout }`; `ZM_Party::Lead()/Add()/Count()/AllFainted()/HealAllFull()`; `ZM_MonsterToBattleSpec`/`ZM_ApplyBattleMonsterToRecord`/`ZM_MonsterFromBattleMonster`; `ZM_MakeStarterGameState()`. NOTE: `ZM_MonsterToBattleSpec` does NOT yet carry damaged curHP/PP -- **SC3 must append `m_uCurHP` to `ZM_BattleMonsterSpec`** + wire `ZM_BuildBattleMonster` to honor it.

**S5 item 5 SC2 (ZM-D-107) -- `ZM_GameStateManager` owns the persistent in-memory `ZM_GameState`.** The `DontDestroyOnLoad` manager (order 104) now owns `ZM_GameState m_xGameState` by value, seeds it once in `OnStart` (Fernfawn L5 starter; before the persist-move, only for the authoritative singleton -- survives every SINGLE load, no mid-game re-seed), exposes `static TryGetGameState(ZM_GameState*&)` (fresh unique-manager resolve, mutable, fail-closed), and re-seeds between batched tests via `ResetGameStateForTests` in the between-tests hook. No disk serialization (in-memory only; S7 owns disk). Windowed `ZM_GameStatePersistence_Test` proves seed -> mutate -> scene load -> survived; no T0 units (baseline stays 2009). Focused reviewer NO BLOCKERS (2 nits are forward notes: `ResetGameStateForTests` could resolve via `s_xSingletonEntityID` for symmetry like its sibling; the persistence test's clean-starter asserts only actively prove the reseed if an earlier test dirtied state). Build + gate + battle-regression green. Prior: SC1 (ZM-D-106) pure Party model; item 4 COMPLETE via SC1-SC6 (ZM-D-100..105).

## Notes for next agent (S5 item 5)

- **Item 4 is CLOSED (all 6 SCs, ZM-D-100..105).** The windowed suite proves player-driven win (`ZM_BattleMenuWin_Test`) + flee (`ZM_BattleMenuRun_Test`) + presentation/resume (`ZM_BattleHUD_Test`, `ZM_BattleDirectorRoundTrip_Test`). Item 5 ("Catch / exp / faint / whiteout applied to GameState") is the LAST S5 item; the S5 STAGE gate (visual hard-stop, user sign-off) is AFTER it.
- **Item 5 is where PERSISTENT GameState first appears.** Item 4 deliberately used a deterministic placeholder player (`ZM_BattleDirector::BuildPlaceholderPlayerSpec`, discarded on unload) with exp OFF + no catch. Item 5 wires a real party/GameState, flips `m_bAwardExp`/`m_bCanCatch` (the EXP/LEVEL/CATCH events are already mapped -> minimal `ZM_BattleDirectorCore` change), and handles faint->whiteout. Plan it first (it touches SaveData/party persistence). No `ZM_GameState`/`ZM_Party` exists yet (verified at SC3).
- **Item-5 FORWARD NOTES from the SC5 reviewer (act on these WHEN item 5 lands real party/moves -- they are NOT bugs today):**
  1. **The battle menu couples cursor-index to raw move-SLOT-index** (`ZM_UI_BattleHUD.cpp` menu refresh + `MenuConfirm` submits `m_uMoveSlot = cursor`). Correct only while movesets pack contiguously from slot 0 (the placeholder player always does). Item 5's real party members can have GAPPED movesets -> switch the menu to a FILLED-SLOT index table (map cursor -> the Nth non-empty slot) before that lands, or a gapped active shows a blank unselectable button and the real move is unreachable.
  2. **All-0-PP active -> Fight submenu is a silent dead-end** (no Struggle, no feedback). Not a softlock (Run + 30 s deadline). Struggle is the pre-existing engine gap Q-2026-07-12-003; add it (engine) if item 5+ needs it.
  3. **The two H0-edited windowed tests prove resolution INDIRECTLY** (resume deadline < softlock). Consider adding a direct `GetCore().IsOver()` latch+assert to `ZM_BattleDirectorRoundTrip_Test`/`ZM_BattleHUD_Test` for robustness (the new menu tests already assert `GetWinner()`).
- **The SC3 `RequestBattleEnd`-return nit is DONE** (both sites branch on the bool now).
- **HUD/menu seam is Option B:** `ZM_UI_BattleHUD` owned by value in the director (`m_xHud`), NOT an ECS component. Re-resolve UI elements each frame (never cache). Keep the director the single `ZM_BattleDirectorCore` owner. Next-free ECS order is **112** (SC4/SC5 added NO component).
- **Gate ORDER + baselines (permanent):** `zenith test Zenithmon --headless` FIRST (heals DLLs). Boot unit gate hangs after the units line -> `pwsh -NoProfile -File Tools\run_unit_gate.ps1 -Exe <exe> -Baseline 1995 -TimeoutSec 300` (the 300s timeout-kill is EXPECTED). Windowed: `--filter ZM_Battle` runs all 8 battle windowed tests in one boot. New files -> `Build\regen.ps1`. ENGINE-unit changes bump BOTH `zm-tests.yml` AND `run_unit_gate.ps1`'s default (1088) + need Combat/RenderTest/DP/CityBuilder regression (RenderTest pre-existingly red -- Q-2026-07-16-001). Diagnosing a windowed test: the game's `Zenith_Log(LOG_CATEGORY_UNITTEST)` diagnostics are NOT in the harness stdout -- run the exe directly `<exe> --automated-test <Name> --exit-after-frames N` to see them.
- **Working model:** MASTER-ONLY (ZM-D-031); LOCAL gate is authority; `zm-tests` post-push backstop. Orchestrated: only the orchestrator builds/tests/commits; subagents author, never build. Sweep stray `zenithmon.exe` before ending. NEVER commit baked assets or captures.
- **Open Questions:** Q-2026-07-17-002 (S5 item-4 scoping -- SC3/SC4/SC5 acted on best-guesses (a) placeholder player team, (b) exp OFF, (c) Fight+Run only, (d) GREEDY wild AI); Q-2026-07-17-001 (`BiomeForScene` hard-coded table -- WorldSpec column at S9/S10); Q-2026-07-16-001 (RenderTest terrain); Q-2026-07-09-003 (S5 bleed-through -- verified/falsified only at the S5 SCREENSHOT gate).
- **The S5 gate is the next VISUAL hard-stop** (all 5 items: windowed encounter round-trip + catch + exp + exact resume + the bleed-through screenshot). Do not tick it or start S6 without the user's sign-off.
