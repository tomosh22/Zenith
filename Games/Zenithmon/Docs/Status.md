# Zenithmon Status

**Last updated:** 2026-07-16
**Stage:** S4 (Asset generators) -- in progress. **`ZM_BuildingGen`/`ZM_PropGen` COMPLETE (SC1-SC6, ZM-D-079..084).** Both STATIC-model families generate + bake skeleton-less 4-file bundles to disk. **`ZM_HumanGen`/`ZM_CreatureGen`/`ZM_CreatureAnimGen` COMPLETE.** S1/S2/S3 COMPLETE. **Only `ZM_BakeManifest` remains as an S4 code box**, then the S4 gate.
**Build:** GREEN -- SC6 is Docs-only (no build); the last code gate was SC5's full 5-config matrix (Vulkan Debug/Release x True/False + `D3D12_vs2022_Debug_Win64_False` link proof), all green.
**Tests:** boot unit gate **1905 ran / 1904 passed / 0 failed / 1 skipped** (unchanged -- SC6 added no units); `zenith test Zenithmon --headless` **7 passed / 0 failed**. `.github/workflows/zm-tests.yml` baseline = **1905**. TestPlan.md now reconciled to 1905; the `ZM_Gen` category = 132 units (6 tools-only bake smokes: 2 creature + 2 human + 1 building + 1 prop).

## Current task

**`ZM_BakeManifest` -- the LAST S4 code box.** Per-FAMILY manifest guard so warm boots skip the bake: a stamp recording (generator-version + file-existence) per family (creatures / humans / buildings / props / each terrain set), so a `*_True` tools boot re-bakes a family ONLY when its version changed or its files are missing; and the byte-identical re-bake invariant (same seed -> byte-identical output, re-run the tools boot -> zero diffs) as a tested guard. Read AssetManifest.md section 6 (the manifest-stamp scheme -- `uZM_CREATUREGEN_VERSION`=3, `uZM_HUMANGEN_VERSION`=1, `uZM_BUILDINGGEN_VERSION`=1, `uZM_PROPGEN_VERSION`=1, terrain-family `ZMTR` markers) for the contract. Likely a shared `ZM_BakeManifest` helper (tools-only) + per-family stamp write/read + a `ZM_Gen` (or tools-only) determinism/guard unit. After `ZM_BakeManifest`: the **S4 GATE** -- 4-config matrix + a windowed gallery scene showing every asset family (creatures already gate-signed ZM-D-067; buildings/props/humans are NEW) + **visual sign-off = the next VISUAL GATE (HARD STOP for the user)**. Consult MasterPlan.md / Roadmap.md S4 for the gate detail.

## Last completed

**`ZM_BuildingGen`/`ZM_PropGen` SC6 -- coverage + doc reconcile, ROADMAP ITEM COMPLETE (ZM-D-084).** No code change: the totality coverage is already carried by the shipped `BuildingGen_RosterTotality` (30) + `PropGen_RosterTotality` (25) units. AssetManifest section 3 filled with the resolved 4-file static-bundle breakdown (buildings 30x4=120, props 25x4=100; colliders scene-authored) + version stamps; TestPlan section 5.4 gains the BuildingGen (9) + PropGen (8) + 2 bake-smoke subsections and reconciles its baseline 1866->1905. Roadmap box TICKED. Delegated the reference-doc bodies to a Doc Maintainer; orchestrator wrote the living docs + ticked the box.

## Notes for next agent

- **S4 status:** ALL generator families COMPLETE (creatures, creature-anims, humans, buildings, props). The ONLY remaining S4 code box is **`ZM_BakeManifest`** (manifest guards + byte-identical re-bake invariant). Then the **S4 gate** = the next VISUAL GATE (windowed gallery + human sign-off) -- HARD STOP. When you reach the gate: run the automated items, capture windowed screenshot evidence, set a `GATE-WAIT: S4 gallery visual sign-off` marker in Status.md, commit + push the docs, and END (do NOT tick the gate or start S5). The creature gallery was signed off (ZM-D-067) but buildings/props/humans are NEW families to show.
- **Baked static bundle (shipped SC5, git-ignored):** per building `game:Buildings/<Name>/` {`.zmesh` skeleton-less / `_facade.ztxtr` BC1 256^2 / `.zmtrl` / `.zmodel` no-rig}; per prop `game:Props/<Name>/` {`.zmesh` / `_albedo.ztxtr` BC1 128^2 / `.zmtrl` / `.zmodel`}. `ZM_BakeAllBuildings`/`ZM_BakeAllProps` bake the whole roster; `ZM_BakeManifest` should gate those (skip when version+files current). Existing family versions: creature 3, human 1, building 1, prop 1.
- **Asset-gen kit (all shipped):** `ZM_GenCommon` (RNG/seed/noise/loft), `ZM_StaticMesh` (bone-free box/roof kit + `ZM_PushStaticTri`), `ZM_TextureSynth`, the FOUR bake bridges (`ZM_GenBakeMesh` own-skel / `ZM_GenBakeSkeleton` shared-skel / `ZM_GenBakeMeshWithSharedSkeleton` bind-shared / `ZM_GenBakeStaticMesh` no-skel), `ZM_ValidateGenMesh`(skinned)/`ZM_ValidateGenMeshStatic`. `ZM_GEN_DOMAIN` PINNED. Determinism is per-family `f(id/seed)`; the byte-identical re-bake invariant is what `ZM_BakeManifest` formalizes.
- **Doc cadence:** CI baseline + TestPlan + AssetManifest are now all reconciled at 1905. Future per-SC: bump the CI baseline every SC; reconcile prose at item completion.
- **Gate ORDER + baseline (unchanged):** `zenith test Zenithmon --headless` FIRST (heals DLLs, else `0xC0000135`); boot unit gate hangs after the units line -> `run_unit_gate.ps1 -Baseline N -TimeoutSec 300` times out+kills but captures `Unit tests complete: N ran`. New files need `pwsh -NoProfile -File Build\regen.ps1`. Non-default configs via msbuild ABSOLUTE sln path (`C:\Program Files\Microsoft Visual Studio\18\Insiders\MSBuild\Current\Bin\MSBuild.exe`), serial only. `git -C C:/dev/Zenith ...`.
- **Working model:** MASTER-ONLY (ZM-D-031); local gate is the authority; `zm-tests` is the post-push backstop, fix forward on red. Sweep stray `zenithmon.exe` before ending. Orchestrator+subagents this session (Plan -> Implement -> Reviewer per SC; Doc Maintainer for reference docs); only the orchestrator builds/tests/commits. A stray harness worktree exists at `.claude/worktrees/` -- NEVER regen/build/commit from it (Invariant 2). Never commit baked assets.
