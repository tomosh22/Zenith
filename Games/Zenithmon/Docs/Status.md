# Zenithmon Status

**Last updated:** 2026-07-14
**Stage:** S4 (Asset generators) -- in progress. `ZM_GenCommon`/`ZM_TextureSynth` foundation (ZM-D-059) + `ZM_CreatureGen` **SC1** (QUADRUPED, ZM-D-060) + **SC2** (BIPED + AVIAN, ZM-D-061) + **SC3** (SERPENT + AQUATIC, ZM-D-062) + **SC4** (INSECTOID + BLOB, ZM-D-063) COMPLETE -- 7 of 8 archetypes wired. S1/S2/S3 COMPLETE (S3 visual gate SIGNED OFF 2026-07-13).
**Build:** passing -- Vulkan_vs2022_Debug_Win64_True green.
**Tests:** boot unit gate **1842 ran / 1841 passed / 0 failed / 1 skipped** (baseline bumped 1836 -> 1842, in `.github/workflows/zm-tests.yml` too); `zenith test Zenithmon --headless` **6 passed / 0 failed**.

## Current task

`ZM_CreatureGen` archetype waves against the FROZEN `Source/Gen/ZM_CreatureGen.h` seam. 7 of 8 builders wired (QUADRUPED, BIPED, AVIAN, SERPENT, AQUATIC, INSECTOID, BLOB). **Next unchecked: SC5 -- the FINAL archetype + the S4 GATE.** SC5 is multi-part: (1) author `ZM_CreatureArchetype_FloaterPlantoid.cpp` + `Tests/ZM_Tests_CreatureArchetype_FloaterPlantoid.cpp` (a floating body + radial petals/tendrils, NO ground legs, off-origin no-foot layout -- may need a local helper like AVIAN/AQUATIC) and wire the last switch case; (2) with all 8 wired, ADD the all-152 coverage gate to `ZM_Tests_CreatureGen.cpp` (assert every ZM_SPECIES_ID is buildable = no NONE archetypes escape) + a tools-gated bake smoke; (3) complete the SC5-DEFERRED `.zmtrl` (normal + shiny child material) + `.zmodel` bundle writes in `ZM_BakeCreature` (currently a `// SC5:` TODO) -- confirm the Zenith_MaterialAsset / .zmodel author API first; (4) run the windowed species-gallery visual check (a scene showing a sampled dozen species animating -- AgentBriefing.md has the windowed-capture recipe), capture screenshot evidence under `Build/artifacts/zenithmon/s4/visual/`, write a `GATE-WAIT: S4 visual sign-off` marker into this file with the evidence paths, append the automated gate results to DecisionLog.md, commit + push, and **STOP** for user sign-off. Do NOT tick S4 or start S5 without the sign-off (StartPrompts.md prompt 4).

## Last completed

**S4 `ZM_CreatureGen` SC4** (this commit, ZM-D-063): the INSECTOID (19-bone; 6 legs + antennae -- the HIGH-bone extreme) + BLOB (4-bone super-ellipse body -- the LOW-bone extreme) archetype builders + per-archetype tests, wired into the explicit `ZM_GetArchetypeBuilder` switch. +6 `ZM_Gen` units (1836 -> 1842). Reviewer: no blockers/majors/minors (3 cosmetic nits, not fixed). No stale-test churn.

## Notes for next agent

- **How to land an archetype (the SC3-5 recipe -- NO header edit needed anymore):** the frozen `ZM_CreatureGen.h` already declares all 8 `ZM_BuildArchetype_<Name>` functions. So each remaining SC only: (1) authors `Source/Gen/ZM_CreatureArchetype_<Name>.cpp` (its builder), (2) authors `Tests/ZM_Tests_CreatureArchetype_<Name>.cpp` (mirror the QUADRUPED/BIPED/AVIAN test: a top-level `HasXBuilder()` early-return + the universal harness over the archetype's species + a structural bone assert), and (3) ORCHESTRATOR wires ONE line in the `ZM_GetArchetypeBuilder` switch in `ZM_CreatureGen.cpp` (`case ZM_ARCHETYPE_<NAME>: return &ZM_BuildArchetype_<Name>;`). Do NOT edit the header, the kit, or `ZM_Tests_CreatureGen.cpp`.
- **The generic harness auto-grows:** `Tests/ZM_Tests_CreatureGen.cpp` loops every species whose builder is non-null and runs the 12 universal invariants -- so wiring an archetype automatically extends full coverage to its species. `CreatureGen_ArchetypeDispatch` is now SC-agnostic (do not re-add a fixed un-wired list). Per-archetype test files must guard every build on a non-null-builder check so they no-op if unwired.
- **Reuse the kit** (`ZM_CreatureArchetypeCommon`): `ZM_AppendSpineTube/Limb/Tail/Horn/EllipsoidHead` + `ZM_SizeClassScale` + `ZM_FormatBoneName`. If an archetype needs a shape the kit can't express (e.g. AVIAN's flat wing), add a LOCAL helper INSIDE that builder's .cpp anon namespace -- do NOT edit the shared kit (single-writer). Each authored ring binds a SINGLE bone; draw randomness ONLY via `ZM_MakeGenRNG(recipe, ZM_GEN_DOMAIN_MESH/SKELETON)`, ALL draws up-front in a FIXED order; bone topology IDENTICAL across evo stages; scale by `m_fSizeScale`; no finalise/bake in a builder.
- **Golden pins are frozen** (ZM-D-060/061): size-scale curve, type->pattern table, shiny band [80,280), eye placement, dex-icon method, asset scheme, and each archetype's proportions/jitter. Any generation-algorithm change bumps `uZM_CREATUREGEN_VERSION`.
- **SC5 owes:** `.zmtrl` (normal + shiny child material) + `.zmodel` bundle writes (currently a `// SC5:` TODO in `ZM_BakeCreature`), the all-152 coverage gate, and the windowed species-gallery visual gate (set a `GATE-WAIT: S4 visual sign-off` marker + capture evidence, then STOP).
- **Gate ORDER gotcha (cost false failures):** `Tools\run_unit_gate.ps1` boots the exe DIRECTLY and does NOT heal DLLs. Run `Import-Module Build\zenith_buildsystem.psm1; Repair-ZenithRuntimeDlls -ExeDir <exeDir>` (or `zenith test --headless`) FIRST, else the exe dies instantly with `0xC0000135` (STATUS_DLL_NOT_FOUND -- assimp/minizip/poly2tri/pugixml/zlibd1 come from sibling game output dirs). The exe HANGS after the units-at-boot line, so `run_unit_gate` times out + kills, but the `Unit tests complete: N ran...` line is captured before the kill. Pass the CURRENT baseline; on a mismatch it "fails" but prints the real count -- use `-TimeoutSec 300`. When you add ZM_ units, bump `-Baseline` in BOTH your gate command AND `.github/workflows/zm-tests.yml` in the same commit.
- **Stale-test churn to expect per SC:** wiring an archetype turns its species "buildable", so any test that assumed a species was un-buildable will change behavior. The dispatch test is already SC-agnostic; watch for others.
- **Workflow `args` gotcha:** the Workflow tool's `args` object arrived as literal `"undefined"` inside the script this session -- inline paths/data as string literals in the workflow script body, don't rely on `args`.
- **Format-doc debt still deferred to the S4 GATE** (per ZM-D-059's at-stage-gate policy): full `Docs/Shortfalls.md` verdict/count refresh, the `ZM_Gen` creature test group in `Docs/TestPlan.md`, and the creature-bundle catalogue/budgets in `Docs/AssetManifest.md` land in the S4 stage-gate commit.
- **Working model unchanged:** MASTER-ONLY (ZM-D-031); the local gate (build + boot unit gate + headless) is the authority; `zm-tests` is the post-push backstop, fix forward on red. Sweep stray `zenithmon.exe` (`Get-Process zenithmon`) before ending each iteration.
