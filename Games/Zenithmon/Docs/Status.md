# Zenithmon Status

**Last updated:** 2026-07-14
**Stage:** S4 (Asset generators) -- in progress. **`ZM_CreatureAnimGen` SC2 of 6 LANDED (ZM-D-069):** BIPED + AVIAN clip builders added (mirroring the SC1 QUADRUPED pattern) + wired into the dispatch; the generic `ZM_Gen` anim harness was generalized so its byte-identity / determinism / loop-wrap / one-shot-neutral / faint-clamp tests auto-cover EVERY wired archetype. 3 of 8 archetypes now animate (Quadruped/Biped/Avian). Clips are 6 per archetype (Idle/Walk/Attack/Special/Hit/Faint), pure `f(archetype,clip-id)`, rotation-only, byte-identical across every species of an archetype; reuses the engine headerless `Flux_AnimationClip::Export` (no engine change). `ZM_CreatureGen` COMPLETE + gate-signed (ZM-D-067). S1/S2/S3 COMPLETE.
**Build:** GREEN -- Vulkan_vs2022_Debug_Win64_True. (SC2 adds no new engine dependency beyond the all-config `Flux_AnimationClip` already proven at SC1 across Debug False + the D3D12 Debug False null-backend link proof, so the cross-config matrix was not re-run for this sub-commit; the next stage/major gate re-runs the full matrix.)
**Tests:** boot unit gate **1859 ran / 1858 passed / 0 failed / 1 skipped** (was 1857; +2 new `ZM_Gen` per-archetype units -- the generalized harness added coverage, not test count); `zenith test Zenithmon --headless` **7 passed / 0 failed**. `.github/workflows/zm-tests.yml` baseline bumped 1857 -> **1859** (same commit).

## Current task

**`ZM_CreatureAnimGen` SC3: Serpent + Aquatic clip builders (LIMBLESS).** Add `Source/Gen/ZM_CreatureAnimArchetype_Serpent.cpp` (~12-bone spine chain incl. `Spine05`; author the 6 clips as travelling lateral spine undulation / strike / recoil / collapse -- NO legs) and `_Aquatic.cpp` (~8 bones; caudal + pectoral FIN sweeps + body-yaw sway -- NO legs), wire both into `ZM_GetArchetypeAnimBuilder`, and add per-archetype structural tests. FIRST read `Source/Gen/ZM_CreatureArchetype_Serpent.cpp` / `_Aquatic.cpp` for the EXACT frozen bone names. The generic harness auto-exercises them once wired (incl. the now bone-name-agnostic FaintSettlesAndClamps). Mirror the SC1/SC2 pattern exactly; reuse the `ZM_CreatureAnimCommon` kit (add a spine-undulation or fin-sweep helper only if genuinely new). Still NO disk bake / NO `ZM_CreatureGen.*` edit (SC6). Rebuild, boot unit gate at the new measured count, headless, commit + push to master. Remaining after SC3: SC4 Insectoid+Blob, SC5 FloaterPlantoid + all-8 totality gate, SC6 bundle-bake wiring (ticks the Roadmap box), SC7 optional windowed playability gate.

## Last completed

**`ZM_CreatureAnimGen` SC2 (ZM-D-069):** BIPED builder (14-bone set: Spine00-03, Head, Arm{L,R}{Up,Lo}, Leg{L,R}{Up,Lo}, Crest) + AVIAN builder (13-bone: Spine00-02, Head, Beak, Wing{L,R}, Leg{L,R}{Up,Lo}, Tail0{0,1}), wired into `ZM_GetArchetypeAnimBuilder`; `Tests/ZM_Tests_CreatureAnimArchetype_Biped.cpp` + `_Avian.cpp` structural locks (Crest=Special-only, Beak=Attack-only, + negative locks); generic harness tests 4-9 generalized to loop all wired archetypes (helpers `WiredAnimArchetypes`/`FindTwoSpeciesOfArchetype`; `FaintSettlesAndClamps` de-hardcoded off "Spine01"). Committed + pushed to master.

## Notes for next agent

- **The per-archetype authoring pattern is proven (SC1 Quadruped, SC2 Biped/Avian).** Per archetype: read the archetype's `ZM_CreatureArchetype_<Name>.cpp` for its EXACT frozen bone names + hierarchy, author 6 rotation-only clips against those names (only key bones that exist), wire `ZM_GetArchetypeAnimBuilder`, add a per-archetype structural test (positive + negative bone-signature locks). The generic `ZM_Tests_CreatureAnimGen.cpp` harness is now `HasAnimBuilder`-gated AND archetype-generalized, so it auto-covers new archetypes (ChannelsMatchSkeleton, ValidationPasses, byte-identity-across-species, determinism, clips-distinct, loop-wrap, one-shot-neutral, faint-clamp) -- do NOT edit it per archetype.
- **Per-archetype bone counts** (confirm exact NAMES from the code): QUADRUPED 18 (done), BIPED 14 (done), AVIAN 13 (done), SERPENT 12, AQUATIC 8, INSECTOID 19, BLOB 4, FLOATER-PLANTOID 10. Limbless archetypes (SERPENT/AQUATIC/BLOB/FLOATER) drive spine-undulation / fin / tendril rotations instead of leg gait; BLOB (Spine00-02 + Nub) is sparsest; FLOATER's crown bone is literally named "Head".
- **Rotation-only is load-bearing** -- channels REPLACE bind-local TRS (not deltas); bind-local rotation is identity + scale unit for ALL species so absolute rotation is species-safe, but position varies per species (size scale) so NO position/scale channels. Express bob/hop/collapse via spine-flexion rotation.
- **Gotchas (paid for once):** quats are glm WXYZ (`glm::angleAxis(glm::radians(deg),axis)`); `SetDuration` is SECONDS but keyframe times are TICKS (tick = t01*durSec*24) -- use `ZM_AnimTicksForT01`; looping clips must key0==keyN per channel (Idle/Walk); one-shot clips non-looping, Attack/Special/Hit end at ~identity (clip-end clamp holds neutral), Faint holds its collapsed final pose (do NOT return to bind). Byte-equality uses `Zenith_DataStream::GetCursor()` (bytes written) not `GetCapacity()`. Fn-ptr nullness via `!= nullptr` (MSVC rejects `ZENITH_ASSERT_NULL` on fnptrs).
- **SC6 (the box-ticking commit) touches `ZM_CreatureGen.*`:** append 6 ANIM values to `ZM_CREATURE_ASSET_KIND` + 6 `ZM_CreatureBasenameFmt` cases (`%s_Idle.zanim`..`%s_Faint.zanim`); implement `ZM_BakeCreatureClips` (under `#ifdef ZENITH_TOOLS`); call it inside `ZM_BakeCreature` AFTER the texture bakes (Export creates no dirs -- relies on the species folder those bakes made); `AddAnimationPath` x6 in BOTH `.zmodel` blocks; bump `uZM_CREATUREGEN_VERSION` 2->3 (baked `.zmodel` bytes change). The enum extension + the `ZM_BakeCreatureClips` writes MUST land together (the existing `CreatureBake_BundleFilesLand` loop auto-covers the new kinds). Tools bake smoke = ONE species (Fernfawn) x 6 clips. SC6 also updates TestPlan.md + AssetManifest.md (reference docs) and re-runs the full 4-config + D3D12 matrix (it edits `ZM_CreatureGen.*`).
- **Gate ORDER + baseline gotchas (unchanged):** `Tools\run_unit_gate.ps1` boots the exe DIRECTLY -- run `zenith test Zenithmon --headless` FIRST to heal DLLs, else `0xC0000135`. The exe hangs after the units line so the gate times out + kills but captures `Unit tests complete: N ran`; pass the CURRENT baseline + `-TimeoutSec 300`. When ZM_ boot units change, bump the count in BOTH `.github/workflows/zm-tests.yml` AND Status.md in the SAME commit. Build non-default configs via msbuild with the ABSOLUTE sln path `C:\dev\Zenith\Games\Zenithmon\zenithmon_win64.sln`. `git -C C:/dev/Zenith ...` in Bash calls.
- **Working model:** MASTER-ONLY (ZM-D-031); local gate is the authority; `zm-tests` is the post-push backstop, fix forward on red. Sweep stray `zenithmon.exe` before ending each iteration. The remaining S4 items have NO visual gate until the S8 vertical-slice go/no-go.
