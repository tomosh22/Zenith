# Zenithmon Status

**Last updated:** 2026-07-14
**Stage:** S4 (Asset generators) -- in progress. **`ZM_CreatureAnimGen` SC5 of 6 LANDED (ZM-D-072): ALL 8 archetype clip builders DONE.** FLOATER-PLANTOID added -> `ZM_GetArchetypeAnimBuilder` is now TOTAL (totality gate test added). Every one of the 152 species yields 6 pure rotation-only clips (Idle/Walk/Attack/Special/Hit/Faint) headless, byte-identical across species of an archetype, all auto-verified by the archetype-generalized harness. Only the DISK BUNDLE BAKE (SC6) remains. `ZM_CreatureGen` COMPLETE + gate-signed (ZM-D-067). S1/S2/S3 COMPLETE.
**Build:** GREEN -- Vulkan_vs2022_Debug_Win64_True. (Cross-config purity proven at SC1; SC6 re-runs the full 4-config + D3D12 matrix since it edits `ZM_CreatureGen.*`.)
**Tests:** boot unit gate **1865 ran / 1864 passed / 0 failed / 1 skipped** (was 1863; +2: FloaterPlantoid structural + the all-8 totality gate); `zenith test Zenithmon --headless` **7 passed / 0 failed**. `.github/workflows/zm-tests.yml` baseline bumped 1863 -> **1865** (same commit).

## Current task

**`ZM_CreatureAnimGen` SC6: bundle-bake wiring -- the box-ticking commit (touches `ZM_CreatureGen.*`; re-runs the full matrix + updates reference docs).** This is the largest remaining sub-commit. Steps:
1. **`Source/Gen/ZM_CreatureGen.h`:** append 6 values to the `ZM_CREATURE_ASSET_KIND` enum, IN THE SAME ORDER as `ZM_ANIM_CLIP` (`ZM_CREATURE_ASSET_ANIM_IDLE, _WALK, _ATTACK, _SPECIAL, _HIT, _FAINT`), placed BEFORE `ZM_CREATURE_ASSET_KIND_COUNT` (raising it 9->15), so the contiguous mapping `(ZM_CREATURE_ASSET_KIND)(ZM_CREATURE_ASSET_ANIM_IDLE + eClip)` holds. Bump `uZM_CREATUREGEN_VERSION` 2u->3u (baked `.zmodel` bytes change) with a documented reason at the constant.
2. **`Source/Gen/ZM_CreatureGen.cpp`:** add 6 cases to the `ZM_CreatureBasenameFmt` switch returning `"%s_Idle.zanim" .. "%s_Faint.zanim"` (MUST match `ZM_CreatureClipName`); `#include` the anim-gen header; in `ZM_BakeCreature`, call `ZM_BakeCreatureClips(eId)` AFTER the texture bakes (Export creates NO dirs -- it relies on the species folder the mesh/texture bakes already `create_directories`'d); and in BOTH `.zmodel` model blocks (base + shiny), right after `SetSkeletonPath`, loop `c` in `[0, ZM_ANIM_CLIP_COUNT)`: resolve the clip ref via `ZM_CreatureAssetPath(eId, (ZM_CREATURE_ASSET_KIND)(ZM_CREATURE_ASSET_ANIM_IDLE + c), ...)` and `pxModel->AddAnimationPath(...)` so each `.zmodel` self-lists its 6 clips.
3. **`Source/Gen/ZM_CreatureAnimGen.cpp`:** REPLACE the SC1 `return false` stub of `ZM_BakeCreatureClips`/`ZM_BakeAllCreatureClips` with the real bodies (under `#ifdef ZENITH_TOOLS`): for each of the 6 clips build via `ZM_BuildCreatureClip(archetype, clip, xClip)`, resolve the tools filesystem path (`ZM_CreatureFsPath` from ZM_CreatureGen with the new ANIM kind), `xClip.Export(acFs)`, and `bOk &= std::filesystem::exists(...)` (Export is void, so exists() is the IO signal -- mirror `ZM_BakeCreature`). Skip archetypes with no anim builder (all wired now, but keep the guard). `ZM_BakeAllCreatureClips` loops all species.
4. **Tests:** add `Tests/ZM_Tests_CreatureAnimBake.cpp` -- ENTIRE file `#ifdef ZENITH_TOOLS` (empty TU in _False). `CreatureAnimBake_ClipsLandAndModelReferences`: `if(!ZM_BakeCreature(FERNFAWN)) ZENITH_SKIP(...)`; assert the 6 `<Name>_<Clip>.zanim` resolve + `std::filesystem::exists` + `file_size>0`; load the baked `.zmodel` and assert `GetNumAnimations()==6` with each `GetAnimationPath(i)` == the expected clip ref. ONE species (Fernfawn) scope -- do NOT loop all 152. The existing `CreatureBake_BundleFilesLand` auto-extends to 15 kinds.
5. **Docs (reference, SC6):** `Docs/TestPlan.md` (unit count + move ZM_CreatureAnimGen to shipped) + `Docs/AssetManifest.md` (mark the ~912 `.zanim` shipped). Orchestrator-owned living docs (Status/Roadmap/DecisionLog) as usual + TICK the Roadmap `ZM_CreatureAnimGen` box `[x]`.
6. **Gate:** full 4-config Vulkan matrix (Debug/Release x True/False) + `D3D12_vs2022_Debug_Win64_False` link proof + boot unit gate at the new measured count + headless. Because `uZM_CREATUREGEN_VERSION` bumped, a `*_True` boot COLD-re-bakes creature bundles -- expect the first tools boot to take longer.
Then SC7 (optional) windowed playability gate closes the Roadmap item's gate.

## Last completed

**`ZM_CreatureAnimGen` SC5 (ZM-D-072):** FLOATER-PLANTOID builder (10-bone: Spine00-02, Head, Tendril0-5 -- per-tendril radial-axis wave/paddle/bloom + crown/bulb flex, rotation-only), wired so the dispatch is TOTAL; `Tests/ZM_Tests_CreatureAnimArchetype_FloaterPlantoid.cpp` structural lock + `CreatureAnimGen_AllArchetypesHaveAnimBuilder` totality gate; fixed the last stale header comment. Committed + pushed to master.

## Notes for next agent

- **All 8 archetype builders are DONE (SC1-SC5).** The pure ALL-CONFIG generator is complete: `ZM_BuildCreatureClip(archetype, clip, out)` yields 6 rotation-only clips for every archetype, byte-identical across species, gate-verified. SC6 only adds the DISK BAKE + the `.zmodel` clip refs; do NOT touch the builders.
- **SC6 dependency shape:** `ZM_CreatureAnimGen.cpp`'s real `ZM_BakeCreatureClips` needs `ZM_CreatureFsPath` + the new `ZM_CREATURE_ASSET_ANIM_*` kinds from `ZM_CreatureGen.h`; `ZM_CreatureGen.cpp`'s `ZM_BakeCreature` needs `ZM_BakeCreatureClips` + `ZM_ANIM_CLIP_COUNT` from `ZM_CreatureAnimGen.h`. Include each header from the other's .cpp (not header-to-header) to avoid a cycle. The enum extension AND the `ZM_BakeCreatureClips` writes MUST land in the SAME commit -- the existing `CreatureBake_BundleFilesLand` loop iterates `[0, ZM_CREATURE_ASSET_KIND_COUNT)`, so raising the count to 15 immediately requires the 6 `.zanim` to exist or that test goes red.
- **Clip<->kind<->filename contract:** `ZM_ANIM_CLIP` order = IDLE,WALK,ATTACK,SPECIAL,HIT,FAINT; `ZM_CreatureClipName` = "Idle".."Faint"; basename fmt = `"%s_Idle.zanim".."%s_Faint.zanim"`; asset kinds `ZM_CREATURE_ASSET_ANIM_IDLE.._FAINT` in the same order. Keep all four aligned.
- **Rotation-only / gotchas (still apply):** quats glm WXYZ; ticks = t01*durSec*24; byte-equality uses `GetCursor()`; fn-ptr nullness `!= nullptr`; `Zenith_Assert` real vs `ZENITH_ASSERT` no-op; tools bake smoke is `#ifdef ZENITH_TOOLS` + `ZENITH_SKIP` when the bake env/assets are unavailable (distinct from the windowed harness's `RequestSkip`).
- **Gate ORDER + baseline gotchas (unchanged):** `Tools\run_unit_gate.ps1` boots the exe DIRECTLY -- run `zenith test Zenithmon --headless` FIRST to heal DLLs, else `0xC0000135`. Exe hangs after the units line -> gate times out+kills but captures `Unit tests complete: N ran`; pass the CURRENT baseline + `-TimeoutSec 300`. Bump the count in `.github/workflows/zm-tests.yml` + Status.md + TestPlan.md together. Build non-default configs via msbuild with the ABSOLUTE sln path `C:\dev\Zenith\Games\Zenithmon\zenithmon_win64.sln`. `git -C C:/dev/Zenith ...` in Bash calls. Never commit baked assets (the `.zanim`/bundles are git-ignored).
- **Working model:** MASTER-ONLY (ZM-D-031); local gate is the authority; `zm-tests` is the post-push backstop, fix forward on red. Sweep stray `zenithmon.exe` before ending each iteration. After SC6 the `ZM_CreatureAnimGen` Roadmap box ticks; the NEXT unchecked S4 item is `ZM_HumanGen` (one shared skeleton + one shared 9-clip set; ~35 models). NO visual gate until the S8 vertical-slice go/no-go.
