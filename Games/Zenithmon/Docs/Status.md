# Zenithmon Status

**Last updated:** 2026-07-14
**Stage:** S4 (Asset generators) -- in progress. **`ZM_CreatureGen` COMPLETE + VISUAL GATE SIGNED OFF (ZM-D-067):** all 8 archetype builders (SC1-SC5a) + `.zmtrl`/`.zmodel` bundle bake (SC5b) + windowed species-gallery gate (SC5c) + reflective floor + a +60% albedo saturation boost (SC5d); the user APPROVED the gallery. Every one of the 152 species builds + bakes a scene-loadable 9-file bundle. (Foundation = ZM-D-059.) S1/S2/S3 COMPLETE.
**Build:** GREEN across the S4-gate matrix -- Vulkan_vs2022_{Debug,Release}_Win64_{True,False} all build + the D3D12_vs2022_Debug_Win64_False null-backend link proof.
**Tests:** boot unit gate **1847 ran / 1846 passed / 0 failed / 1 skipped**; `zenith test Zenithmon --headless` **6 passed / 0 failed** (windowed `ZM_CreatureGallery_Test` skips headless). `.github/workflows/zm-tests.yml` baseline = 1847.

## Current task

**Next unchecked S4 Roadmap item: `ZM_CreatureAnimGen`** -- 6 clips per archetype template (idle / walk / attack / hurt / faint / cheer-or-similar), instantiated + exported per species (~900 `.zanim`). Build on the SAME frozen `ZM_CreatureGen` seam: the per-archetype skeletons are FIXED bone-name sets (identical across evo stages), so an archetype-level clip authored against those bone names transfers to every species of that archetype. Follow the proven pattern -- a design workflow (survey the engine's animation asset `Zenith_SkeletalAnimationAsset`/`.zanim` write path + the creature skeletons' bone-name conventions -> frozen anim-gen header) then per-archetype authoring against it, orchestrator builds/gates serially, reviewer. Determinism: procedural clips are pure functions of (archetype, clip-id) over the frozen bone set; version-stamp them. Then `ZM_HumanGen`, `ZM_BuildingGen`/`ZM_PropGen`, and `ZM_BakeManifest` (the byte-identical re-bake invariant -- the current creature bake smoke only checks the 9 files land, not re-bake determinism) close S4. The S4 gallery gate is DONE, so the remaining S4 items have no visual gate until the S8 vertical-slice go/no-go.

## Last completed

**S4 `ZM_CreatureGen` gate close (ZM-D-066 + ZM-D-067):** SC5c landed the windowed species-gallery test (`ZM_AutoTests_Gallery.cpp`) + the S4-gate matrix + the deferred TestPlan/AssetManifest/Shortfalls refresh; the user reviewed and requested two showcase enhancements, both delivered -- a smooth metallic reflective floor + SSR (creatures mirror on the floor), and a +60% deterministic HSV albedo saturation boost (`fZM_CREATURE_ALBEDO_SATURATION_BOOST`, version 1->2) so the palette reads vivid. Gate SIGNED OFF; loop resumes. `ZM_CreatureGen` (mesh/skeleton/albedo/shiny/icon/material/model generation) is feature-complete for the full dex.

## Notes for next agent

- **ZM_CreatureAnimGen leverage:** every archetype's skeleton is a FIXED, named bone set identical across evo stages (QUADRUPED Spine00..03/Head/Leg*Up-Lo/Tail/Horn = 18; BIPED 14; AVIAN 13; SERPENT 12; AQUATIC 8; INSECTOID 19; BLOB 4; FLOATER-PLANTOID 10 -- see the per-archetype tests). So author clips ONCE per archetype against those bone names; they instantiate per species. Survey `Zenith_SkeletalAnimationAsset` (or the engine's clip/`.zanim` asset) + how `Tools/Zenith_Tools_TestAssetExport.cpp` (StickFigure) exports clips, before designing the anim-gen seam. Add the clip refs to the creature `.zmodel`/bundle if models reference their clips.
- **Creature generator recap (all done):** all 8 `ZM_BuildArchetype_*` wired via the explicit switch in `ZM_CreatureGen.cpp`; generic 12-invariant harness (`ZM_Tests_CreatureGen.cpp`) over all 152 species; per-archetype structural tests + all-152 gate + golden-locks + a tools-gated bake smoke (`ZM_Tests_CreatureBake.cpp`); `ZM_BakeCreature` writes the 9-file bundle. Golden pins (size-scale, type->pattern, shiny band, asset scheme, per-archetype proportions, the +60% saturation) are frozen -- any change bumps `uZM_CREATUREGEN_VERSION`/`uZM_TEXTURESYNTH_VERSION` + forces a cold re-bake. The saturation factor + gallery floor are tunable named constants if more art direction is wanted.
- **Visual-gate / capture recipe (reusable at S8):** a windowed `ZENITH_AUTOMATED_TEST_REGISTER` test with `m_bRequiresGraphics=true` (auto-skips headless); tune the look via `Zenith_GraphicsOptions::Get()` SAVED in Setup + RESTORED in cleanup (bloom, skybox on/off+colour, auto-exposure OFF -> fixed exposure 1.0, IBL, `m_bSSREnabled`, `m_bTextEnabled`/`m_bQuadsEnabled` to kill UI bleed); at fixed exposure 1.0 directional light is O(1) lux (anchor on Flux_MaterialPreview sun=3.0); a smooth metallic material (metallic 1.0 / roughness ~0.06) + SSR gives a mirror floor; capture with `Flux_Screenshot::RequestDump(fsTgaPath)` (full-swapchain TGA; create the dir first; convert TGA->PNG with Python PIL for viewing). Run windowed via `zenith test Zenithmon --filter <TestName>` (NO --headless). `dbg_fHDRExposure` is a static -- NOT settable from a test; set SSR ONCE in Setup (no mid-run toggle -> avoids the SSR-toggle SRV crash).
- **Gate ORDER gotcha:** `Tools\run_unit_gate.ps1` boots the exe DIRECTLY without healing DLLs -- run `Repair-ZenithRuntimeDlls -ExeDir <exeDir>` (or `zenith test --headless`) FIRST, else `0xC0000135` (STATUS_DLL_NOT_FOUND). The exe hangs after the units line, so the gate times out + kills but captures `Unit tests complete: N ran...`; pass the CURRENT baseline (`-TimeoutSec 300`). Bump `-Baseline` in the gate cmd AND `.github/workflows/zm-tests.yml` together when ZM_ boot units change.
- **Matrix / git gotchas:** build non-default configs via msbuild with the ABSOLUTE sln path (`C:\dev\Zenith\Games\Zenithmon\zenithmon_win64.sln`) -- a relative path fails MSB1009 in a background job. And `git` in a Bash call runs in the persisted cwd -- use `git -C C:/dev/Zenith ...` (or don't `cd` away) so `git add <repo-relative>` resolves.
- **Working model:** MASTER-ONLY (ZM-D-031); local gate is the authority; `zm-tests` is the post-push backstop, fix forward on red. Sweep stray `zenithmon.exe` before ending each iteration.
