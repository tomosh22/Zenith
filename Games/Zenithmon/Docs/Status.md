# Zenithmon Status

**Last updated:** 2026-07-16
**Stage:** S4 (Asset generators) -- **ALL S4 CODE BOXES DONE.** Every generator family (creatures, creature-anims, humans, buildings, props) is COMPLETE, and `ZM_BakeManifest` (the per-family bake guard + byte-identical re-bake invariant) just landed (ZM-D-085). **The ONLY remaining S4 work is the S4 GATE -- a VISUAL sign-off (hard stop for the user).** S1/S2/S3 COMPLETE.
**Build:** GREEN -- `ZM_BakeManifest` re-ran the FULL 5-config matrix (Vulkan Debug/Release x True/False + `D3D12_vs2022_Debug_Win64_False` link proof), all build.
**Tests:** boot unit gate **1908 ran / 1907 passed / 0 failed / 1 skipped** (was 1905; +3 `ZM_Gen` BakeManifest units -- EnumerationMatchesRoster + the tools-only RebakeByteIdentical/GuardWarmStale, which RAN + PASSED); `zenith test Zenithmon --headless` **7 passed / 0 failed**. `.github/workflows/zm-tests.yml` baseline = **1908**.

## Current task

**S4 GATE -- windowed asset gallery + VISUAL SIGN-OFF (the next VISUAL GATE; HARD STOP for the user).** All S4 automated gate items are already green: the 4-config Vulkan matrix + D3D12 link proof (re-run at ZM_BakeManifest), the boot unit gate (1908), and `zenith test --headless` (7/0). Every family is locked by `ZM_Gen` units (determinism, structural validity, static/skeletal contract) + tools bake smokes (files land on disk; static `.zmodel` has no rig / creature `.zmodel` self-lists its clips). The **creature species gallery was already visually signed off (ZM-D-067)**; humans/buildings/props are NEW asset families added since. **Awaiting the user's direction on the S4 gate approach** -- either (a) close S4 on the existing creature-gallery sign-off + the new families' unit/bake-smoke coverage, or (b) build a fresh windowed gallery showing humans/buildings/props (via `ZM_BakeAllAssets()` in a gallery Setup -- the deferred wiring), capture screenshot evidence, and take a fresh visual sign-off. This is a visual gate = a standing-order HARD STOP; the loop parks here.

## Last completed

**`ZM_BakeManifest` (ZM-D-085) -- the last S4 code box.** New `Source/Gen/ZM_BakeManifest.{h,cpp}`: a per-family deterministic 12-byte stamp (`ZMBM` magic + u32-LE generator version + u32-LE expected-file count; mirrors the terrain `ZMTR` marker; no timestamps/paths) written atomically (temp+rename) to `game:<Family>/.manifest`. All-config `ZM_BakeManifestCheck` reads WARM iff the stamp is current AND every enumerated file is present non-empty (FAIL-OPEN); each `ZM_BakeAll*` now gates at top + stamps after a fully-successful bake. Tools-only `ZM_BakeAllAssets()` ANDs the four but is deliberately UNCALLED (boot/gallery wiring DEFERRED to this S4 gate -- an unconditional cold full-family bake on every unit-gate/CI/headless boot would be a large cost for no current benefit). NO version bumps; no generation byte moves. +3 `ZM_Gen` units. Plan -> implement (author cut off by a session limit mid-task; work recovered complete from disk + orchestrator-verified) -> FULL 5-config matrix + 1908 unit gate -> reviewer (clean; 1 cosmetic NIT left, documented).

## Notes for next agent / the S4 gate

- **This is a VISUAL GATE -- do NOT tick it or start S5 without the user's sign-off** (standing order). If the user chooses to build the new-family gallery: wire `ZM_BakeAllAssets()` (from `ZM_BakeManifest.h`, tools-only, guarded by the manifest so warm boots are fast) into a windowed gallery test Setup (mirror `ZM_AutoTests_Gallery.cpp`'s creature-dozen bake), add the humans/buildings/props to the gallery scene, run it windowed via a `--filter` test, capture screenshots (the SetWindowPos + CopyFromScreen recipe), write the evidence paths into Status.md under a `GATE-WAIT: S4 gallery visual sign-off` marker, append the automated results to DecisionLog.md, commit + push, and END. The user unblocks via StartPrompts prompt 4.
- **All S4 generators shipped:** `ZM_CreatureGen`(v3, 152 species, 15 files each incl. 6 clips) + `ZM_CreatureAnimGen`; `ZM_HumanGen`(v1, 34 models, ONE shared 16-bone rig + 9 shared clips); `ZM_BuildingGen`(v1, 30, static 4-file bundles) + `ZM_PropGen`(v1, 25, static 4-file bundles). Baked via the FOUR `ZM_GenCommon` bridges (`ZM_GenBakeMesh` own-skel / `ZM_GenBakeSkeleton` shared / `ZM_GenBakeMeshWithSharedSkeleton` bind-shared / `ZM_GenBakeStaticMesh` no-skel). `ZM_BakeManifest` gates re-bakes per family (version+file-existence stamp). Baked assets git-ignored.
- **After the S4 gate signs off:** S4 is COMPLETE -> S5 (Battle integration slice) is next on the critical path (battle scene + `ZM_EncounterZone`/`ZM_TallGrassSystem` + `ZM_BattleDirector` + `ZM_UI_BattleHUD` + engine E3 typewriter/E5 grass-reset). See Roadmap S5 + MasterPlan.
- **Doc cadence:** CI baseline + TestPlan + AssetManifest all reconciled at 1908/current. Future per-SC: bump CI baseline every SC; reconcile prose at item completion.
- **Gate ORDER + baseline (unchanged):** `zenith test Zenithmon --headless` FIRST (heals DLLs, else `0xC0000135`); boot unit gate hangs after the units line -> `run_unit_gate.ps1 -Baseline N -TimeoutSec 300` times out+kills but captures `Unit tests complete: N ran`. New files need `pwsh -NoProfile -File Build\regen.ps1`. Non-default configs via msbuild ABSOLUTE sln path (`C:\Program Files\Microsoft Visual Studio\18\Insiders\MSBuild\Current\Bin\MSBuild.exe`), serial only. `git -C C:/dev/Zenith ...`.
- **Working model:** MASTER-ONLY (ZM-D-031); local gate is the authority; `zm-tests` is the post-push backstop, fix forward on red. Sweep stray `zenithmon.exe` before ending. Orchestrator+subagents this session (Plan -> Implement -> Reviewer per box; Doc Maintainer for reference docs); only the orchestrator builds/tests/commits. A stray harness worktree exists at `.claude/worktrees/` -- NEVER regen/build/commit from it (Invariant 2). Never commit baked assets.
