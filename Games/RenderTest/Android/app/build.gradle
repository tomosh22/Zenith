plugins {
	id 'com.android.application'
}

android {
	namespace 'com.zenith.rendertest'
	compileSdk 34

	defaultConfig {
		applicationId 'com.zenith.rendertest'
		minSdk 26
		targetSdk 34
		versionCode 1
		versionName '1.0'

		ndk {
			abiFilters 'arm64-v8a'
		}
	}

	buildTypes {
		debug {}
		release {
			minifyEnabled false
		}
	}

	sourceSets {
		main {
			assets.srcDirs = [
				'../../Assets',
				'../../../../Zenith/Assets'
			]
			jniLibs.srcDirs = ['jniLibs']
		}
	}
}
