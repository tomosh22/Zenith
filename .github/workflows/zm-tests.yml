# Zenithmon tests (active PR gate from S0 -- see Games/Zenithmon/Docs/CIPolicy.md).
#
# Modeled on dp-tests.yml (the proven GPU-less gate; see its header for the
# full history of the headless-boot approach). Summary of the constraints:
#   - GitHub-hosted windows-latest runners have no GPU / Vulkan ICD. Headless is
#     a BUILD CONFIG: the Null_* configs compile the GPU-less Zenith/Null backend
#     and create the window hidden, so every render path RUNS against no-op
#     backend calls instead of being skipped by a runtime branch. Only tests that
#     read PIXELS (m_bRequiresGraphics) are skipped.
#   - Baked assets are git-ignored, so a fresh checkout has NO Assets/ content;
#     asset/scene-dependent automated tests must exists-guard and RequestSkip
#     (the commit-94813489 pattern). The CI backbone is the pure-logic headless
#     suites (boot unit tests + headless automated tests).
#   - slang-llvm.dll is > 100 MB, so Slang DLLs are fetched per run by
#     .github/actions/zenith-setup, never committed.
#
# Required check name: `zm-tests` (branch protection is a manual GitHub-UI
# step -- Games/Zenithmon/Docs/ManualSetupChecklist.md).

name: zm-tests

on:
  pull_request:
    branches: [master]
  push:
    branches: [master]
  workflow_dispatch:
    inputs:
      runner:
        description: 'Runner override (e.g. self-hosted with GPU).'
        required: false
        default: 'windows-latest'

permissions:
  contents: read

# Superseded PR pushes cancel the in-flight run; master pushes always complete.
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  zm-tests:
    name: zm-tests
    runs-on: ${{ github.event.inputs.runner || 'windows-latest' }}
    timeout-minutes: 60

    env:
      VCPKG_DEFAULT_TRIPLET: x64-windows
      DOTNET_CLI_TELEMETRY_OPTOUT: 1
      VCPKG_DISABLE_METRICS: 1
      SLANG_VERSION: '2026.1'
      VULKAN_SDK_VERSION: '1.3.290.0'
      # Single source for where the runner writes + the artifact uploads from.
      ZM_RESULTS_DIR: Build/artifacts/test_results/zenithmon

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Provision Zenith toolchain (MSBuild + vcpkg + Vulkan + Slang)
        uses: ./.github/actions/zenith-setup
        with:
          vulkan-sdk-version: ${{ env.VULKAN_SDK_VERSION }}
          slang-version: ${{ env.SLANG_VERSION }}

      - name: Regenerate solution (Sharpmake)
        shell: pwsh
        working-directory: Build
        run: |
          ./regen.ps1 -UseDotnet
          if ($LASTEXITCODE -ne 0) { exit 1 }

      - name: Build Zenithmon
        shell: pwsh
        run: |
          msbuild Games\Zenithmon\zenithmon_win64.sln `
              /t:Zenithmon `
              /p:Configuration=Vulkan_vs2022_Debug_Win64_True `
              /p:Platform=x64 `
              /p:WindowsTargetPlatformVersion=10.0 `
              -maxCpuCount `
              -nologo `
              -clp:Summary `
              -clp:ErrorsOnly
          if ($LASTEXITCODE -ne 0) { exit 1 }

      - name: Build Zenithmon (Null backend -- THE headless/CI build)
        shell: pwsh
        run: |
          # Everything below this point runs THIS exe. The Vulkan build above stays
          # as a compile proof for the real renderer; it is never executed here (a
          # Vulkan boot hangs in vkEnumeratePhysicalDevices on a GPU-less runner).
          msbuild Games\Zenithmon\zenithmon_win64.sln `
              /t:Zenithmon `
              /p:Configuration=Null_vs2022_Debug_Win64_True `
              /p:Platform=x64 `
              /p:WindowsTargetPlatformVersion=10.0 `
              -maxCpuCount `
              -nologo `
              -clp:Summary `
              -clp:ErrorsOnly
          if ($LASTEXITCODE -ne 0) { exit 1 }

      - name: Build Zenithmon (D3D12 null backend -- backend-neutrality link proof)
        shell: pwsh
        run: |
          # The D3D12 null backend is a compile + link conformance oracle proving
          # the Flux renderer surface is backend-neutral (zero real rendering;
          # Vulkan stays the shipping backend). _False omits editor/tools +
          # Slang/Vulkan and links the system d3d12/dxgi import libs only.
          msbuild Games\Zenithmon\zenithmon_win64.sln `
              /t:Zenithmon `
              /p:Configuration=D3D12_vs2022_Debug_Win64_False `
              /p:Platform=x64 `
              /p:WindowsTargetPlatformVersion=10.0 `
              -maxCpuCount `
              -nologo `
              -clp:Summary `
              -clp:ErrorsOnly
          if ($LASTEXITCODE -ne 0) { exit 1 }

      - name: Copy runtime DLLs into exe output dir
        shell: pwsh
        run: |
          $exeDir = 'Games\Zenithmon\Build\output\win64\null_vs2022_debug_win64_true'
          if (-not (Test-Path $exeDir)) {
              Write-Error "exe output dir does not exist: $exeDir"
              exit 1
          }
          # Repair-ZenithRuntimeDlls is the single source of truth for what an exe
          # dir needs: the whole slang dependency tree (the post-build event only
          # copies slang.dll) plus the assimp runtime tree. A FIRST build in a new
          # config leaves those absent and the exe dies with STATUS_DLL_NOT_FOUND
          # (exit 0xC0000135) before main() -- an empty log that reads as a build
          # failure. No vulkan-1.dll: the Null backend never loads a Vulkan loader.
          # NOTE the .\ prefix: without a leading dot PowerShell reads the argument
          # as a MODULE NAME and searches $PSModulePath, not the repo.
          Import-Module .\Build\zenith_buildsystem.psm1 -Force
          $copied = Repair-ZenithRuntimeDlls -ExeDir (Resolve-Path $exeDir).Path
          Write-Host "healed $($copied.Count) runtime DLL(s): $($copied -join ', ')"
          Write-Host "DLLs in exe output dir:"
          Get-ChildItem "$exeDir\*.dll" | Select-Object Name, @{N='SizeMB';E={[math]::Round($_.Length/1MB,1)}} | Sort-Object Name | Format-Table -AutoSize | Out-String | Write-Host

      - name: Verify exe boots far enough to enumerate tests
        shell: pwsh
        run: |
          $exe = 'Games\Zenithmon\Build\output\win64\null_vs2022_debug_win64_true\zenithmon.exe'
          # --list-automated-tests exits 0 cleanly without entering the main loop.
          # The Null build creates no Vulkan instance at all, so
          # vkEnumeratePhysicalDevices never runs on the GPU-less runner.
          $out = & $exe --list-automated-tests --skip-tool-exports --skip-unit-tests 2>&1
          $code = $LASTEXITCODE
          Write-Host "exe --list exited $code; first 30 lines:"
          $out | Select-Object -First 30 | Out-Host
          if ($code -ne 0) {
              Write-Host "... tail (last 40 lines) ..."
              $out | Select-Object -Last 40 | Out-Host
              Write-Error "exe failed to enumerate tests (exit $code) -- engine can't boot on this runner."
              exit 1
          }

      - name: Run boot unit tests (engine + Zenithmon ZM_* suites)
        shell: pwsh
        run: |
          # The enumerate step and `zenith test` both pass --skip-unit-tests for
          # speed, so the boot ZENITH_TEST suite -- engine units PLUS Zenithmon's
          # ZM_* data-core cases (the S1+ gate backbone) -- is exercised here via the
          # shared engine-gate helper. It boots the Null exe with tool-exports ON
          # (some engine unit tests reload generated assets), watchdog-kills the
          # known tools-build idle after the units line is logged, and asserts the
          # full registered count ran with 0 failed. Bump -Baseline when the ZM_*
          # (or engine) unit-test count changes -- see Docs/CIPolicy.md.
          #
          # -TimeoutSec IS NOT OPTIONAL HERE, and the default was actively dangerous.
          # run_unit_gate.ps1 defaults to 180s, but the watchdog exists to kill the
          # known tools-build IDLE *after* the units line is logged -- it is a hang
          # guard, not a runtime budget. Because the units line has to be written
          # before the kill, the timeout silently doubles as a ceiling on how long the
          # unit suite may take. MEASURED 2026-07-29 on an idle dev machine, seconds
          # from process start to the "Unit tests complete" line: 175, 193, 229, 235
          # -- i.e. the suite STRADDLES the old 180s default, and which side it lands
          # on is decided by machine load, not by the code under test. When it loses
          # that race the gate reports "no 'Unit tests complete' line in boot output",
          # which reads like a crash or a DLL load failure and sends the next person
          # hunting a phantom. 600s is pure headroom: a genuinely hung boot still
          # fails, just later. See ZM-D-163.
          #
          # ★ THIS PIN IS SET FROM AN OBSERVED LINE, NEVER ARITHMETIC. ZM-D-173
          # found the previous one already wrong -- it read 2804 while a clean
          # HEAD build produced 2809, so this gate was RED on master before that
          # work started. Whoever moves it runs a clean Null_ build, reads the
          # "Unit tests complete: N ran" line, and pastes N here.
          #
          # History, newest first (the DERIVATION is the audit trail; the OBSERVED
          # number is the authority):
          #   3151  input program WP0 (device foundations), observed 2026-08-11 on
          #                   a Null_ build
          #                   (3151 ran / 3149 passed / 0 failed / 2 skipped).
          #                   +24 ENGINE units, no ZM units: the Zenith_Input
          #                   FIFO/drain/edge/gamepad/lifecycle/simulator rework in
          #                   Zenith/Input/Zenith_Input.Tests.inl. See
          #                   run_unit_gate.ps1 for the per-test derivation.
          #   3127  TAA sky motion vectors, observed 2026-08-10 on a Null_ build
          #                   (3127 ran / 3125 passed / 0 failed / 2 skipped).
          #                   +4 ENGINE units, no ZM units: the TAASkyVelocity cases in
          #                   Flux/TAA/Flux_TAAJitter.Tests.inl, pinning the w = 0
          #                   point-at-infinity reprojection the sky's new velocity-only
          #                   pass rests on -- above all that camera TRANSLATION must not
          #                   move the sky (w = 0 drops the view matrix's translation
          #                   column), which is why the whole sky-smear class is invisible
          #                   to a translation-only or still capture. See run_unit_gate.ps1
          #                   for the per-test derivation.
          #   3123  TAA disocclusion fix, observed 2026-08-10 on a Null_ build
          #                   (3123 ran / 3121 passed / 0 failed / 2 skipped).
          #                   +6 ENGINE units, no ZM units: the disocclusion suite in
          #                   Flux/TAA/Flux_TAA_ResolveCPU.Tests.inl. TAA's disocclusion
          #                   test compared the history's stored depth against the CURRENT
          #                   frame's depth instead of the reprojection its own contract
          #                   specified, and compared against a POINT instead of the
          #                   neighbourhood depth RANGE -- so a camera dolly and the
          #                   sub-pixel jitter both read as disocclusions and history was
          #                   thrown away almost everywhere. See run_unit_gate.ps1 for the
          #                   per-test derivation.
          #   3117  Flux screen-space-quality review follow-ups, observed
          #                   2026-08-10 on a Null_ build
          #                   (3117 ran / 3115 passed / 0 failed / 2 skipped).
          #                   +11 ENGINE units, no ZM units: 6 in
          #                   Flux/SSAO/Flux_SSAO.Tests.inl (the three-field
          #                   Flux_SSAOSelection comparison, the committed-handle
          #                   selector's commit / rebuild-request / hold-until-
          #                   re-commit contract, the reciprocal-texel blur
          #                   constants and their 32-byte CB layout) and 5 in
          #                   Flux/Shadows/Flux_Shadows.Tests.inl (distinct
          #                   single-bit quality flags, the OR-fold, the shipping
          #                   defaults, the exact uint->float->uint round trip
          #                   through g_xParams2.y, the 96-byte GPU mirror).
          #                   NOTE the same change added a SlangProbes E5b probe
          #                   that does NOT appear in this count: SlangProbes is
          #                   gated on ZENITH_WINDOWS && ZENITH_VULKAN in
          #                   Flux_MaterialTable.cpp and this is a Null_ build.
          #   3106  GPU-driven particle path WIRED (its instance buffer had no
          #                   reader, its Initialise had no caller, and no emitter
          #                   ever registered, so the compute pass was inert),
          #                   observed 2026-08-10 on a Null_ build
          #                   (3106 ran / 3104 passed / 0 failed / 2 skipped).
          #                   +22 ENGINE units, all in the new
          #                   Flux/Particles/Flux_ParticleGPU.Tests.inl: the spawn
          #                   ring (7), blend-partition + indirect-command
          #                   addressing (4), the uINDIRECT_WORDS source-text pin
          #                   (1), pool registration lifetime against the live impl
          #                   (6), and the frame's CPU half incl. the arm/DISARM
          #                   latch (4). No ZM units.
          #   3084  Scene-publish guard + authoring-FP determinism (89fa3647),
          #                   observed 2026-08-10 on a Null_ build
          #                   (3084 ran / 3082 passed / 0 failed / 2 skipped).
          #                   +2 ENGINE units, both in Zenith_Editor.Tests.inl:
          #                   SceneSaveDeltaClassifiesPublish (the four
          #                   CompareWithFile verdicts + transient entities never
          #                   moving the serialized counts) and
          #                   HeadlessSaveNeverRewritesSceneAsset (the publish
          #                   policy end to end -- byte-identical afterwards under
          #                   Null, CHANGED under a real backend, so it asserts
          #                   something in either config).
          #   3082  Compressed-vertex Phase 6 T6.a review fix pass, observed
          #                   2026-08-09 on a Null_ build. +1 ENGINE unit: the
          #                   source-text pin tying Flux_ParticleUpdate.slang's
          #                   uINSTANCE_WORDS to the C++ mirror (nothing
          #                   reflectable carries a plain static const uint, so
          #                   the two spellings could drift silently).
          #   3081  Compressed-vertex Phase 6 T6.a, the instance-stream flips
          #                   (Text 56->36 B, Quads 72->52 B, Particles 32->20 B,
          #                   Gizmos 24->16 B), observed 2026-08-09 on a Null_
          #                   build. +19 ENGINE units, no ZM units: 3 in
          #                   Flux_VertexCodec.Tests.inl for the new uint16x4 lane
          #                   (order, in-range identity, and that it SATURATES
          #                   rather than wraps so a truncated-negative screen rect
          #                   stays off screen), then 4/5/4/3 in the new
          #                   Flux_TextVertex / Flux_QuadInstance /
          #                   Flux_ParticleInstance / Flux_GizmoVertex .Tests.inl,
          #                   each reading the packed words back out of the raw
          #                   object bytes at the GENERATED offsets plus the
          #                   not-compressed rationales (4K pixel lanes,
          #                   far-from-origin world positions, the Quads gradient's
          #                   negative sentinel). The Gizmos three are
          #                   ZENITH_TOOLS-only, like the feature; this gate builds
          #                   Null_..._True, so they are present here.
          #   3062  Compressed-vertex Phase 5 T5.a adversarial-review fix pass,
          #                   observed 2026-08-08 on a Null_ build. +4 ENGINE units,
          #                   all in Flux_Terrain.Tests.inl: the TerrainConstants CB
          #                   fill vs the authored box (values, not just layout),
          #                   quant-bridge writes landing at the SHADER's reflected
          #                   offsets, decode->re-encode word idempotence (the
          #                   sculpt/carve seam-safety property), and the UV
          #                   write/read round trip incl. the integer snap.
          #   3058  Compressed-vertex Phase 5 T5.a, the terrain compression flip
          #                   (28 -> 20 B/vertex), observed 2026-08-08 on a Null_
          #                   build. +2 ENGINE units, both in Flux_VertexCodec
          #                   .Tests.inl: the frozen Slang transcription of
          #                   Flux_DequantPosition (plus the fixed-function
          #                   VK_FORMAT_R16G16B16A16_SNORM fetch) swept against the
          #                   CPU codec, and the out-of-box clamp. That function
          #                   gained its first caller with this flip.
          #   3056  Compressed-vertex Phase 4 T4.a adversarial-review fix pass,
          #                   observed 2026-08-08 on a Null_ build. +7 ENGINE units:
          #                   codec +4 (hand-derived half word pins, frozen Slang
          #                   half-transcription parity incl. the pinned tie
          #                   divergence, bit-classified positive-finite branches,
          #                   over-unity bone-weight pre-scale), packer +1 (the
          #                   half-range position guard), interleave +1 (skin-output
          #                   encoder vs static packer byte identity), skinning +1
          #                   (negative-determinant blends flip the bitangent sign).
          #                   ZM's own registrations unmoved.
          #   3049  Compressed-vertex Phase 4 T4.a (the mesh compression flip)
          #                   observed 2026-08-08 on a Null_ build. +5 NET ENGINE
          #                   units (1419 -> 1424): the two mesh-family memcmp
          #                   goldens now compare against an INDEPENDENT codec
          #                   transcription rather than the frozen float32
          #                   interleave loops (the flip changes those bytes by
          #                   design), plus the skin-input/static prefix identity,
          #                   the mirrored-frame bitangent SIGN, the procedural
          #                   mesh-pipeline pack, the packed skin-vertex codec round
          #                   trip and the one-byte bone sentinel. ZM's own
          #                   registrations unmoved.
          #   3044  Compressed-vertex Phase 3 T3.b (mesh-family replacement)
          #                   observed 2026-08-08 on a Null_ build. +4 NET ENGINE
          #                   units (1415 -> 1419): the mesh-family suite was
          #                   rewritten from 3 hand-layout tests to 7 — three
          #                   byte-for-byte memcmp goldens (the static pack and the
          #                   skin-input build, each against a FROZEN transcription
          #                   of the interleave loop it replaced, over 7 adversarial
          #                   meshes), the ported layout/defaults expectations, and
          #                   the bind-pose position override. ZM's own
          #                   registrations unmoved.
          #   3040  Compressed-vertex Phase 3 T3.a (the reflection-driven packer)
          #                   observed 2026-08-08 on a Null_ build. +15 ENGINE units
          #                   (1400 -> 1415): the Flux_PackVertices suite — the
          #                   whole-layout byte contract over two mixed-format
          #                   tables, the canonical attribute defaults, (semantic,
          #                   index) source keying, the 4-lane TANGENT handedness
          #                   both signs plus an authored w winning, SNORM10
          #                   pre-normalisation and its zero-length fallback, the
          #                   lane pad/truncate rules, the POSITION quant box,
          #                   stride-advance across 3 vertices, the no-op shapes and
          #                   binding-1 skipping. ZM's own registrations unmoved.
          #   3025  Compressed-vertex Phase 2 T2.d (review hardening)
          #                   observed 2026-08-08 on a Null_ build. +12 ENGINE units
          #                   (1388 -> 1400): the vertex-layout-validation suite
          #                   driving the boot tripwire's comparator through the
          #                   OK path, both no-input spellings, and the COUNT /
          #                   STRIDE / ELEMENT / SEMANTIC mismatch branches real
          #                   boots never take. ZM's own registrations unmoved.
          #   3013  Compressed-vertex Phase 2 T2.b (codegen vertex-layout emission)
          #                   observed 2026-08-07 on a Null_ build. +11 ENGINE
          #                   units (1377 -> 1388): the Flux_VertexLayoutDesc
          #                   equality suite (element-wise over distinct arrays,
          #                   drift in offset/type/semantic index/stride/count, the
          #                   canonical empty layout, the semantic vocabulary) plus
          #                   three Codegen emission units -- baked table content,
          #                   the null layout, and the hard failure on a semantic
          #                   the vocabulary does not name. ZM's own registrations
          #                   unmoved.
          #   3002  Compressed-vertex Phase 2 T2.a (reflection sidecar v6)
          #                   observed 2026-08-07 on a Null_ build. +10 ENGINE
          #                   units (1367 -> 1377): the Flux_ShaderReflection
          #                   vertex-input suite -- tight-pack offsets/strides,
          #                   independent per-binding packing, the empty table, the
          #                   v6 DataStream round-trip, the adopt-once merge, and
          #                   the pure [VtxFmt] vocabulary / inference / override
          #                   validation. ZM's own registrations unmoved.
          #   2992  Compressed-vertex Phase 1 (format vocabulary + codec)
          #                   observed 2026-08-07 on a Null_ build. +15 ENGINE
          #                   units (1352 -> 1367): the Flux_VertexCodec suite --
          #                   half/snorm16/unorm16/unorm8/uint8 round-trips and
          #                   lane order, SNORM10 bit-identity against the
          #                   transcribed legacy terrain packer, the position
          #                   quantisation box, and the bone weight/index lanes.
          #                   ZM's own registrations unmoved.
          #   2977  Debug-variable audit remediation  observed 2026-08-07 on a
          #                   Null_ build. +2 ENGINE units (1350 -> 1352): the AI
          #                   debug-draw routing pins (master-toggle short-circuit,
          #                   safe against an un-Initialise()d SquadManager) added
          #                   when the AI/* debug-variable subtree was given its
          #                   first call site. ZM's own registrations unmoved.
          #   2975  GPU-grass overhaul Phase 7 (types authoring)  observed
          #                   2026-08-06 on a Null_ build. +6 ENGINE units
          #                   (1344 -> 1350): grass-type name->field mapping,
          #                   working-copy isolation, the AddStep_GrassTypes*
          #                   family end-to-end, save+apply, enum contiguity.
          #   2969  GPU-grass overhaul Phase 6 (displacement)  observed 2026-08-06
          #                   on a Null_ build. +4 ENGINE units (1340 -> 1344): the
          #                   displacement trail map's re-anchor pure functions
          #                   (sub-texel stability, one-texel steps, integral shift,
          #                   frame-rate-independent decay).
          #   2965  GPU-grass overhaul Phase 5 (shadow casting)  observed 2026-08-06
          #                   on a Null_ build. +3 ENGINE units (1337 -> 1340): grass
          #                   shadow enable-flag truth table, active-slot-mask
          #                   exclusion, and the grass-before-shadows registration
          #                   pin (reader-links-earlier-writer).
          #   2962  GPU-grass overhaul Phase 4 (THE SWAP)  observed 2026-08-06 on a
          #                   Null_ build. +11 net ENGINE units (1326 -> 1337): the
          #                   CPU-scatter grass died with its 3 member-poking tests;
          #                   14 new units cover the GPU pipeline's pure mirrors,
          #                   impl surface, and type-table serialization. ZM's own
          #                   registrations unmoved (registry stays 55).
          #   2951  GPU-grass overhaul Phase 3  observed 2026-08-06 on a Null_
          #                   build. +4: TerrainEditor GrassType-map units (hard-edged
          #                   categorical dab, 1-byte-texel stroke-undo round-trip,
          #                   GrassType.ztxtr save/load + absent-file fallback, sculpt-
          #                   notify latch). All ENGINE units: 1322 -> 1326.
          #   2947  GPU-grass overhaul Phase 2  observed 2026-08-06 on a Null_
          #                   build. +2: FluxBufferReadback units pinning the new
          #                   Flux_MemoryManager::DownloadBufferData headless zero-fill
          #                   + exact-size contract. Both ENGINE units: 1320 -> 1322.
          #   2945  GPU-grass overhaul Phase 1  observed 2026-08-06 on a Null_
          #                   build. +20: Flux/Vegetation/Flux_Grass.Tests.inl gained the
          #                   FluxGrassTypes suite -- pure tile-selection / wind /
          #                   map-sampling / GPU-record-packing functions
          #                   (Flux_GrassTypes.h) plus pinned integer-hash vectors the
          #                   Shaders/Common/Noise.slang GPU mirror is held to. All
          #                   twenty are ENGINE units, so the engine default moves in
          #                   lockstep: 1300 -> 1320.
          #   2925  terrain exporter border chunks  observed 2026-08-06 on a Null_
          #                   build. +5: Flux/Terrain/Flux_Terrain.Tests.inl gained the
          #                   FluxTerrainSourceGrid suite (Flux_TerrainSourceGrid.h) when
          #                   the exporter was found to bake INCOMPLETE chunks at the
          #                   positive grid border -- its source grid was one sample per
          #                   edge short, so the last chunk column/row had no neighbour to
          #                   stitch from and 127 of 4096 chunks were silently dropped from
          #                   LOW LOD and physics alike. All five are ENGINE units, so the
          #                   engine default moves in lockstep: 1295 -> 1300.
          #   2920  terrain wireframe under TAA  observed 2026-08-06 on a Null_
          #                   build. +11 over the PIN of 2909, which was ALREADY STALE
          #                   by 5: the CommandLine ParseArgs characterization tests
          #                   (2026-08-05) moved run_unit_gate.ps1's engine default
          #                   1284 -> 1289 and did not move this pin, so HEAD was really
          #                   2914 and this gate was RED before this work started.
          #                   +6 new: Flux/Terrain/Flux_Terrain.Tests.inl -- the terrain
          #                   G-buffer pipeline variant 2x2, added when the terrain
          #                   "Wireframe" checkbox was found dead in every default run
          #                   (the TAA velocity latch beat the wireframe flag, and TAA
          #                   ships ON). All six are ENGINE units, so the engine default
          #                   moves in lockstep: 1289 -> 1295.
          #   2909  ZM-D-184 rival spawn clearance  observed 2026-08-04 on a Null_
          #                   build. +1: ZM_Interaction/
          #                   Vesper_SpawnsClearOfTheGroundNotOnIt. The engine half of
          #                   ZM-D-184 (the physics substep cap) adds no unit of its
          #                   own; it is covered by the ZM automated tests that load
          #                   Dawnmere. Engine default stays 1284.
          #   2908  ZM-D-183 frozen rival facing  observed 2026-08-04 on a Null_
          #                   build. +2: ZM_Interaction/
          #                   Vesper_FrozenFacingIsExactlyTheDeclaredBits and the new
          #                   TU Tests/ZM_Tests_CommittedSceneBytes.cpp's
          #                   DawnmereCarriesTheFrozenRivalFacingBitExactly -- the
          #                   first headless guard on the COMMITTED .zscen bytes.
          #                   Engine-side count unmoved (the new automation step has
          #                   no unit of its own; it is covered by the ZM units that
          #                   ride it). Engine default stays 1284.
          #   2906  swapchain policy  observed 2026-08-04 on a Null_ build. +13:
          #                   Flux/Flux_SwapchainPolicy.Tests.inl -- the surface
          #                   preTransform / zero-extent / SUBOPTIMAL-rebuild
          #                   decisions, pulled OUT of Zenith/Vulkan so they are
          #                   reachable at all from a Null_ build (the backend is
          #                   build-excluded from it). Engine default 1271 -> 1284.
          #   2893  exit-after-unit-tests  observed 2026-08-03 on a Null_ build.
          #                   +1: CommandLine::ExitAfterUnitTestsIsNotAnAutomatedTestRun.
          #                   The gate now boots with --exit-after-unit-tests and the exe
          #                   self-terminates, so -TimeoutSec is a real hang guard again
          #                   (a run went 600s -> ~75s). Engine default 1270 -> 1271.
          #   2892  per-test timings  observed 2026-08-03 on a Null_ build. +1:
          #                   Core::UnitTestTimingReport, covering the new
          #                   --unit-test-timings plumbing. Engine default 1269 -> 1270.
          #   2891  boot-profiling  observed 2026-08-03 on a Null_ build. +27 over
          #                   the PIN of 2864, which was ALREADY STALE by 4: the
          #                   shadow-cascade fix (33531b6a) added four engine units
          #                   and moved neither this pin nor the engine default, so
          #                   HEAD was really 2868. +23 new: 18 boot-capture units
          #                   in Zenith_Profiling.Tests.inl (+1 CommandLine boot-flag
          #                   split), 1 dump-coordinator latch unit in
          #                   Zenith_Main.Tests.inl, 1 present-latch unit, 1
          #                   automation-session-gating unit, 2 boot-timeline
          #                   helper units. All are ENGINE units, so
          #                   run_unit_gate.ps1's engine default moves in lockstep:
          #                   1242 -> 1269.
          #   2864  ZM-D-182  observed 2026-08-02 on a clean Null_ build. +1: the
          #                   ZM_HumanVisual rig-rebind unit. run_unit_gate.ps1's
          #                   engine default stays 1242 -- ZM-D-182 added no unit
          #                   under Zenith/.
          #   2863  ZM-D-181  observed. +14 = five explicit-collider ENGINE units
          #                   (which every game inherits, and which took the engine
          #                   gate 1237 -> 1242) + nine game units: five
          #                   ZM_HumanVisual, two ZM_Gen human-anchor, one creature
          #                   origin-policy, one Home-exterior envelope.
          #   2849  ZM-D-180  name tags.
          #   2825  ZM-D-174  +8 ProfLab placement units.
          #   2817  ZM-D-173  = 2809 + 8: four ENGINE sensor-raycast units plus
          #                   two camera fixtures and two Home-placement units.
          $exe = 'Games\Zenithmon\Build\output\win64\null_vs2022_debug_win64_true\zenithmon.exe'
          pwsh -NoProfile -File Tools\run_unit_gate.ps1 -Exe $exe -Baseline 3151 -TimeoutSec 600
          if ($LASTEXITCODE -ne 0) { Write-Error "boot unit tests failed (see log above)"; exit $LASTEXITCODE }

      - name: Run Zenithmon tests
        shell: pwsh
        run: |
          # zenith test batches every registered test in ONE process. --headless
          # selects the Null-backend exe built above; pixel-reading tests
          # (m_bRequiresGraphics) are skipped there, and asset-dependent tests
          # RequestSkip on the assets-absent checkout.
          & .\zenith.bat test Zenithmon --headless --results-dir $env:ZM_RESULTS_DIR
          $testExit = $LASTEXITCODE
          Write-Host "zenith test exit: $testExit"
          if ($testExit -ne 0) { exit $testExit }

      - name: Upload test result JSON artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: zm-test-results
          path: ${{ env.ZM_RESULTS_DIR }}/
          if-no-files-found: warn
